## THIS FILE WAS GENERATED BY MIB2SCHEMA
## For attributes containing printable ASCII
## characters, you may add a format string
## specification in the attribute characteristics
## line
## As described in RFC 1157, some agents cannot
## accept messages whose length exceeds 484 octets
## For those groups whose var-bind list has many
## entries, you may split them up into different
## var-bind lists
proxy ATM-SOFT-PVC-MIB
description "ATM-SOFT-PVC-MIB agent"
serial 2
rpcid 100122
(

## Enumerated Type Definitions

	enum	atmSoftPvcCallFailuresTrapEnableE (
	   1	"true"
	   2	"false"
	)

	enum	atmSoftPVccTargetSelectTypeE (
	   1	"required"
	   2	"any"
	)

	enum	atmSoftPVccOperStatusE (
	   1	"other"
	   2	"establishmentInProgress"
	   3	"connected"
	   4	"retriesExhausted"
	   5	"noAddressSupplied"
	   6	"lowerLayerDown"
	)

	enum	atmSoftPVccRestartE (
	   1	"restart"
	   2	"noop"
	)

	enum	atmSoftPVccRowStatusE (
	   1	"active"
	   2	"notInService"
	   3	"notReady"
	   4	"createAndGo"
	   5	"createAndWait"
	   6	"destroy"
	)

	enum	atmSoftPVpcTargetSelectTypeE (
	   1	"required"
	   2	"any"
	)

	enum	atmSoftPVpcOperStatusE (
	   1	"other"
	   2	"establishmentInProgress"
	   3	"connected"
	   4	"retriesExhausted"
	   5	"noAddressSupplied"
	   6	"lowerLayerDown"
	)

	enum	atmSoftPVpcRestartE (
	   1	"restart"
	   2	"noop"
	)

	enum	atmSoftPVpcRowStatusE (
	   1	"active"
	   2	"notInService"
	   3	"notReady"
	   4	"createAndGo"
	   5	"createAndWait"
	   6	"destroy"
	)

	enum	atmInterfaceSoftPvcAddressRowStatusE (
	   1	"active"
	   2	"notInService"
	   3	"notReady"
	   4	"createAndGo"
	   5	"createAndWait"
	   6	"destroy"
	)


## Group and Table Declarations

	table	atmSoftPVccTable
	description "The (conceptual) table used to manage Soft
        Permanent Virtual Channel Connections (Soft PVCCs).
        The Soft PVCC table is applicable only to switches."
	characteristics "-K ONE"
	(
		notaccessible int	atmSoftPVccLeafReference
		description 	"An arbitrary integer which, in the case of the
        source VCL having an atmVclCastType of
        'p2mpRoot', serves to distinguish between the 
        multiple leaves attached to a root of a 
        point-to-multipoint Soft PVCC. If the atmVclCastType 
        is not 'p2mpRoot' the value 1 shall be used."
		characteristics "-N atmSoftPVccLeafReference -O 1.3.6.1.4.1.353.5.5.1.1.2.1.1 -T INTEGER -A NA -X equal -F 0"

		readwrite octet[20]	atmSoftPVccTargetAddress
		description 	"The target ATM Address of this Soft PVCC.  If no
                      address is supplied, no attempts to establish the
                      Soft PVCC are initiated."
		characteristics "-N atmSoftPVccTargetAddress -O 1.3.6.1.4.1.353.5.5.1.1.2.1.2 -T STRING -A RW -X equal -F 0"

		readwrite enum atmSoftPVccTargetSelectTypeE	atmSoftPVccTargetSelectType
		description 	"Indicates whether the target VPI/VCI values
        are to be used at the destination.
        
        If the value 'any' is specified, the destination
        switch will choose the VPI/VCI values. In such a
        case, once the Soft PVCC atmSoftPVccOperStatus
        value is 'connected', the value of this object
        changes to 'required', such that the same VPI/VCI
        values will continue to be used even if the connection
        is subsequently torn down and re-established. The
        VPI/VCI values chosen will be available for reading in 
        atmSoftPVccTargetVpi and atmSoftPVccTargetVci.
        
        If the value 'required' is specified, then values
        must be supplied for objects atmSoftPVccTargetVpi
        and atmSoftPVccTargetVci prior to activation of the
        row. These values are then to be used at the destination."
		characteristics "-N atmSoftPVccTargetSelectType -O 1.3.6.1.4.1.353.5.5.1.1.2.1.3 -T INTEGER -A RW -X equal -F 0"

		readwrite int	atmSoftPVccTargetVpi
		description 	"The VPI value of the VCL used at the target interface.
        This value is not relevant when the value of 
        atmSoftPVccTargetSelectType is 'any'."
		characteristics "-N atmSoftPVccTargetVpi -O 1.3.6.1.4.1.353.5.5.1.1.2.1.4 -T INTEGER -A RW -X equal -F 0"

		readwrite int	atmSoftPVccTargetVci
		description 	"The VCI value of the VCL used at the target interface.
        This value must be filled in when the
        atmSoftPVccTargetSelectType is set to 'required'.This 
        value is not relevant when the value of 
        atmSoftPVccTargetSelectType is 'any'."
		characteristics "-N atmSoftPVccTargetVci -O 1.3.6.1.4.1.353.5.5.1.1.2.1.5 -T INTEGER -A RW -X equal -F 0"

		readonly int	atmSoftPVccLastReleaseCause
		description 	"Value of the Cause field of the Cause
        Information Element in the last RELEASE
        signalling message received for this Soft PVCC.
        Indicates the reason for the Release."
		characteristics "-N atmSoftPVccLastReleaseCause -O 1.3.6.1.4.1.353.5.5.1.1.2.1.6 -T INTEGER -A RO -X equal -F 0"

		readonly octet[8]	atmSoftPVccLastReleaseDiagnostic
		description 	"Value of the first 8 bytes of diagnostic information
        from the Cause field of the Cause Information Element
        in the last RELEASE signalling message received for
        this Soft PVCC."
		characteristics "-N atmSoftPVccLastReleaseDiagnostic -O 1.3.6.1.4.1.353.5.5.1.1.2.1.7 -T STRING -A RO -X equal -F 0"

		readonly enum atmSoftPVccOperStatusE	atmSoftPVccOperStatus
		description 	"Describes the status of the Soft PVCC.  Valid values
                      are:
                         other              - none of the types specified below
                         establishmentInProgress - connection or party is not
                                                   operational, but setup or add
                                                   party attempts are ongoing
                         connected          - connection or party is currently
                                              operational
                         retriesExhausted   - retry limit has been reached and
                                              setup or add party attempts have
                                              ceased
                         noAddressSupplied  - no remote address has been
                                              configured, so no setup or add
                                              party attempts are initiated
                         lowerLayerDown     - underlying ATM interface is not
                                              operational
                      When the row is not 'active', the value of this
        object is 'other'."
		characteristics "-N atmSoftPVccOperStatus -O 1.3.6.1.4.1.353.5.5.1.1.2.1.8 -T INTEGER -A RO -X equal -F 0"

		readwrite enum atmSoftPVccRestartE	atmSoftPVccRestart
		description 	"When the value is set to 'restart' the Soft PVCC
        is released if necessary and a new setup procedure
        is begun. As a result of this action, the
        atmSoftPVccOperStatus object transitions to
        'establishmentInProgress' (if not already in this state)
        and the atmSoftPVccRetryFailures object is cleared
        
        When the value is set to 'noop' no operation is
        performed. When read, the value 'noop' is returned."
		characteristics "-N atmSoftPVccRestart -O 1.3.6.1.4.1.353.5.5.1.1.2.1.9 -T INTEGER -A RW -X equal -F 0"

		readwrite int	atmSoftPVccRetryInterval
		description 	"Defines the period to wait before attempting
        to establish the Soft PVCC after the first failed call
        attempt.  The time to wait between subsequent call
                      attempts may differ to implement a backoff scheme.
                      Zero represents an infinite interval indicating no
        retries."
		characteristics "-N atmSoftPVccRetryInterval -O 1.3.6.1.4.1.353.5.5.1.1.2.1.10 -T INTEGER -A RW -X equal -F 0"

		readonly int	atmSoftPVccRetryTimer
		description 	"Indicates the current value of the retry timer for
        this connection. When the value reaches zero an attempt
        will be made to establish the Soft PVCC. When the timer
        is not running, the value zero shall be returned."
		characteristics "-N atmSoftPVccRetryTimer -O 1.3.6.1.4.1.353.5.5.1.1.2.1.11 -T INTEGER -A RO -X equal -F 0"

		readwrite int	atmSoftPVccRetryThreshold
		description 	"Indicates the number of consecutive call setup attempts for
        the same Soft PVCC which need to fail before the
        atmSoftPvcCallFailures object is incremented. A value of
        zero indicates that an infinite number of call attempts
        are required to increment the atmSoftPvcCallFailures object
        and thus disables alarms for the Soft PVCC."
		characteristics "-N atmSoftPVccRetryThreshold -O 1.3.6.1.4.1.353.5.5.1.1.2.1.12 -T INTEGER -A RW -X equal -F 0"

		readonly int	atmSoftPVccRetryFailures
		description 	"Indicates how many attempts to establish the connection
        have failed.  This count is reset whenever a connection
        is successfully established or the Soft PVCC is restarted."
		characteristics "-N atmSoftPVccRetryFailures -O 1.3.6.1.4.1.353.5.5.1.1.2.1.13 -T INTEGER -A RO -X equal -F 0"

		readwrite int	atmSoftPVccRetryLimit
		description 	"Sets a maximum limit on how many consecutive unsuccessful
        call setup attempts can be made before stopping the attempt
        to set up the connection. If this limit is reached then
        management action will be required (e.g. setting 
        atmSoftPVccRestart to 'restart') to initiate a new attempt
        to establish the connection. A value of zero indicates
        no limit - the attempts will continue until successful."
		characteristics "-N atmSoftPVccRetryLimit -O 1.3.6.1.4.1.353.5.5.1.1.2.1.14 -T INTEGER -A RW -X equal -F 0"

		readwrite enum atmSoftPVccRowStatusE	atmSoftPVccRowStatus
		description 	"Used to create and delete a Soft PVCC. When this
        object is set to 'active' an attempt is made to
        set up the Soft PVCC. When this object has the value
        'active' and is set to another value, any
        set-up or connection in-progress is released."
		characteristics "-N atmSoftPVccRowStatus -O 1.3.6.1.4.1.353.5.5.1.1.2.1.15 -T INTEGER -A RW -X equal -F 0"

	)

	table	atmSoftPVpcTable
	description "The (conceptual) table used to manage Soft
        Permanent Virtual Path Connections (Soft PVPCs)
        The Soft PVPC table is applicable only to switches."
	characteristics "-K ONE"
	(
		notaccessible int	atmSoftPVpcLeafReference
		description 	"An arbitrary integer which, in the case of the
        source VPL having a atmVplCastType of
        'p2mpRoot', serves to distinguish between the
        multiple leaves attached to a root of a
        point-to-multipoint Soft PVPC.
        
        If the atmVplCastType is not 'p2mpRoot', the
        value 1 shall be used."
		characteristics "-N atmSoftPVpcLeafReference -O 1.3.6.1.4.1.353.5.5.1.1.3.1.1 -T INTEGER -A NA -X equal -F 0"

		readwrite octet[20]	atmSoftPVpcTargetAddress
		description 	"The target ATM Address of this Soft PVPC.  If no
                      address is supplied, no attempts to establish the
                      Soft PVPC are initiated."
		characteristics "-N atmSoftPVpcTargetAddress -O 1.3.6.1.4.1.353.5.5.1.1.3.1.2 -T STRING -A RW -X equal -F 0"

		readwrite enum atmSoftPVpcTargetSelectTypeE	atmSoftPVpcTargetSelectType
		description 	"Indicates whether the target VPI value
        is to be used at the destination.
        If the value 'any' is specified, the
        destination switch will choose the VPI
        value. In such a case, once the Soft PVPC
        atmSoftPVpcOperStatus value is 'connected',
        the value of this object changes to 'required',
        such that the same VPI value will continue to
        be used even if the connection is subsequently
        torn down and re-established. The VPI value
        chosen will be available for reading in
        atmSoftPVpcTargetVpi.
        
        If the value 'required' is specified, then
        a value must be supplied for object
        atmSoftPVpcTargetVpi prior to activation
        of the row. This value is then to be used
        at the destination."
		characteristics "-N atmSoftPVpcTargetSelectType -O 1.3.6.1.4.1.353.5.5.1.1.3.1.3 -T INTEGER -A RW -X equal -F 0"

		readwrite int	atmSoftPVpcTargetVpi
		description 	"The VPI value of the VPL used at the
        target interface.
        
        This value must be filled in when the
        atmSoftPVpcTargetSelectType is set to 'required'.
        This value is not relevant when the value of
        atmSoftPVpcTargetSelectType is 'any'."
		characteristics "-N atmSoftPVpcTargetVpi -O 1.3.6.1.4.1.353.5.5.1.1.3.1.4 -T INTEGER -A RW -X equal -F 0"

		readonly int	atmSoftPVpcLastReleaseCause
		description 	"Value of the Cause field of the Cause
        Information Element in the last RELEASE
        signalling message received for this Soft PVPC.
        Indicates the reason for the Release."
		characteristics "-N atmSoftPVpcLastReleaseCause -O 1.3.6.1.4.1.353.5.5.1.1.3.1.5 -T INTEGER -A RO -X equal -F 0"

		readonly octet[8]	atmSoftPVpcLastReleaseDiagnostic
		description 	"Value of the first 8 bytes of diagnostic information
        from the Cause field of the Cause Information Element
        in the last RELEASE signalling message received for
        this Soft PVPC."
		characteristics "-N atmSoftPVpcLastReleaseDiagnostic -O 1.3.6.1.4.1.353.5.5.1.1.3.1.6 -T STRING -A RO -X equal -F 0"

		readonly enum atmSoftPVpcOperStatusE	atmSoftPVpcOperStatus
		description 	"Describes the status of the Soft PVPC. 
                    other              - none of the types specified below
                    establishmentInProgress - connection or party is not
                                              operational, but setup or add
                                              party attempts are ongoing
                    connected          - connection or party is currently
                                         operational
                    retriesExhausted   - retry limit has been reached and
                                         setup or add party attempts have
                                         ceased
                    noAddressSupplied  - no remote address has been
                                         configured, so no setup or add
                                         party attempts are initiated
                    lowerLayerDown     - underlying ATM interface is not
                                         operational
                 When the row is not 'active', the value of this
        object is 'other'."
		characteristics "-N atmSoftPVpcOperStatus -O 1.3.6.1.4.1.353.5.5.1.1.3.1.7 -T INTEGER -A RO -X equal -F 0"

		readwrite enum atmSoftPVpcRestartE	atmSoftPVpcRestart
		description 	"When the value is set to 'restart', the Soft PVPC is
        released if necessary and a new setup procedure is begun.
        As a result of this action, the atmSoftPVpcOperStatus
        object transitions to 'establishmentInProgress' (if not
        already in this state) and the atmSoftPVpcRetryFailures
        object is cleared.
        
        When the value is set to 'noop', no operation is performed.
        When read, the value 'noop' is returned."
		characteristics "-N atmSoftPVpcRestart -O 1.3.6.1.4.1.353.5.5.1.1.3.1.8 -T INTEGER -A RW -X equal -F 0"

		readwrite int	atmSoftPVpcRetryInterval
		description 	"Defines the period to wait before attempting
        to establish the Soft PVPC after the first failed 
                      call attempt.  The time to wait between subsequent
                      call attempts may differ to implement a backoff scheme.
                      Zero represents an infinite interval indicating no 
                      retries."
		characteristics "-N atmSoftPVpcRetryInterval -O 1.3.6.1.4.1.353.5.5.1.1.3.1.9 -T INTEGER -A RW -X equal -F 0"

		readonly int	atmSoftPVpcRetryTimer
		description 	"Indicates the current value of the retry timer for
        this connection. When the value reaches zero an attempt
        will be made to establish the Soft PVPC. When the
        timer is not running, the value zero shall be returned."
		characteristics "-N atmSoftPVpcRetryTimer -O 1.3.6.1.4.1.353.5.5.1.1.3.1.10 -T INTEGER -A RO -X equal -F 0"

		readwrite int	atmSoftPVpcRetryThreshold
		description 	"Indicates the number of consecutive call setup attempts for
        the same Soft PVPC which need to fail before the
        atmSoftPvcCallFailures object is incremented. A value of
        zero indicates that an infinite number of call attempts
        are required to increment the atmSoftPvcCallFailures object
        and thus disables alarms for the Soft PVPC."
		characteristics "-N atmSoftPVpcRetryThreshold -O 1.3.6.1.4.1.353.5.5.1.1.3.1.11 -T INTEGER -A RW -X equal -F 0"

		readonly int	atmSoftPVpcRetryFailures
		description 	"Indicates how many attempts to establish the connection
        have failed.  This count is reset whenever a connection
        is successfully established or the Soft PVPC is restarted."
		characteristics "-N atmSoftPVpcRetryFailures -O 1.3.6.1.4.1.353.5.5.1.1.3.1.12 -T INTEGER -A RO -X equal -F 0"

		readwrite int	atmSoftPVpcRetryLimit
		description 	"Sets a maximum limit on how many consecutive unsuccessful
        call setup attempts can be made before stopping the attempt
        to set up the connection. If this limit is reached then
        management action will be required (e.g. setting
        atmSoftPVpcRestart to 'restart') to initiate a new attempt
        to establish the connection. A value of zero indicates no
        limit - the attempts will continue until successful."
		characteristics "-N atmSoftPVpcRetryLimit -O 1.3.6.1.4.1.353.5.5.1.1.3.1.13 -T INTEGER -A RW -X equal -F 0"

		readwrite enum atmSoftPVpcRowStatusE	atmSoftPVpcRowStatus
		description 	"Used to create and delete a Soft PVPC. When this
        object is set to 'active' an attempt is made to
        set up the Soft PVPC. When this object has the value
        'active' and is set to another value, any
        set-up or connection in-progress is released."
		characteristics "-N atmSoftPVpcRowStatus -O 1.3.6.1.4.1.353.5.5.1.1.3.1.14 -T INTEGER -A RW -X equal -F 0"

	)

	table	atmInterfaceSoftPvcAddressTable
	description "This table is used to configure ATM addresses at 
        an ATM interface on this node prior to setting up
        Soft PVPCs or Soft PVPCs at that interface."
	characteristics "-K ???"
	(
		notaccessible octet[20]	atmInterfaceSoftPvcAddress
		description 	"Specifies the address that can be used to establish a Soft
        PVCC or Soft PVPC to this interface."
		characteristics "-N atmInterfaceSoftPvcAddress -O 1.3.6.1.4.1.353.5.5.1.1.4.1.1 -T STRING -A NA -X equal -F 0"

		readwrite enum atmInterfaceSoftPvcAddressRowStatusE	atmInterfaceSoftPvcAddressRowStatus
		description 	"Used to create and delete an ATM address at this interface
        for setting up Soft PVCCs or Soft PVPCs."
		characteristics "-N atmInterfaceSoftPvcAddressRowStatus -O 1.3.6.1.4.1.353.5.5.1.1.4.1.2 -T INTEGER -A RW -X equal -F 0"

	)

	table	atmCurrentlyFailingSoftPVccTable
	description "A table indicating all Soft Permanent Virtual Channel
        Connections (Soft PVCCs) for which the atmSoftPVccRowStatus
        is 'active' and the atmSoftPVccOperStatus is other than
        'connected'."
	characteristics "-K ???"
	(
		readonly timeticks	atmCurrentlyFailingSoftPVccTimeStamp
		description 	"The time at which this Soft PVCC began to fail."
		characteristics "-N atmCurrentlyFailingSoftPVccTimeStamp -O 1.3.6.1.4.1.353.5.5.1.1.5.1.1 -T TIMETICKS -A RO -X equal -F 0"

	)

	table	atmCurrentlyFailingSoftPVpcTable
	description "A table indicating all Soft Permanent Virtual Path
        Connections (Soft PVPCs) for which the atmSoftPVpcRowStatus
        is 'active' and the atmSoftPVpcOperStatus is other than
        'connected'."
	characteristics "-K ???"
	(
		readonly timeticks	atmCurrentlyFailingSoftPVpcTimeStamp
		description 	"The time at which this Soft PVPC began to fail."
		characteristics "-N atmCurrentlyFailingSoftPVpcTimeStamp -O 1.3.6.1.4.1.353.5.5.1.1.6.1.1 -T TIMETICKS -A RO -X equal -F 0"

	)

	group	atmSoftPvcBaseGroup
	description "(null)"
	characteristics "-K NONE"
	(
		readwrite enum atmSoftPvcCallFailuresTrapEnableE	atmSoftPvcCallFailuresTrapEnable
		description 	"Allows the generation of traps in response to call
        failures. By default, this object is set to 'false'."
		characteristics "-N atmSoftPvcCallFailuresTrapEnable -O 1.3.6.1.4.1.353.5.5.1.1.1.1 -T INTEGER -A RW -X equal -F 0"

		readonly counter	atmSoftPvcCallFailures
		description 	"The number of times a series of call attempts has failed to
        establish a Soft PVCC or Soft PVPC. The number of call
        attempts in a series is determined by
        atmSoftPVccRetryThreshold or atmSoftPVpcRetryThreshold,
        respectively."
		characteristics "-N atmSoftPvcCallFailures -O 1.3.6.1.4.1.353.5.5.1.1.1.2 -T COUNTER -A RO -X equal -F 0"

		readonly int	atmSoftPvcCurrentlyFailingSoftPVccs
		description 	"The current number of Soft PVCCs for which there is
        an active row in the atmSoftPVccTable having an
        atmSoftPVccOperStatus with a value other than 'connected'."
		characteristics "-N atmSoftPvcCurrentlyFailingSoftPVccs -O 1.3.6.1.4.1.353.5.5.1.1.1.3 -T INTEGER -A RO -X equal -F 0"

		readonly int	atmSoftPvcCurrentlyFailingSoftPVpcs
		description 	"The current number of Soft PVPCs for which there is an
        active row in the atmSoftPVpcTable having an 
        atmSoftPVpcOperStatus with a value other than 'connected'."
		characteristics "-N atmSoftPvcCurrentlyFailingSoftPVpcs -O 1.3.6.1.4.1.353.5.5.1.1.1.4 -T INTEGER -A RO -X equal -F 0"

		readwrite int	atmSoftPvcNotificationInterval
		description 	"The minimum interval between the sending
        of atmSoftPvcCallFailuresTrap notifications."
		characteristics "-N atmSoftPvcNotificationInterval -O 1.3.6.1.4.1.353.5.5.1.1.1.5 -T INTEGER -A RW -X equal -F 0"

	)

agenterrors (
	1	"cannot dispatch request"
	2	"select(2) failed"
	3	"sendto(2) failed"
	4	"recvfrom(2) failed"
	5	"no response from system"
	6	"response too big"
	7	"missing attribute"
	8	"bad attribute type"
	9	"cannot get sysUpTime"
	10	"sysUpTime type bad"
	11	"unknown SNMP error"
	12	"bad variable value"
	13	"variable is read only"
	14	"general error"
	15	"cannot make request PDU"
	16	"cannot make request varbind list"
	17	"cannot parse response PDU"
	18	"request ID - response ID mismatch"
	19	"string contains non-displayable characters"
	20	"cannot open schema file"
	21	"cannot parse schema file"
	22	"cannot open host file"
	23	"cannot parse host file"
	24	"attribute unavailable for set operations"
             )
)


## End of Agent Definitions
