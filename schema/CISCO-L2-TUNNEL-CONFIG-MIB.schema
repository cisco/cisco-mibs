## THIS FILE WAS GENERATED BY MIB2SCHEMA
## For attributes containing printable ASCII
## characters, you may add a format string
## specification in the attribute characteristics
## line
## As described in RFC 1157, some agents cannot
## accept messages whose length exceeds 484 octets
## For those groups whose var-bind list has many
## entries, you may split them up into different
## var-bind lists
proxy CISCO-L2-TUNNEL-CONFIG-MIB
description "CISCO-L2-TUNNEL-CONFIG-MIB agent"
serial 2
rpcid 100122
(

## Enumerated Type Definitions

	enum	cltcNotificationEnableE (
	   1	"true"
	   2	"false"
	)

	enum	cltcTunnelSysDropNotifEnableE (
	   1	"true"
	   2	"false"
	)

	enum	cltcDot1qTunnelModeE (
	   1	"enabled"
	   2	"disabled"
	)

	enum	cltcTunnelThresholdProtocolIndexE (
	   1	"all"
	   2	"cdp"
	   3	"vtp"
	   4	"stp"
	   5	"eoam"
	   6	"lldp"
	)

	enum	cltcTunneledProtocolIndexE (
	   2	"cdp"
	   3	"vtp"
	   4	"stp"
	   5	"eoam"
	   6	"lldp"
	)

	enum	cltcDot1qAllTaggedEnabledE (
	   1	"true"
	   2	"false"
	)

	enum	cltcDot1qAllTaggedIfEnabledE (
	   1	"true"
	   2	"false"
	)


## Group and Table Declarations

	group	cltcGlobal
	description "(null)"
	characteristics "-K NONE"
	(
		readwrite int	cltcTunnelCos
		description 	"Specifies the user priority of the tunneled PDUs and applies
        to all ingress tunneling interfaces."
		characteristics "-N cltcTunnelCos -O 1.3.6.1.4.1.9.9.246.1.1.1 -T INTEGER -A RW -X equal -F 0"

		readwrite enum cltcNotificationEnableE	cltcNotificationEnable
		description 	"This object indicates whether the system will
        generate the cltcTunnelDropThresholdExceeded and
        cltcTunnelShutdownThresholdExceeded notifications."
		characteristics "-N cltcNotificationEnable -O 1.3.6.1.4.1.9.9.246.1.1.2 -T INTEGER -A RW -X equal -F 0"

		readwrite gauge	cltcTunnelSysDropThreshold
		description 	"This object specifies the drop threshold at the
        system level.  A value of zero indicates that
        system level rate limiting is disabled."
		characteristics "-N cltcTunnelSysDropThreshold -O 1.3.6.1.4.1.9.9.246.1.1.3 -T GAUGE -A RW -X equal -F 0"

		readwrite enum cltcTunnelSysDropNotifEnableE	cltcTunnelSysDropNotifEnable
		description 	"This object specifies whether the system will generate
        the cltcTunnelSysDropThresholdExceeded notification."
		characteristics "-N cltcTunnelSysDropNotifEnable -O 1.3.6.1.4.1.9.9.246.1.1.4 -T INTEGER -A RW -X equal -F 0"

	)

	table	cltcDot1qTunnelTable
	description "This table contains information about the dot1q tunnel
        interfaces. Only dot1q tunneling capable interfaces are shown.
        1Q-in-1Q will allow service providers to separate the traffic
        of various customers within their infrastructure while the
        customers appear to be on the same VLANs."
	characteristics "-K ???"
	(
		readwrite enum cltcDot1qTunnelModeE	cltcDot1qTunnelMode
		description 	"Indicates the dot1q tunnel mode of the interface.
        
        Setting the interface to dot1q tunnel 'disabled' mode causes
        the dot1q tunnel feature to be disabled on this interface.
        Setting the interface to dot1q tunnel 'enabled' mode causes
        the dot1q tunnel feature to be enabled on this interface."
		characteristics "-N cltcDot1qTunnelMode -O 1.3.6.1.4.1.9.9.246.1.2.1.1.1 -T INTEGER -A RW -X equal -F 0"

	)

	table	cltcTunneledProtocolTable
	description "This table contains information about the protocols being
        tunneled. Only tunneled protocol filtering capable interfaces
        are shown."
	characteristics "-K ???"
	(
		readwrite octet[2]	cltcTunneledProtocolType
		description 	"Indicates tunneled protocol of the interface.
        
        If a BIT is set, then the value of the corresponding
        protocol is tunneled. Specifically, if the 'cdp(0)'
        BIT is set, then the Cisco Discovery Protocol PDU is
        tunneled; if the 'vtp(1)' BIT is set, then the VLAN
        Trunking Protocol PDU is tunneled; if the 'stp(2)' BIT
        is set, then the Spanning Tree Protocol PDU is tunneled;
        if the 'eoam(3)' BIT is set, then the ethernet Operation,
        Administration and Maintenance PDU is tunneled; if the
        'lldp(4)' BIT is set, then the Link Layer Discovery
        Protocol is tunneled.
        
        If the bit for a given protocol is set for an interface, then
        the statistics for that interface and protocol will start to be
        monitored."
		characteristics "-N cltcTunneledProtocolType -O 1.3.6.1.4.1.9.9.246.1.3.1.1.1 -T STRING -A RW -X equal -F 0"

	)

	table	cltcTunnelThresholdTable
	description "This table contains information about the thresholds for
        protocol tunneling. Only tunneled protocol filtering capable
        interfaces are shown. The objects will be on a per interface,
        per protocol basis."
	characteristics "-K ONE"
	(
		notaccessible enum cltcTunnelThresholdProtocolIndexE	cltcTunnelThresholdProtocolIndex
		description 	"A tunneled protocol of an interface.
        
        - all(1) is supported for devices which allow the setting
          of thresholds on a per interface basis. This threshold is
          a combined threshold for the interface which includes all
          supported protocols.
        
        - cdp(2), vtp(3), stp(4), eoam(5) and lldp(6) are supported
          for devices which allow the setting of thresholds on a
          per interface, per protocol basis."
		characteristics "-N cltcTunnelThresholdProtocolIndex -O 1.3.6.1.4.1.9.9.246.1.4.1.1.1 -T INTEGER -A NA -X equal -F 0"

		readwrite gauge	cltcTunnelDropThreshold
		description 	"The drop threshold on an interface for a given protocol. After
        reaching this drop threshold, the interface will start dropping
        PDUs for the given protocol. This value cannot be greater than
        the value of cltcTunnelShutdownThreshold.  A value of 0
        indicates that no limit is set."
		characteristics "-N cltcTunnelDropThreshold -O 1.3.6.1.4.1.9.9.246.1.4.1.1.2 -T GAUGE -A RW -X equal -F 0"

		readwrite gauge	cltcTunnelShutdownThreshold
		description 	"The shutdown threshold on an interface for a given protocol.
        After reaching the shutdown threshold, the interface will
        shutdown for the given protocol.  This value cannot be less
        than the value of cltcTunnelDropThreshold. A value of 0
        indicates that no limit is set."
		characteristics "-N cltcTunnelShutdownThreshold -O 1.3.6.1.4.1.9.9.246.1.4.1.1.3 -T GAUGE -A RW -X equal -F 0"

	)

	table	cltcTunnelStatisticsTable
	description "This table contains protocol tunneling statistics on the
        interface."
	characteristics "-K ONE"
	(
		notaccessible enum cltcTunneledProtocolIndexE	cltcTunneledProtocolIndex
		description 	"A tunneled protocol of an interface."
		characteristics "-N cltcTunneledProtocolIndex -O 1.3.6.1.4.1.9.9.246.1.5.1.1.1 -T INTEGER -A NA -X equal -F 0"

		readonly counter	cltcTunnelEncapStats
		description 	"The tunneled PDU encapsulation statistics of an interface.
        These statistics cover the number of tunneled ingress
        PDUs."
		characteristics "-N cltcTunnelEncapStats -O 1.3.6.1.4.1.9.9.246.1.5.1.1.2 -T COUNTER -A RO -X equal -F 0"

		readonly counter	cltcTunnelDeEncapStats
		description 	"The tunneled PDU de-encapsulation statistics of an interface.
        These statistics cover the number of tunneled egress PDUs."
		characteristics "-N cltcTunnelDeEncapStats -O 1.3.6.1.4.1.9.9.246.1.5.1.1.3 -T COUNTER -A RO -X equal -F 0"

		readonly counter	cltcTunnelDropStats
		description 	"The number of PDUs dropped on an interface for a given
        protocol. The PDUs will be dropped when the
        cltcTunnelDropThreshold is reached."
		characteristics "-N cltcTunnelDropStats -O 1.3.6.1.4.1.9.9.246.1.5.1.1.4 -T COUNTER -A RO -X equal -F 0"

	)

	table	cltcTunnelDropStatTable
	description "This table contains protocol tunneling drop statistics."
	characteristics "-K ???"
	(
		readonly counter	cltcTunnelTotalDropStats
		description 	"The number of tunneled ingress PDUs that have been dropped."
		characteristics "-N cltcTunnelTotalDropStats -O 1.3.6.1.4.1.9.9.246.1.5.2.1.1 -T COUNTER -A RO -X equal -F 0"

	)

	group	cltcDot1qAllTagged
	description "(null)"
	characteristics "-K NONE"
	(
		readwrite enum cltcDot1qAllTaggedEnabledE	cltcDot1qAllTaggedEnabled
		description 	"This object controls if dot1q-all-tagged feature is enabled in
        the managed system."
		characteristics "-N cltcDot1qAllTaggedEnabled -O 1.3.6.1.4.1.9.9.246.1.6.1 -T INTEGER -A RW -X equal -F 0"

	)

	table	cltcDot1qAllTaggedIfTable
	description "This table contains information about the dot1q-all-tagged
        feature's configuration of capable interfaces in the system."
	characteristics "-K ???"
	(
		readwrite enum cltcDot1qAllTaggedIfEnabledE	cltcDot1qAllTaggedIfEnabled
		description 	"This object specifies whether dot1q-all-tagged feature has been
        enabled on a specific interface."
		characteristics "-N cltcDot1qAllTaggedIfEnabled -O 1.3.6.1.4.1.9.9.246.1.6.2.1.1 -T INTEGER -A RW -X equal -F 0"

	)

agenterrors (
	1	"cannot dispatch request"
	2	"select(2) failed"
	3	"sendto(2) failed"
	4	"recvfrom(2) failed"
	5	"no response from system"
	6	"response too big"
	7	"missing attribute"
	8	"bad attribute type"
	9	"cannot get sysUpTime"
	10	"sysUpTime type bad"
	11	"unknown SNMP error"
	12	"bad variable value"
	13	"variable is read only"
	14	"general error"
	15	"cannot make request PDU"
	16	"cannot make request varbind list"
	17	"cannot parse response PDU"
	18	"request ID - response ID mismatch"
	19	"string contains non-displayable characters"
	20	"cannot open schema file"
	21	"cannot parse schema file"
	22	"cannot open host file"
	23	"cannot parse host file"
	24	"attribute unavailable for set operations"
             )
)


## End of Agent Definitions
