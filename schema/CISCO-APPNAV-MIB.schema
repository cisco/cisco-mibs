## THIS FILE WAS GENERATED BY MIB2SCHEMA
## For attributes containing printable ASCII
## characters, you may add a format string
## specification in the attribute characteristics
## line
## As described in RFC 1157, some agents cannot
## accept messages whose length exceeds 484 octets
## For those groups whose var-bind list has many
## entries, you may split them up into different
## var-bind lists
proxy CISCO-APPNAV-MIB
description "CISCO-APPNAV-MIB agent"
serial 2
rpcid 100122
(

## Enumerated Type Definitions

	enum	cAppNavServContextCurrOpStateE (
	   1	"initializing"
	   2	"converging"
	   3	"internalError"
	   4	"degraded"
	   5	"operational"
	   6	"adminDisabled"
	   7	"initializingJoining"
	   8	"convergingJoining"
	   9	"operationalJoining"
	   10	"degradedJoining"
	)

	enum	cAppNavServContextLastOpStateE (
	   1	"initializing"
	   2	"converging"
	   3	"internalError"
	   4	"degraded"
	   5	"operational"
	   6	"adminDisabled"
	   7	"initializingJoining"
	   8	"convergingJoining"
	   9	"operationalJoining"
	   10	"degradedJoining"
	)

	enum	cAppNavServContextIRStateE (
	   1	"ready"
	   2	"notReady"
	   3	"na"
	)

	enum	cAppNavServContextJoinStateE (
	   1	"unknown"
	   2	"notConfigured"
	   3	"started"
	   4	"aborted"
	   5	"completed"
	)

	enum	cAppNavACIpAddrTypeE (
	   1	"ipv4"
	   2	"ipv6"
	   3	"ipv4z"
	   4	"ipv6z"
	   16	"dns"
	)

	enum	cAppNavACCurrentCMStateE (
	   1	"dead"
	   2	"alive"
	   3	"excluded"
	   4	"partial"
	   5	"na"
	   6	"zombie"
	   7	"inactive"
	)

	enum	cAppNavSNIpAddrTypeE (
	   1	"ipv4"
	   2	"ipv6"
	   3	"ipv4z"
	   4	"ipv6z"
	   16	"dns"
	)

	enum	cAppNavSNCurrentCMStateE (
	   1	"dead"
	   2	"alive"
	   3	"excluded"
	   4	"partial"
	   5	"na"
	   6	"zombie"
	   7	"inactive"
	)


## Group and Table Declarations

	table	cAppNavServContextTable
	description "This table lists the objects, which gives information related
        to the service contexts configured on the AppNav controller."
	characteristics "-K ONE"
	(
		notaccessible gauge	cAppNavServContextIndex
		description 	"This object indicates an index of the
        cAppNavServiceContextTable. It is an arbitrary unsigned integer
        value that uniquely identifies an entry in
        cAppNavServiceContextTable. The value for each entry must remain
        constant at least from one re-initialization of the entity's
        network management system to the next re-initialization."
		characteristics "-N cAppNavServContextIndex -O 1.3.6.1.4.1.9.9.791.1.1.1.1 -T GAUGE -A NA -X equal -F 0"

		readonly octet[128]	cAppNavServContextName
		description 	"This object indicates the name of the service context."
		characteristics "-N cAppNavServContextName -O 1.3.6.1.4.1.9.9.791.1.1.1.2 -T STRING -A RO -X equal -F 0"

		readonly enum cAppNavServContextCurrOpStateE	cAppNavServContextCurrOpState
		description 	"This object indicates the current operational state of the
        service context."
		characteristics "-N cAppNavServContextCurrOpState -O 1.3.6.1.4.1.9.9.791.1.1.1.3 -T INTEGER -A RO -X equal -F 0"

		readonly enum cAppNavServContextLastOpStateE	cAppNavServContextLastOpState
		description 	"This object indicates the last operational state of the service
        context."
		characteristics "-N cAppNavServContextLastOpState -O 1.3.6.1.4.1.9.9.791.1.1.1.4 -T INTEGER -A RO -X equal -F 0"

		readonly enum cAppNavServContextIRStateE	cAppNavServContextIRState
		description 	"This object indicates the Interception Readiness(IR) state of
        the service context."
		characteristics "-N cAppNavServContextIRState -O 1.3.6.1.4.1.9.9.791.1.1.1.5 -T INTEGER -A RO -X equal -F 0"

		readonly enum cAppNavServContextJoinStateE	cAppNavServContextJoinState
		description 	"This object indicates the join state of the service context."
		characteristics "-N cAppNavServContextJoinState -O 1.3.6.1.4.1.9.9.791.1.1.1.6 -T INTEGER -A RO -X equal -F 0"

	)

	table	cAppNavACGTable
	description "This table lists the objects, which gives information related
        to the AppNav controller group configured on the AppNav
        controller."
	characteristics "-K ONE"
	(
		notaccessible gauge	cAppNavACGIndex
		description 	"This object indicates an index of the cAppNavACGTable. It is
        an arbitrary unsigned integer value that uniquely identifies an
        entry in cAppNavACGTable. The value for each entry must remain
        constant at least from one re-initialization of the entity's
        network management system to the next re-initialization."
		characteristics "-N cAppNavACGIndex -O 1.3.6.1.4.1.9.9.791.2.1.1.1 -T GAUGE -A NA -X equal -F 0"

		readonly octet[128]	cAppNavACGName
		description 	"This object indicates the name of the AppNav controller group."
		characteristics "-N cAppNavACGName -O 1.3.6.1.4.1.9.9.791.2.1.1.2 -T STRING -A RO -X equal -F 0"

		readonly octet[96]	cAppNavACGServContextName
		description 	"This object indicates the service context to which AppNav
        controller belongs."
		characteristics "-N cAppNavACGServContextName -O 1.3.6.1.4.1.9.9.791.2.1.1.3 -T STRING -A RO -X equal -F 0"

	)

	table	cAppNavSNGTable
	description "This table lists the objects, which gives information related
        to the service node group configured on the AppNav controller."
	characteristics "-K ONE"
	(
		notaccessible gauge	cAppNavSNGIndex
		description 	"This object specifies an index of the cAppNavSNGTable. It is an
        arbitrary unsigned integer value that uniquely identifies an
        entry in  cAppNavSNGTable. The value for each entry  must remain
        constant at least from one re-initialization of the entity's
        network management system to the next re-initialization."
		characteristics "-N cAppNavSNGIndex -O 1.3.6.1.4.1.9.9.791.3.1.1.1 -T GAUGE -A NA -X equal -F 0"

		readonly octet[128]	cAppNavSNGName
		description 	"This object indicates the name of service node group."
		characteristics "-N cAppNavSNGName -O 1.3.6.1.4.1.9.9.791.3.1.1.2 -T STRING -A RO -X equal -F 0"

		readonly octet[128]	cAppNavSNGServContextName
		description 	"This object indicates the name of the service context to which
        given service node group belongs."
		characteristics "-N cAppNavSNGServContextName -O 1.3.6.1.4.1.9.9.791.3.1.1.3 -T STRING -A RO -X equal -F 0"

	)

	table	cAppNavACTable
	description "This table lists the objects, which give information related to
        the AppNav controllers configured on the AppNav controller to
        assign them to the service context."
	characteristics "-K ONE"
	(
		notaccessible gauge	cAppNavACIndex
		description 	"This object indicates an index of the cAppNavACTable. It is an
        arbitrary unsigned integer value that uniquely identifies an
        entry in  cAppNavACTable. The value for each entry must remain
        constant at least from one re-initialization of the entity's
        network management system to the next re-initialization."
		characteristics "-N cAppNavACIndex -O 1.3.6.1.4.1.9.9.791.4.1.1.1 -T GAUGE -A NA -X equal -F 0"

		readonly enum cAppNavACIpAddrTypeE	cAppNavACIpAddrType
		description 	"This object indicates the address type of the cAppNavACIpAddr.
        The cAppNavACEntries are only valid for address type of IPv4 and
        IPv6."
		characteristics "-N cAppNavACIpAddrType -O 1.3.6.1.4.1.9.9.791.4.1.1.2 -T INTEGER -A RO -X equal -F 0"

		readonly octet[255]	cAppNavACIpAddr
		description 	"This object indicates the IP address of the AppNav controller."
		characteristics "-N cAppNavACIpAddr -O 1.3.6.1.4.1.9.9.791.4.1.1.3 -T STRING -A RO -X equal -F 0"

		readonly octet[128]	cAppNavACServContextName
		description 	"This object indicates the name of the service context to which
        given AppNav controller belongs."
		characteristics "-N cAppNavACServContextName -O 1.3.6.1.4.1.9.9.791.4.1.1.4 -T STRING -A RO -X equal -F 0"

		readonly octet[128]	cAppNavACACGName
		description 	"This object indicates the name of the AppNav controller group
        to which given AppNav controller belongs."
		characteristics "-N cAppNavACACGName -O 1.3.6.1.4.1.9.9.791.4.1.1.5 -T STRING -A RO -X equal -F 0"

		readonly enum cAppNavACCurrentCMStateE	cAppNavACCurrentCMState
		description 	"This object indicates the current cluster membership state of
        the given AppNav controller."
		characteristics "-N cAppNavACCurrentCMState -O 1.3.6.1.4.1.9.9.791.4.1.1.6 -T INTEGER -A RO -X equal -F 0"

	)

	table	cAppNavSNTable
	description "This table lists the objects, which gives information related
        to the service nodes configured on the AppNav controller to
        assign them to the service context."
	characteristics "-K ONE"
	(
		notaccessible gauge	cAppNavSNIndex
		description 	"This object indicates an index of the cAppNavSNTable. It is an
        arbitrary unsigned integer value that uniquely identifies an
        entry in  cAppNavSNTable. The value for each entry must remain 
        constant at least from one re-initialization of the entity's 
        network management system to the next re-initialization."
		characteristics "-N cAppNavSNIndex -O 1.3.6.1.4.1.9.9.791.5.1.1.1 -T GAUGE -A NA -X equal -F 0"

		readonly enum cAppNavSNIpAddrTypeE	cAppNavSNIpAddrType
		description 	"This object indicates the address type of cacSnIpAddr.
        cacSNEntries are only valid for address type of IPv4 and 
        IPv6."
		characteristics "-N cAppNavSNIpAddrType -O 1.3.6.1.4.1.9.9.791.5.1.1.2 -T INTEGER -A RO -X equal -F 0"

		readonly octet[255]	cAppNavSNIpAddr
		description 	"This object indicates the IP address of the given service
        node."
		characteristics "-N cAppNavSNIpAddr -O 1.3.6.1.4.1.9.9.791.5.1.1.3 -T STRING -A RO -X equal -F 0"

		readonly octet[128]	cAppNavSNServContextName
		description 	"This object indicates the name of the service context to which
        given service node belongs."
		characteristics "-N cAppNavSNServContextName -O 1.3.6.1.4.1.9.9.791.5.1.1.4 -T STRING -A RO -X equal -F 0"

		readonly octet[128]	cAppNavSNSNGName
		description 	"This object indicates the name of the service node group to
        which given service node belongs."
		characteristics "-N cAppNavSNSNGName -O 1.3.6.1.4.1.9.9.791.5.1.1.5 -T STRING -A RO -X equal -F 0"

		readonly enum cAppNavSNCurrentCMStateE	cAppNavSNCurrentCMState
		description 	"This object indicats the current cluster membership state of
        the given service node."
		characteristics "-N cAppNavSNCurrentCMState -O 1.3.6.1.4.1.9.9.791.5.1.1.6 -T INTEGER -A RO -X equal -F 0"

	)

agenterrors (
	1	"cannot dispatch request"
	2	"select(2) failed"
	3	"sendto(2) failed"
	4	"recvfrom(2) failed"
	5	"no response from system"
	6	"response too big"
	7	"missing attribute"
	8	"bad attribute type"
	9	"cannot get sysUpTime"
	10	"sysUpTime type bad"
	11	"unknown SNMP error"
	12	"bad variable value"
	13	"variable is read only"
	14	"general error"
	15	"cannot make request PDU"
	16	"cannot make request varbind list"
	17	"cannot parse response PDU"
	18	"request ID - response ID mismatch"
	19	"string contains non-displayable characters"
	20	"cannot open schema file"
	21	"cannot parse schema file"
	22	"cannot open host file"
	23	"cannot parse host file"
	24	"attribute unavailable for set operations"
             )
)


## End of Agent Definitions
