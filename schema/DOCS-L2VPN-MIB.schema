## THIS FILE WAS GENERATED BY MIB2SCHEMA
## For attributes containing printable ASCII
## characters, you may add a format string
## specification in the attribute characteristics
## line
## As described in RFC 1157, some agents cannot
## accept messages whose length exceeds 484 octets
## For those groups whose var-bind list has many
## entries, you may split them up into different
## var-bind lists
proxy DOCS-L2VPN-MIB
description "DOCS-L2VPN-MIB agent"
serial 2
rpcid 100122
(

## Enumerated Type Definitions

	enum	docsL2vpnCmCompliantCapabilityE (
	   1	"true"
	   2	"false"
	)

	enum	docsL2vpnCmDutFilteringCapabilityE (
	   1	"true"
	   2	"false"
	)

	enum	docsL2vpnCmNsiEncapSubtypeE (
	   1	"other"
	   2	"ieee8021q"
	   3	"ieee8021ad"
	   4	"mpls"
	   5	"l2tpv3"
	)


## Group and Table Declarations

	table	docsL2vpnIdToIndexTable
	description "Table indexed by the octet string DocsL2vpnIdentifier that 
        provides the local agent's internally assigned docsL2vpnIdx 
        value for that DocsL2vpnIdentifier value. The mapping of  
        DocsL2vpnIdentifier to docsL2vpnIdx is 1-1. The agent  
        must instantiate a row in both docsL2vpnIndexToIdTable and 
        docsL2vpnIdToIndexTable for each known L2VPN Identifier."
	characteristics "-K ???"
	(
		notaccessible octet[16]	docsL2vpnId
		description 	"An externally configured octet string that identifies an 
        L2VPN."
		characteristics "-N docsL2vpnId -O 1.3.6.1.4.1.4491.2.1.8.1.1.1.1 -T STRING -A NA -X equal -F 0"

		readonly gauge	docsL2vpnIdToIndexIdx
		description 	"An internally assigned index value for a known L2VPN."
		characteristics "-N docsL2vpnIdToIndexIdx -O 1.3.6.1.4.1.4491.2.1.8.1.1.1.2 -T GAUGE -A RO -X equal -F 0"

	)

	table	docsL2vpnIndexToIdTable
	description "Table indexed by agent's local docsL2vpnIdx that provides 
        the global L2VPN Identifier. The mapping of docsL2vpnIdx to 
        DocsL2vpnIdentifier is 1-1. The agent must instantiate a  
        row in both docsL2vpnIndexToIdTable and  
        docsL2vpnIdToIndexTable for each known L2VPN."
	characteristics "-K ONE"
	(
		notaccessible gauge	docsL2vpnIdx
		description 	"An internally assigned index value for a known L2VPN."
		characteristics "-N docsL2vpnIdx -O 1.3.6.1.4.1.4491.2.1.8.1.2.1.1 -T GAUGE -A NA -X equal -F 0"

		readonly octet[16]	docsL2vpnIndexToIdId
		description 	"An administered octet string that externally identifies an 
        L2VPN."
		characteristics "-N docsL2vpnIndexToIdId -O 1.3.6.1.4.1.4491.2.1.8.1.2.1.2 -T STRING -A RO -X equal -F 0"

	)

	table	docsL2vpnCmTable
	description "This table describes L2VPN per-CM information that 
        is in common with all L2VPNs for the CM, regardless 
        of forwarding mode."
	characteristics "-K ???"
	(
		readonly enum docsL2vpnCmCompliantCapabilityE	docsL2vpnCmCompliantCapability
		description 	"This object reports whether an L2VPN forwarding CM is 
        compliant with the DOCSIS L2VPN specification, as reported 
        in the L2VPN Capability encoding in the CM's registration 
        request message. 
        
        If the capability encoding was omitted, this object must 
        report the value false(2)."
		characteristics "-N docsL2vpnCmCompliantCapability -O 1.3.6.1.4.1.4491.2.1.8.1.3.1.1 -T INTEGER -A RO -X equal -F 0"

		readonly enum docsL2vpnCmDutFilteringCapabilityE	docsL2vpnCmDutFilteringCapability
		description 	"This object reports whether an L2VPN forwarding CM is 
        capable of Downstream Unencrypted Traffic (DUT) Filtering, 
        as reported in the CM's registration request message. 
        
        If the capability encoding was omitted, this object must 
        report the value false(2)."
		characteristics "-N docsL2vpnCmDutFilteringCapability -O 1.3.6.1.4.1.4491.2.1.8.1.3.1.2 -T INTEGER -A RO -X equal -F 0"

		readonly octet[2]	docsL2vpnCmDutCMIM
		description 	"This object reports the value configured in a per-CM  
        L2VPN Encoding for Downstream Unencrypted Traffic (DUT) 
        Cable Modem Interface Mask (CMIM).  
        
        The DUT CMIM is a bit string with a '1' for each bit  
        position K for an internal or external CM interface with  
        ifIndex K to which the CM permits DUT to be forwarded. A CM 
        capable of DUT filtering MUST discard DUT to interfaces 
        with a '0' in the DUT CMIM.  
        
        If a CM's top-level registration request L2VPN Encoding 
        contained no DUT CMIM subtype, this object is reported 
        with its default value of a '1' in bit position 0  
        (corresponding to the eCM's own 'self' host) and a '1' in  
        each bit position K for which an eSAFE interface exists at 
        ifIndex K. In other words, the default DUT CMIM includes  
        the eCM and all eSAFE interfaces. 
        
        This value is reported independently of whether the CM is 
        actually capable of performing DUT filtering."
		characteristics "-N docsL2vpnCmDutCMIM -O 1.3.6.1.4.1.4491.2.1.8.1.3.1.3 -T STRING -A RO -X equal -F 0"

		readonly octet[2]	docsL2vpnCmDhcpSnooping
		description 	"This object reports the value of the Enable DHCP Snooping 
        subtype of a top-level L2VPN Encoding.  
        
        It has the syntax of a CM Interface List bitmask and 
        represents a set of CM MAC bridge interfaces  
        corresponding to eSAFE hosts for which the CMTS is enabled  
        to snoop DHCP traffic in order to learn the eSAFE host MAC 
        address on that interface.   
        
        Only bits corresponding to eSAFE host MAC addresses may be 
        validly set in this object, including cpe(1) for ePS 
        and the eSAFE interfaces in bits positions 16 through 31."
		characteristics "-N docsL2vpnCmDhcpSnooping -O 1.3.6.1.4.1.4491.2.1.8.1.3.1.4 -T STRING -A RO -X equal -F 0"

	)

	table	docsL2vpnVpnCmTable
	description "This table describes the operation of L2VPN forwarding 
        on each CM."
	characteristics "-K ???"
	(
		readonly octet[2]	docsL2vpnVpnCmCMIM
		description 	"A Cable Modem Interface Mask represents a set of  
        MAC bridge interfaces within the CM. This object 
        represents the CMIM within a forwarding per-SF L2VPN  
        encoding, which specifies a set of CM MAC bridge  
        interfaces to which L2VPN forwarding is restricted. 
        
        If the CMIM Subtype is omitted from a forwarding 
        per-SF encoding, its default value includes only 
        cpePrimary(1) and cableMac(2), which can be encoded 
        with a single octet with the value 0x60."
		characteristics "-N docsL2vpnVpnCmCMIM -O 1.3.6.1.4.1.4491.2.1.8.1.4.1.1 -T STRING -A RO -X equal -F 0"

		readonly int	docsL2vpnVpnCmIndividualSAId
		description 	"The BPI+ Security Association ID in which traffic intended 
        for point-to-point forwarding through an individual CM is  
        forwarded.  
        
        If the CMTS does not allocate an individual SAID for 
        multipoint forwarding (as is recommended),it MUST  
        report this object as zero."
		characteristics "-N docsL2vpnVpnCmIndividualSAId -O 1.3.6.1.4.1.4491.2.1.8.1.4.1.2 -T INTEGER -A RO -X equal -F 0"

		readonly octet[128]	docsL2vpnVpnCmVendorSpecific
		description 	"This object encodes the concatenation of all Vendor  
        Specific Subtype encodings that appeared in any  
        registration per-CM L2VPN Encoding associated with this  
        entry."
		characteristics "-N docsL2vpnVpnCmVendorSpecific -O 1.3.6.1.4.1.4491.2.1.8.1.4.1.3 -T STRING -A RO -X equal -F 0"

	)

	table	docsL2vpnVpnCmStatsTable
	description "This table contains statistics for forwarding of  
        packets to and from a CM on each VPN."
	characteristics "-K ???"
	(
		readonly counter	docsL2vpnVpnCmStatsUpstreamPkts
		description 	"The number of L2vpn-forwarded packets received 
        from this instance's Cable Modem on 
        this instance's L2VPN."
		characteristics "-N docsL2vpnVpnCmStatsUpstreamPkts -O 1.3.6.1.4.1.4491.2.1.8.1.5.1.1 -T COUNTER -A RO -X equal -F 0"

		readonly counter	docsL2vpnVpnCmStatsUpstreamBytes
		description 	"The number of L2vpn-forwarded bytes received 
        from this instance's Cable Modem on 
        this instance's L2VPN."
		characteristics "-N docsL2vpnVpnCmStatsUpstreamBytes -O 1.3.6.1.4.1.4491.2.1.8.1.5.1.2 -T COUNTER -A RO -X equal -F 0"

		readonly counter	docsL2vpnVpnCmStatsUpstreamDiscards
		description 	"The number of L2-forwarded packets  
        discarded from this instance's  
        Cable Modem on this instance's VPN."
		characteristics "-N docsL2vpnVpnCmStatsUpstreamDiscards -O 1.3.6.1.4.1.4491.2.1.8.1.5.1.3 -T COUNTER -A RO -X equal -F 0"

		readonly counter	docsL2vpnVpnCmStatsDownstreamPkts
		description 	"The number of L2-forwarded packets 
        transmitted to this instance's 
        Cable Modem on this instance's VPN."
		characteristics "-N docsL2vpnVpnCmStatsDownstreamPkts -O 1.3.6.1.4.1.4491.2.1.8.1.5.1.4 -T COUNTER -A RO -X equal -F 0"

		readonly counter	docsL2vpnVpnCmStatsDownstreamBytes
		description 	"The number of L2-forwarded bytes 
        transmitted to this instance's 
        Cable Modem on this instance's VPN."
		characteristics "-N docsL2vpnVpnCmStatsDownstreamBytes -O 1.3.6.1.4.1.4491.2.1.8.1.5.1.5 -T COUNTER -A RO -X equal -F 0"

		readonly counter	docsL2vpnVpnCmStatsDownstreamDiscards
		description 	"The number of L2-forwarded packets that were discarded  
        before they could be transmitted to this instance's  
        Cable Modem on this instance's VPN."
		characteristics "-N docsL2vpnVpnCmStatsDownstreamDiscards -O 1.3.6.1.4.1.4491.2.1.8.1.5.1.6 -T COUNTER -A RO -X equal -F 0"

	)

	table	docsL2vpnPortStatusTable
	description "This table displays summary information for the 
        run-time state of each VPN that is currently operating  
        on each bridge port."
	characteristics "-K ???"
	(
		readonly int	docsL2vpnPortStatusGroupSAId
		description 	"The Group SAID associated with this VPN on a  
        particular CMTS MAC domain. This SAID is used to encrypt 
        all downstream flooded bridge traffic sent to CMs on  
        this VPN and CMTS MAC domain bridge port.  
        
        A value of '0' means there is no associated Group SAID for 
        this VPN and bridge port, e.g., if the L2VPN uses 
        point-to-point individual SAIDs only for forwarding. 
        
        A bridge port that is not a CMTS MAC 
        domain will report a value of '0'."
		characteristics "-N docsL2vpnPortStatusGroupSAId -O 1.3.6.1.4.1.4491.2.1.8.1.6.1.1 -T INTEGER -A RO -X equal -F 0"

	)

	table	docsL2vpnSfStatusTable
	description "This table displays SF-specific L2VPN forwarding status  
        for each upstream service flow configured with a per-SF  
         L2VPN Encoding. 
        
         Objects which were signaled in a per-SF L2VPN Encoding but 
         apply for the entire CM are shown in the  
         docsL2vpnVpnCmTable."
	characteristics "-K ???"
	(
		readonly octet[128]	docsL2vpnSfStatusL2vpnId
		description 	"This object represents the value of the L2VPN Identifier 
        subtype of a per-SF L2VPN Encoding."
		characteristics "-N docsL2vpnSfStatusL2vpnId -O 1.3.6.1.4.1.4491.2.1.8.1.7.1.1 -T STRING -A RO -X equal -F 0"

		readonly gauge	docsL2vpnSfStatusIngressUserPriority
		description 	"This object provides the configured Ingress User Priority 
        subtype of a per-SF L2VPN Encoding for this CM. If the  
        subtype was omitted, this object's value is zero."
		characteristics "-N docsL2vpnSfStatusIngressUserPriority -O 1.3.6.1.4.1.4491.2.1.8.1.7.1.2 -T GAUGE -A RO -X equal -F 0"

		readonly octet[128]	docsL2vpnSfStatusVendorSpecific
		description 	"This object provides the set of configured Vendor Specific 
        subtypes within a per-SF L2VPN Encoding for a CM. If no  
        Vendor Specific subtype was specified, this object is a  
        zero length octet string. If one or more Vendor Specific  
        subtype parameters was specified, this object represents  
        the concatenation of all such subtypes."
		characteristics "-N docsL2vpnSfStatusVendorSpecific -O 1.3.6.1.4.1.4491.2.1.8.1.7.1.3 -T STRING -A RO -X equal -F 0"

	)

	table	docsL2vpnPktClassTable
	description "This table provides the L2VPN-specific objects for 
        packet classifiers that apply to only L2VPN traffic.  
        The indices of this table are a subset of the 
        indices of classifiers in docsQosPktClassTable."
	characteristics "-K ???"
	(
		readonly octet[16]	docsL2vpnPktClassL2vpnId
		description 	"The locally assigned L2VPN index corresponding to the VPN 
        Identifier subtype of a Downstream Classifier L2VPN  
        Encoding."
		characteristics "-N docsL2vpnPktClassL2vpnId -O 1.3.6.1.4.1.4491.2.1.8.1.8.1.1 -T STRING -A RO -X equal -F 0"

		readonly gauge	docsL2vpnPktClassUserPriRangeLow
		description 	"The lower priority of the user Priority Range subtype 
        of a Downstream Classifier L2VPN Encoding. If the subtype 
        was omitted, this object has value 0."
		characteristics "-N docsL2vpnPktClassUserPriRangeLow -O 1.3.6.1.4.1.4491.2.1.8.1.8.1.2 -T GAUGE -A RO -X equal -F 0"

		readonly gauge	docsL2vpnPktClassUserPriRangeHigh
		description 	"The higher priority of the user Priority Range subtype 
        of a Downstream Classifier L2VPN Encoding. If the subtype 
        was omitted, this object has value 7."
		characteristics "-N docsL2vpnPktClassUserPriRangeHigh -O 1.3.6.1.4.1.4491.2.1.8.1.8.1.3 -T GAUGE -A RO -X equal -F 0"

		readonly octet[2]	docsL2vpnPktClassCMIM
		description 	"The Cable Modem Interface Mask (CMIM) signaled in a  
        Packet Classifier Encoding.  In a Downstream Packet  
        Classifier Encoding, a specified CMIM value restricts the  
        classifier to match packets with a Destination MAC address 
        corresponding to the interfaces indicated in the CMIM mask. 
        The eCM self and any eSAFE interface bits correspond to 
        the individual eCM and eSAFE host MAC addresses. 
        
        In an Upstream Packet Classifier encoding, a specified CMIM 
        value restricts the classifier to match packets with an  
        ingress bridge port interface matching the bits in the  
        CMIM value. 
        
        If the CMIM subtype was omitted, this object should be  
        reported as a zero length octet string."
		characteristics "-N docsL2vpnPktClassCMIM -O 1.3.6.1.4.1.4491.2.1.8.1.8.1.4 -T STRING -A RO -X equal -F 0"

		readonly octet[128]	docsL2vpnPktClassVendorSpecific
		description 	"This object provides the set of configured  
        Vendor Specific subtypes within a Packet Classifier  
        Encoding for a CM. If no Vendor Specific subtype was  
        specified, this object is a zero length octet string.  
        If one or more Vendor Specific subtype parameters was  
        specified, this object represents the concatenation of all 
        such subtypes."
		characteristics "-N docsL2vpnPktClassVendorSpecific -O 1.3.6.1.4.1.4491.2.1.8.1.8.1.5 -T STRING -A RO -X equal -F 0"

	)

	table	docsL2vpnCmNsiTable
	description "This table describes the NSI configuration for a single 
        CM when operating in point-to-point forwarding mode for an 
        L2VPN."
	characteristics "-K ???"
	(
		readonly enum docsL2vpnCmNsiEncapSubtypeE	docsL2vpnCmNsiEncapSubtype
		description 	"The General Encapsulation Information (GEI) subtype of the 
        Network System Interface (NSI) encapsulation configured 
        for the CM."
		characteristics "-N docsL2vpnCmNsiEncapSubtype -O 1.3.6.1.4.1.4491.2.1.8.1.9.1.1 -T INTEGER -A RO -X equal -F 0"

		readonly octet[128]	docsL2vpnCmNsiEncapValue
		description 	"The encapsulation value for L2VPN forwarded packets on NSI 
        ports."
		characteristics "-N docsL2vpnCmNsiEncapValue -O 1.3.6.1.4.1.4491.2.1.8.1.9.1.2 -T STRING -A RO -X equal -F 0"

		readonly octet[128]	docsL2vpnCmNsiAGI
		description 	"This object is the configuration of any Attachment Group  
        Identifier subtype in the per-SF L2VPN Encoding  
        represented by this row. If the subtype was omitted, this  
        object's value is a zero length string."
		characteristics "-N docsL2vpnCmNsiAGI -O 1.3.6.1.4.1.4491.2.1.8.1.9.1.3 -T STRING -A RO -X equal -F 0"

		readonly octet[128]	docsL2vpnCmNsiSAII
		description 	"This object is the configuration of any Source  
        Attachment Individual ID subtype in the L2VPN Encoding  
        represented by this row. If the subtype was omitted, this 
        object's value is a zero length string."
		characteristics "-N docsL2vpnCmNsiSAII -O 1.3.6.1.4.1.4491.2.1.8.1.9.1.4 -T STRING -A RO -X equal -F 0"

		readonly octet[128]	docsL2vpnCmNsiTAII
		description 	"This object is the configuration of any Target 
        Attachment Individual ID subtype in the L2VPN Encoding 
        represented by this row. If the subtype was omitted, this 
        object's value is a zero length string."
		characteristics "-N docsL2vpnCmNsiTAII -O 1.3.6.1.4.1.4491.2.1.8.1.9.1.5 -T STRING -A RO -X equal -F 0"

	)

	table	docsL2vpnCmVpnCpeTable
	description "This table is a list of CPEs, indexed by the VPNs on a  
        Cable Modem."
	characteristics "-K ???"
	(
		readonly octet[6]	docsL2vpnCmVpnCpeMacAddress
		description 	"The Customer Premise Equipment (CPE) Mac Address 
        that is attached to this instances Cable Modem 
        and bridging on this instance's VPN Id."
		characteristics "-N docsL2vpnCmVpnCpeMacAddress -O 1.3.6.1.4.1.4491.2.1.8.1.10.1.1 -T STRING -A RO -X equal -F 0"

	)

	table	docsL2vpnVpnCmCpeTable
	description "This table contains a list of bridging CPEs, indexed by 
        L2VPN Index and the corresponding CMs on that VPN."
	characteristics "-K ???"
	(
		readonly octet[6]	docsL2vpnVpnCmCpeMacAddress
		description 	"The Customer Premise Equipment (CPE) Mac Address 
        that is attached to this instances Cable Modem 
        and bridging on this instance's L2vpn Index."
		characteristics "-N docsL2vpnVpnCmCpeMacAddress -O 1.3.6.1.4.1.4491.2.1.8.1.11.1.1 -T STRING -A RO -X equal -F 0"

	)

	table	docsL2vpnDot1qTpFdbExtTable
	description "This table contains packet counters for  
        Unicast MAC Addresses within a VPN."
	characteristics "-K ???"
	(
		readonly counter	docsL2vpnDot1qTpFdbExtTransmitPkts
		description 	"The number of packets where the Destination  
        MAC Address matched this instance  
        dot1qTpFdbAddress and packet was bridged on 
        a VPN, where the VPN ID matched this  
        instance's dot1qFdbId."
		characteristics "-N docsL2vpnDot1qTpFdbExtTransmitPkts -O 1.3.6.1.4.1.4491.2.1.8.1.12.1.1 -T COUNTER -A RO -X equal -F 0"

		readonly counter	docsL2vpnDot1qTpFdbExtReceivePkts
		description 	"The number of packets where the Source MAC  
        Address matched this instance dot1qTpFdbAddress 
        and the packet was bridged on a VPN, 
        where the docsL2vpnIdx matched this instance's  
        dot1qFdbId."
		characteristics "-N docsL2vpnDot1qTpFdbExtReceivePkts -O 1.3.6.1.4.1.4491.2.1.8.1.12.1.2 -T COUNTER -A RO -X equal -F 0"

	)

	table	docsL2vpnDot1qTpGroupExtTable
	description "This table contains packet counters for  
        Multicast MAC Addresses within a VPN."
	characteristics "-K ???"
	(
		readonly counter	docsL2vpnDot1qTpGroupExtTransmitPkts
		description 	"The number of packets where the Destination  
        MAC Address matched this instance  
        dot1qTpGroupAddress and packet was bridged on 
        a VPN, where the docsL2vpnIdx matched this  
        instance's dot1qVlanIndex."
		characteristics "-N docsL2vpnDot1qTpGroupExtTransmitPkts -O 1.3.6.1.4.1.4491.2.1.8.1.13.1.1 -T COUNTER -A RO -X equal -F 0"

		readonly counter	docsL2vpnDot1qTpGroupExtReceivePkts
		description 	"The number of packets where the Source MAC  
        Address matched this instance dot1qTpGroupAddress 
        and the packet was bridged on a VPN, 
        where the docsL2vpnIdx matched this instance's  
        dot1qVlanIndex."
		characteristics "-N docsL2vpnDot1qTpGroupExtReceivePkts -O 1.3.6.1.4.1.4491.2.1.8.1.13.1.2 -T COUNTER -A RO -X equal -F 0"

	)

agenterrors (
	1	"cannot dispatch request"
	2	"select(2) failed"
	3	"sendto(2) failed"
	4	"recvfrom(2) failed"
	5	"no response from system"
	6	"response too big"
	7	"missing attribute"
	8	"bad attribute type"
	9	"cannot get sysUpTime"
	10	"sysUpTime type bad"
	11	"unknown SNMP error"
	12	"bad variable value"
	13	"variable is read only"
	14	"general error"
	15	"cannot make request PDU"
	16	"cannot make request varbind list"
	17	"cannot parse response PDU"
	18	"request ID - response ID mismatch"
	19	"string contains non-displayable characters"
	20	"cannot open schema file"
	21	"cannot parse schema file"
	22	"cannot open host file"
	23	"cannot parse host file"
	24	"attribute unavailable for set operations"
             )
)


## End of Agent Definitions
