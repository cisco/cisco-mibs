## THIS FILE WAS GENERATED BY MIB2SCHEMA
## For attributes containing printable ASCII
## characters, you may add a format string
## specification in the attribute characteristics
## line
## As described in RFC 1157, some agents cannot
## accept messages whose length exceeds 484 octets
## For those groups whose var-bind list has many
## entries, you may split them up into different
## var-bind lists
proxy CISCO-SSM-PROV-MIB
description "CISCO-SSM-PROV-MIB agent"
serial 2
rpcid 100122
(

## Enumerated Type Definitions

	enum	ssmProvFeatureNeedsImageE (
	   1	"true"
	   2	"false"
	)

	enum	ssmProvFeatureIfForceRemoveE (
	   1	"forceReset"
	   2	"noop"
	)

	enum	ssmProvFeatureIfRowStatusE (
	   1	"active"
	   2	"notInService"
	   3	"notReady"
	   4	"createAndGo"
	   5	"createAndWait"
	   6	"destroy"
	)


## Group and Table Declarations

	table	ssmProvFeatureTable
	description "A table containing list of features that can be
         provisioned on an SSM."
	characteristics "-K ???"
	(
		notaccessible octet[80]	ssmProvFeatureName
		description 	"The name of the feature that can be provisioned
         on the SSM."
		characteristics "-N ssmProvFeatureName -O 1.3.6.1.4.1.9.9.448.1.1.1.1.1 -T STRING -A NA -X equal -F 0"

		readonly enum ssmProvFeatureNeedsImageE	ssmProvFeatureNeedsImage
		description 	"The object indicates if the feature needs an 
        image name to be specified during provisioning.
        
        If the value of this object is 'true', then an
        image needs to be specified.
        
        If the value of this object is 'false', then an
        image does not need to be specified."
		characteristics "-N ssmProvFeatureNeedsImage -O 1.3.6.1.4.1.9.9.448.1.1.1.1.2 -T INTEGER -A RO -X equal -F 0"

	)

	table	ssmProvFeatureIfTable
	description "A table containing feature related information
        for interfaces.  This table gives a list of
        interfaces that are assigned to different features.
        The interfaces supported are of the type
        fibreChannel.
        
        If a feature is removed from ssmProvFeatureTable,
        then corresponding entries in ssmProvFeatureIfTable
        will also get removed."
	characteristics "-K ???"
	(
		notaccessible int	ssmProvFeatureIfStartPort
		description 	"The starting interface index on the SSM card to
         which a feature is going to be assigned.  A feature
         is assigned to a DPP by specifying a starting and
         ending port."
		characteristics "-N ssmProvFeatureIfStartPort -O 1.3.6.1.4.1.9.9.448.1.1.2.1.1 -T INTEGER -A NA -X equal -F 0"

		notaccessible int	ssmProvFeatureIfEndPort
		description 	"The end interface index on the SSM card to
         which a feature is going to be assigned.  A
         feature is assigned to a DPP by specifying a
         start and end port."
		characteristics "-N ssmProvFeatureIfEndPort -O 1.3.6.1.4.1.9.9.448.1.1.2.1.2 -T INTEGER -A NA -X equal -F 0"

		notaccessible octet[80]	ssmProvFeatureIfFeatureName
		description 	"The SSM feature name bound to the interface. This
         must be one of the features listed in 
         ssmProvFeatureTable."
		characteristics "-N ssmProvFeatureIfFeatureName -O 1.3.6.1.4.1.9.9.448.1.1.2.1.3 -T STRING -A NA -X equal -F 0"

		readwrite enum ssmProvFeatureIfForceRemoveE	ssmProvFeatureIfForceRemove
		description 	"This object indicates if the provisioning should be 
         forcefully removed. The object must be set 
         concurrently with setting the corresponding instance 
         of ssmProvFeatureIfRowStatus to 'destroy'.
        
         The 'forceReset' option will cause the configuration
         to be removed and the SSM card to be reset in the 
         process.
        
         No action is taken if this object is set to 'noop'.
        
         The value of this object when read is always 'noop'."
		characteristics "-N ssmProvFeatureIfForceRemove -O 1.3.6.1.4.1.9.9.448.1.1.2.1.4 -T INTEGER -A RW -X equal -F 0"

		readwrite octet[80]	ssmProvFeatureIfPartnerImageURI
		description 	"The object representing the location on the local
         system, from where the partner image will be picked
         for installation. The value of this object is only
         relevant if the ssmProvFeatureNeedsImage object is
         set to 'true' for that feature in 
         ssmProvFeatureTable."
		characteristics "-N ssmProvFeatureIfPartnerImageURI -O 1.3.6.1.4.1.9.9.448.1.1.2.1.5 -T STRING -A RW -X equal -F 0"

		readwrite enum ssmProvFeatureIfRowStatusE	ssmProvFeatureIfRowStatus
		description 	"The status of this conceptual row."
		characteristics "-N ssmProvFeatureIfRowStatus -O 1.3.6.1.4.1.9.9.448.1.1.2.1.6 -T INTEGER -A RW -X equal -F 0"

	)

	table	ssmProvDppTable
	description "A table containing DPP information for interfaces.
        This table gives a list of DPPs for every interface.
        Entries get added to the table when an SSM comes 
        online and removed when it goes offline."
	characteristics "-K ONE"
	(
		notaccessible int	ssmProvDppStartPort
		description 	"The starting interface index on the SSM card 
        that corresponds to a DPP."
		characteristics "-N ssmProvDppStartPort -O 1.3.6.1.4.1.9.9.448.1.1.3.1.1 -T INTEGER -A NA -X equal -F 0"

		readonly int	ssmProvDppEndPort
		description 	"The ending interface index on the SSM card 
        that corresponds to a DPP."
		characteristics "-N ssmProvDppEndPort -O 1.3.6.1.4.1.9.9.448.1.1.3.1.2 -T INTEGER -A RO -X equal -F 0"

		readonly octet[80]	ssmProvDppName
		description 	"The DPP that is associated with the given
        starting and ending interface."
		characteristics "-N ssmProvDppName -O 1.3.6.1.4.1.9.9.448.1.1.3.1.3 -T STRING -A RO -X equal -F 0"

	)

agenterrors (
	1	"cannot dispatch request"
	2	"select(2) failed"
	3	"sendto(2) failed"
	4	"recvfrom(2) failed"
	5	"no response from system"
	6	"response too big"
	7	"missing attribute"
	8	"bad attribute type"
	9	"cannot get sysUpTime"
	10	"sysUpTime type bad"
	11	"unknown SNMP error"
	12	"bad variable value"
	13	"variable is read only"
	14	"general error"
	15	"cannot make request PDU"
	16	"cannot make request varbind list"
	17	"cannot parse response PDU"
	18	"request ID - response ID mismatch"
	19	"string contains non-displayable characters"
	20	"cannot open schema file"
	21	"cannot parse schema file"
	22	"cannot open host file"
	23	"cannot parse host file"
	24	"attribute unavailable for set operations"
             )
)


## End of Agent Definitions
