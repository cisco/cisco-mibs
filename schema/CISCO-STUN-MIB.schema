## THIS FILE WAS GENERATED BY MIB2SCHEMA
## For attributes containing printable ASCII
## characters, you may add a format string
## specification in the attribute characteristics
## line
## As described in RFC 1157, some agents cannot
## accept messages whose length exceeds 484 octets
## For those groups whose var-bind list has many
## entries, you may split them up into different
## var-bind lists
proxy CISCO-STUN-MIB
description "CISCO-STUN-MIB agent"
serial 2
rpcid 100122
(

## Enumerated Type Definitions

	enum	stunProtocolTypeE (
	   1	"basic"
	   2	"sdlc"
	   3	"sdlctg"
	   4	"custom"
	)

	enum	stunPortDefaultPeerTypeE (
	   1	"other"
	   2	"ip"
	   3	"direct"
	   4	"frameRelay"
	)

	enum	stunRouteTypeE (
	   1	"other"
	   2	"ip"
	   3	"direct"
	   4	"frameRelay"
	)

	enum	stunRoutePriorityE (
	   1	"low"
	   2	"normal"
	   3	"medium"
	   4	"high"
	)

	enum	stunRoutePeerStateE (
	   1	"dead"
	   2	"closed"
	   3	"opening"
	   4	"openWait"
	   5	"connected"
	   6	"direct"
	)

	enum	stunRouteLocalAckE (
	   1	"true"
	   2	"false"
	)


## Group and Table Declarations


## Group and Table Declarations

	group	stunGlobal
	description "(null)"
	characteristics "-K NONE"
	(
		readonly netaddress	stunIPAddr
		description 	"The configured IP address used for all serial tunnelling
         in this router."
		characteristics "-N stunIPAddr -O 1.3.6.1.4.1.9.9.30.1.1.1 -T IPADDRESS -A RO -X equal -F 0"

	)

	table	stunGroupTable
	description "A table of entries representing STUN groups configured on the
         router.  Each STUN-enabled interface is assigned to a STUN
         group, and packets can only travel between STUN-enabled
         interfaces in the same group."
	characteristics "-K ONE"
	(
		notaccessible int	stunGroupIndex
		description 	"The configured STUN group number."
		characteristics "-N stunGroupIndex -O 1.3.6.1.4.1.9.9.30.1.2.1.1.1 -T INTEGER -A NA -X equal -F 0"

		readonly enum stunProtocolTypeE	stunProtocolType
		description 	"The protocol type for this STUN group."
		characteristics "-N stunProtocolType -O 1.3.6.1.4.1.9.9.30.1.2.1.1.2 -T INTEGER -A RO -X equal -F 0"

	)

	table	stunPortTable
	description "A list of STUN-enabled interfaces (ports)."
	characteristics "-K ???"
	(
		readonly int	stunPortGroupIndex
		description 	"The group number to which the stun port
         belongs.  Frames will only be routed to other 
         ports (on this or another router) in the same 
         stun group.  This group must match a stunGroupIndex
         in the stunGroupTable."
		characteristics "-N stunPortGroupIndex -O 1.3.6.1.4.1.9.9.30.1.3.1.1.1 -T INTEGER -A RO -X equal -F 0"

		readonly enum stunPortDefaultPeerTypeE	stunPortDefaultPeerType
		description 	"The type of identification of the default partner 
         for unrecognized addresses. If there
         is no default route then the stunRouteType
         field of stunPortDefaultRemote is 'other'.
         If ip then the value is in stunRouteIP; if serial or
         serialDirect then the value is in stunRouteSerial."
		characteristics "-N stunPortDefaultPeerType -O 1.3.6.1.4.1.9.9.30.1.3.1.1.2 -T INTEGER -A RO -X equal -F 0"

		readonly netaddress	stunPortDefaultPeerIP
		description 	"The ip address of the remote default STUN partner,
         for unrecognized addresses.  
         0.0.0.0 is returned if the default route type is not ip."
		characteristics "-N stunPortDefaultPeerIP -O 1.3.6.1.4.1.9.9.30.1.3.1.1.3 -T IPADDRESS -A RO -X equal -F 0"

		readonly int	stunPortDefaultPeerSerialInterface
		description 	"If stunRouteType is serial then this is the 
         serial interface index of the point-to-point
         link to the remote partner; 
         if stunRouteType is serialDirect then the 
         partner is in the local STUN.
         if stunRouteType is ip then this field is
         0."
		characteristics "-N stunPortDefaultPeerSerialInterface -O 1.3.6.1.4.1.9.9.30.1.3.1.1.4 -T INTEGER -A RO -X equal -F 0"

	)

	table	stunRouteTable
	description "A table containing information about specific SDLC
         addresses.  There is one table entry for each SDLC
         address configured by the STUN ROUTE command."
	characteristics "-K ONE"
	(
		notaccessible int	stunRouteStationAddress
		description 	"The poll address of the station.  256 indicates the ALL parameter
         on the STUN ROUTE command, which is the route for all 
         unrecognized addresses."
		characteristics "-N stunRouteStationAddress -O 1.3.6.1.4.1.9.9.30.1.4.1.1.1 -T INTEGER -A NA -X equal -F 0"

		readonly enum stunRouteTypeE	stunRouteType
		description 	"The type of identification of the remote partner."
		characteristics "-N stunRouteType -O 1.3.6.1.4.1.9.9.30.1.4.1.1.2 -T INTEGER -A RO -X equal -F 0"

		readonly netaddress	stunRouteRemoteIP
		description 	"The ip address of the remote STUN partner. 
         0.0.0.0 if partner type is not ip."
		characteristics "-N stunRouteRemoteIP -O 1.3.6.1.4.1.9.9.30.1.4.1.1.3 -T IPADDRESS -A RO -X equal -F 0"

		readonly int	stunRouteSerialInterface
		description 	"The local interface index to the remote partner.
         0 is returned if the partner type is not direct or frameRelay."
		characteristics "-N stunRouteSerialInterface -O 1.3.6.1.4.1.9.9.30.1.4.1.1.4 -T INTEGER -A RO -X equal -F 0"

		readonly enum stunRoutePriorityE	stunRoutePriority
		description 	"The priority with which this station's traffic will
         be routed across the network."
		characteristics "-N stunRoutePriority -O 1.3.6.1.4.1.9.9.30.1.4.1.1.5 -T INTEGER -A RO -X equal -F 0"

		readonly enum stunRoutePeerStateE	stunRoutePeerState
		description 	"The state of the peer connection through the
         STUN tunnel."
		characteristics "-N stunRoutePeerState -O 1.3.6.1.4.1.9.9.30.1.4.1.1.6 -T INTEGER -A RO -X equal -F 0"

		readonly enum stunRouteLocalAckE	stunRouteLocalAck
		description 	"Indicates if the STUN connection is locally acknowledged.       
         TRUE-> STUN connection is locally acknowledged
         FALSE-> STUN connection is not locally acknowledged"
		characteristics "-N stunRouteLocalAck -O 1.3.6.1.4.1.9.9.30.1.4.1.1.7 -T INTEGER -A RO -X equal -F 0"

		readonly counter	stunRouteRxPackets
		description 	"Count of frames received from the serial
         interface with this station's address."
		characteristics "-N stunRouteRxPackets -O 1.3.6.1.4.1.9.9.30.1.4.1.1.8 -T COUNTER -A RO -X equal -F 0"

		readonly counter	stunRouteTxPackets
		description 	"Count of frames transmitted at the serial
         interface with this station's address."
		characteristics "-N stunRouteTxPackets -O 1.3.6.1.4.1.9.9.30.1.4.1.1.9 -T COUNTER -A RO -X equal -F 0"

		readonly counter	stunRouteRxBytes
		description 	"Count of bytes received from the serial
         interface with this station's address."
		characteristics "-N stunRouteRxBytes -O 1.3.6.1.4.1.9.9.30.1.4.1.1.10 -T COUNTER -A RO -X equal -F 0"

		readonly counter	stunRouteTxBytes
		description 	"Count of bytes transmitted at the serial
         interface with this station's address."
		characteristics "-N stunRouteTxBytes -O 1.3.6.1.4.1.9.9.30.1.4.1.1.11 -T COUNTER -A RO -X equal -F 0"

	)

agenterrors (
	1	"cannot dispatch request"
	2	"select(2) failed"
	3	"sendto(2) failed"
	4	"recvfrom(2) failed"
	5	"no response from system"
	6	"response too big"
	7	"missing attribute"
	8	"bad attribute type"
	9	"cannot get sysUpTime"
	10	"sysUpTime type bad"
	11	"unknown SNMP error"
	12	"bad variable value"
	13	"variable is read only"
	14	"general error"
	15	"cannot make request PDU"
	16	"cannot make request varbind list"
	17	"cannot parse response PDU"
	18	"request ID - response ID mismatch"
	19	"string contains non-displayable characters"
	20	"cannot open schema file"
	21	"cannot parse schema file"
	22	"cannot open host file"
	23	"cannot parse host file"
	24	"attribute unavailable for set operations"
             )
)


## End of Agent Definitions
