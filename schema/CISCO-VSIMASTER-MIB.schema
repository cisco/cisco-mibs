## THIS FILE WAS GENERATED BY MIB2SCHEMA
## For attributes containing printable ASCII
## characters, you may add a format string
## specification in the attribute characteristics
## line
## As described in RFC 1157, some agents cannot
## accept messages whose length exceeds 484 octets
## For those groups whose var-bind list has many
## entries, you may split them up into different
## var-bind lists
proxy CISCO-VSIMASTER-MIB
description "CISCO-VSIMASTER-MIB agent"
serial 2
rpcid 100122
(

## Enumerated Type Definitions

	enum	vsiControllerTypeE (
	   1	"other"
	   2	"mpls"
	   3	"pnni"
	   4	"par"
	)

	enum	vsiAvailableChnlThresholdStatusE (
	   1	"warning"
	   2	"alarm"
	   3	"recovery"
	   4	"normal"
	)

	enum	vsiSessionStateE (
	   1	"inactive"
	   2	"unknown"
	   3	"configuring"
	   4	"resyncStarting"
	   5	"resyncUnderway"
	   6	"resyncEnding"
	   7	"discovery"
	   8	"established"
	   9	"shutdownStarting"
	)

	enum	vsiLogicalIfOperStateE (
	   1	"removed"
	   2	"active"
	   3	"failedExternal"
	   4	"failedInternal"
	)

	enum	vsiLogicalIfAdminStateE (
	   1	"down"
	   2	"up"
	   3	"pendingDown"
	)

	enum	vsiLogicalIfVcMergeSupportedE (
	   1	"true"
	   2	"false"
	)

	enum	vsiLogicalIfMulticastSupportedE (
	   1	"true"
	   2	"false"
	)

	enum	vsiLogicalIfVpiTranslatedE (
	   1	"true"
	   2	"false"
	)

	enum	vsiLogicalIfStrictSigRangeE (
	   1	"true"
	   2	"false"
	)

	enum	vsiXCStateE (
	   1	"deleted"
	   2	"reserved"
	   3	"committed"
	   4	"reservedFail"
	)

	enum	vsiControllerTrapEnableE (
	   1	"true"
	   2	"false"
	)

	enum	vsiSessionTrapEnableE (
	   1	"true"
	   2	"false"
	)

	enum	vsiLogicalIfTrapEnableE (
	   1	"true"
	   2	"false"
	)

	enum	vsiXCTrapEnableE (
	   1	"true"
	   2	"false"
	)

	enum	vsiVSITrapEnableE (
	   1	"true"
	   2	"false"
	)


## Group and Table Declarations

	group	ciscoVsiMasterObjects
	description "(null)"
	characteristics "-K NONE"
	(
		readwrite enum vsiControllerTrapEnableE	vsiControllerTrapEnable
		description 	"If this object is true, both vsiControllerAdded and  
         vsiControllerDeleted notifications are enabled."
		characteristics "-N vsiControllerTrapEnable -O 1.3.6.1.4.1.9.9.162.1.5 -T INTEGER -A RW -X equal -F 0"

		readwrite enum vsiSessionTrapEnableE	vsiSessionTrapEnable
		description 	"If this object is true, both vsiSessionUp 
         and vsiSessionDown notifications are enabled."
		characteristics "-N vsiSessionTrapEnable -O 1.3.6.1.4.1.9.9.162.1.6 -T INTEGER -A RW -X equal -F 0"

		readwrite enum vsiLogicalIfTrapEnableE	vsiLogicalIfTrapEnable
		description 	"If this object is true, both vsiLogicalIfUp 
         and vsiLogicalIfDown notifications are enabled."
		characteristics "-N vsiLogicalIfTrapEnable -O 1.3.6.1.4.1.9.9.162.1.7 -T INTEGER -A RW -X equal -F 0"

		readwrite enum vsiXCTrapEnableE	vsiXCTrapEnable
		description 	"If this object is true, both vsiXCFailed and 
         vsiLcnExhaustionNotice notifications are enabled."
		characteristics "-N vsiXCTrapEnable -O 1.3.6.1.4.1.9.9.162.1.8 -T INTEGER -A RW -X equal -F 0"

		readwrite enum vsiVSITrapEnableE	vsiVSITrapEnable
		description 	"If this object is true, all of  
         vsiControllerTrapEnable, vsiSessionTrapEnable,  
         vsiLogicalIfTrapEnable, and vsiXCTrapEnable 
         are also set to true.  This allows for enabling 
         and disabling all notifications as a group."
		characteristics "-N vsiVSITrapEnable -O 1.3.6.1.4.1.9.9.162.1.9 -T INTEGER -A RW -X equal -F 0"

	)

	table	vsiMasterControllerTable
	description "A table containing a set of VSI controllers.  Each controller  
         represents an instance of the VSI Master control protocol. 
         A controller communicates with a set of VSI Slaves across  
         a control interface.  Running the VSI protocol, the controller 
         supports a network control application.   
         Through VSI, such an application can control the Virtual  
         Circuit cross-connect table inside the ATM switch,  
         perform monitoring and statistics collection of interfaces and  
         virtual circuits on the switch, and discover configuration  
         information from within the switch."
	characteristics "-K ONE"
	(
		readonly int	vsiControllerIndex
		description 	"A unique value per VSI controller, greater than 0.  This 
         object gets its value from the vsiControllerId, and is used 
         for row selection within the vsiMasterControllerTable."
		characteristics "-N vsiControllerIndex -O 1.3.6.1.4.1.9.9.162.1.1.1.1 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiControllerId
		description 	"The controller identifier for the VSI controller.  Values are  
         arbitrarily assigned by the VSI protocol implementation being  
         managed via this MIB, with local significance only.  These 
         values appear in the header of all VSI protocol messages."
		characteristics "-N vsiControllerId -O 1.3.6.1.4.1.9.9.162.1.1.1.2 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiCrossConnects
		description 	"This object contains a value equal to the number of  
         cross-connects being controlled via VSI protocol via 
         this controller."
		characteristics "-N vsiCrossConnects -O 1.3.6.1.4.1.9.9.162.1.1.1.3 -T INTEGER -A RO -X equal -F 0"

		readonly enum vsiControllerTypeE	vsiControllerType
		description 	"A VSI controller represents an instance of a service layer 
         accessible to a network control application.  This object  
         serves to identify the type of the network control  
         application currently being supported by this controller.   
         other(1): the type is unspecified. 
         mpls(2): indicates that the client is a Label Switch  
                  Controller performing Multi Protocol Label Switching. 
         pnni(3): indicates that the client is an instance of the  
                  ATM Private Network to Network Interface.   
         par(4): indicates that the client is an instance of ATM  
                  Portable Autoroute."
		characteristics "-N vsiControllerType -O 1.3.6.1.4.1.9.9.162.1.1.1.4 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiBaseVersionSupported
		description 	"A VSI Master protocol instance defines internally a set of 
         versions of the protocol that it will support.  These versions  
         are numbered, by default, starting from 1.  This object 
         makes available the lowest numbered version that the VSI 
         Master protocol supports.  This object is related to the other 
         vsiMasterControllerEntry objects, vsiTopVersionSupported and  
         vsiVersionInUse."
		characteristics "-N vsiBaseVersionSupported -O 1.3.6.1.4.1.9.9.162.1.1.1.5 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiTopVersionSupported
		description 	"A VSI Master protocol instance defines internally a set of 
         versions of the protocol that it will support.  These versions  
         are numbered, by default, starting from 1.  This object 
         makes available the highest numbered version that the VSI 
         Master protocol supports.  This object is related to the other 
         vsiMasterControllerEntry objects, vsiBaseVersionSupported and  
         vsiVersionInUse."
		characteristics "-N vsiTopVersionSupported -O 1.3.6.1.4.1.9.9.162.1.1.1.6 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiVersionInUse
		description 	"The number of the VSI version currently being used by this  
         controller.  Protocol selection procedures dictate that this  
         should be the highest value of protocol version, between  
         vsiBaseVersionSupported and vsiTopVersionSupported, that is  
         supported by all slaves.  An exception to this rule will  
         occur when the version is constrained to a specific value  
         indicated by the vsiSpecifiedVersion object."
		characteristics "-N vsiVersionInUse -O 1.3.6.1.4.1.9.9.162.1.1.1.7 -T INTEGER -A RO -X equal -F 0"

		readwrite int	vsiSpecifiedVersion
		description 	"When this object is written with a non-zero value,  
        re-synchronization procedures will be initiated, if necessary, 
        in order to constrain the master and slaves to use the  
         indicated protocol version. When written with zero (0), the  
         master and slaves should use the version dictated by the  
         usual protocol version selection procedures; resynchronization 
         procedures will be invoked, if necessary, to achieve this  
         result."
		characteristics "-N vsiSpecifiedVersion -O 1.3.6.1.4.1.9.9.162.1.1.1.8 -T INTEGER -A RW -X equal -F 0"

		readonly int	vsiControlInterface
		description 	"A VSI controller communicates with a set of VSI Slaves  
         across a control interface.  The controller is connected to 
         the ATM switch via an ATM link called the Slave Control Link. 
         The interface at the Master end of the link is the Master  
         Control Port, and is described by this object; it is the  
         ifIndex of this Master Control Port."
		characteristics "-N vsiControlInterface -O 1.3.6.1.4.1.9.9.162.1.1.1.9 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiLogicalControlInterface
		description 	"This object describes the instance of a logical interface index 
         bound to the slave end of the Slave Control Link, which is 
         referred to in the description of vsiControlInterface."
		characteristics "-N vsiLogicalControlInterface -O 1.3.6.1.4.1.9.9.162.1.1.1.10 -T INTEGER -A RO -X equal -F 0"

		readonly netaddress	vsiControlIfIpAddress
		description 	"The IP address of the interface identified by 
         the associated vsiControlInterface object."
		characteristics "-N vsiControlIfIpAddress -O 1.3.6.1.4.1.9.9.162.1.1.1.11 -T IPADDRESS -A RO -X equal -F 0"

		readwrite int	vsiAvailableChnlWarnThreshold
		description 	"A controller has in its domain multiple instances of the 
         vsiLogicalIfAvailIngressChnls and  
         vsiLogicalIfAvailEgressChnls counters.  This set of values, 
         associated with all the logical interfaces in the  
         vsiLogicalIfTable, is monitored so as to note the minimum  
         value.   
        
         The minimum observed value is compared against this threshold 
         object.  If the minimum observed value is less than this  
         threshold, and the value at the last comparison was greater  
         than or equal to this threshold, a single event will be  
         generated, to indicate a warning that available channels are  
         being depleted.  After a warning event is generated, another  
         such event will not be generated unless the  
         vsiAvailableChnlAlarmThreshold is reached.  If the  
         vsiAvailableChnlAlarmThreshold is reached and then the minimum 
         observed value is greater than or equal to this threshold, a  
         single event is generated, to indicate that a recovery  
         of resources has occurred. 
        
         If this object contains the value zero, then the thresholding 
         operation will be disabled.  Otherwise, the value of this 
         object must be greater than or equal to the value of the  
         associated vsiAvailableChnlAlarmThreshold object. 
         Notification emissions are also gated by the  
         vsiXCTrapEnable object."
		characteristics "-N vsiAvailableChnlWarnThreshold -O 1.3.6.1.4.1.9.9.162.1.1.1.12 -T INTEGER -A RW -X equal -F 0"

		readwrite int	vsiAvailableChnlAlarmThreshold
		description 	"A controller has in its domain multiple instances of the 
         vsiLogicalIfAvailIngressChnls and  
         vsiLogicalIfAvailEgressChnls counters.  This set of values, 
         associated with all the logical interfaces in the  
         vsiLogicalIfTable, is monitored so as to note the minimum  
         value.   
        
         The minimum observed value is compared against this threshold 
         object.  If the minimum observed value is less than this  
         threshold, and the value at the last comparison was greater  
         than or equal to this threshold, a single event will be  
         generated, to indicate an alarm that available channels are  
         being depleted.  After an alarm event is generated, another  
         such event will not be generated unless the  
         vsiAvailableChnlWarnThreshold is reached. 
        
         If this object contains the value zero, then the thresholding 
         operation will be disabled.  Otherwise, the value of this 
         object must be less than or equal to the value of the  
         associated vsiAvailableChnlWarnThreshold object.   
         Notification emissions are also gated by the  
         vsiXCTrapEnable object."
		characteristics "-N vsiAvailableChnlAlarmThreshold -O 1.3.6.1.4.1.9.9.162.1.1.1.13 -T INTEGER -A RW -X equal -F 0"

		readonly enum vsiAvailableChnlThresholdStatusE	vsiAvailableChnlThresholdStatus
		description 	"As described for the vsiAvailableChnlWarnThreshold and  
         vsiAvailableChnlAlarmThreshold objects, events are generated 
         when these thresholds are crossed.  This object defines a  
         set of unique values that are used with the  
         vsiLcnExhaustionNotice to convey information about the  
         context of a threshold crossing.   
        
         warning(1): the event qualifier for indicating 
                     vsiAvailableChnlWarnThreshold violations. 
         alarm(2): the event qualifier for indicating  
                   vsiAvailableChnlAlarmThreshold violations. 
         recovery(3): the event qualifier for indicating 
                      vsiAvailableChnlWarnThreshold recovery. 
         normal(4): the initial value that indicates that no 
                    thresholds have been crossed."
		characteristics "-N vsiAvailableChnlThresholdStatus -O 1.3.6.1.4.1.9.9.162.1.1.1.14 -T INTEGER -A RO -X equal -F 0"

	)

	table	vsiSessionTable
	description "A table containing VSI master-slave session status information. 
         Each VSI controller manages a set of VSI slaves, via a protocol 
         instance called a Session.  VSI slaves are entities that reside 
         on the controlled ATM switch.  Their number and characteristics 
         are discovered by the VSI controller via the VSI protocol.   
         This table has an entry for each slave so discovered."
	characteristics "-K TWO"
	(
		notaccessible int	vsiSessionControllerIndex
		description 	"The ID of the controller which controls this master-slave  
         session.  This identifies which vsiMasterControllerEntry  
         represents the controller communicating with a slave through  
         this Session."
		characteristics "-N vsiSessionControllerIndex -O 1.3.6.1.4.1.9.9.162.1.2.1.1 -T INTEGER -A NA -X equal -F 0"

		notaccessible int	vsiSessionIndex
		description 	"A VSI controller discovers a set of slaves through VSI 
         protocol.  Each of the discovered slaves is bound to a 
         protocol Session and each of these Sessions occupies a 
         row in the vsiSessionTable.  This object is the row selector. 
         The value should remain constant after a VSI session is  
         reinitialized."
		characteristics "-N vsiSessionIndex -O 1.3.6.1.4.1.9.9.162.1.2.1.2 -T INTEGER -A NA -X equal -F 0"

		readonly int	vsiSessionVpi
		description 	"The virtual path identifier for this VSI master-slave session. 
         The Slave Control Link, referred to in the description of 
         vsiLogicalControlInterface, supports an ATM control VC.  This 
         is the vpi for that link."
		characteristics "-N vsiSessionVpi -O 1.3.6.1.4.1.9.9.162.1.2.1.3 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiSessionVci
		description 	"The virtual channel identifier for this VSI master-slave  
         session.  The Slave Control Link, referred to in the  
         description of vsiLogicalControlInterface, supports an  
         ATM control VC.  This is the vci for that link."
		characteristics "-N vsiSessionVci -O 1.3.6.1.4.1.9.9.162.1.2.1.4 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiSessionSwitchId
		description 	"The identifier of the switch which participates in this VSI  
         master-slave session.  The switch terminates the virtual 
         circuit identified by the vpi/vci pair referred to by  
         vsiSessionVpi and vsiSessionVci."
		characteristics "-N vsiSessionSwitchId -O 1.3.6.1.4.1.9.9.162.1.2.1.5 -T INTEGER -A RO -X equal -F 0"

		readonly octet[255]	vsiSessionSwitchName
		description 	"The IP hostname for the slave switch which is controlled by  
         this session.  This is an ascii string of maximum  
         length 64 characters, learned from the ATM switch."
		characteristics "-N vsiSessionSwitchName -O 1.3.6.1.4.1.9.9.162.1.2.1.6 -T STRING -A RO -X equal -F 0"

		readonly int	vsiSessionSlaveId
		description 	"The identifier of the slave for this VSI master-slave session. 
         This value is learned from the slave on the switch."
		characteristics "-N vsiSessionSlaveId -O 1.3.6.1.4.1.9.9.162.1.2.1.7 -T INTEGER -A RO -X equal -F 0"

		readonly enum vsiSessionStateE	vsiSessionState
		description 	"The state of the VSI session. 
         inactive(1): communications data structures for communicating 
         with the slave have been created, but the VSI protocol has  
         not been started. 
         unknown(2): slave has been queried, but its configuration data 
         has not yet been received. 
         configuring(3): configuration data has been sent to the slave, 
         but a response has not yet been received. 
         resyncStarting(4): a re-sync command has been sent to the  
         slave but a response has not yet been received. 
         resyncUnderway(5): a re-sync command response has been  
         received from the slave, but the network applications have  
         not yet completed reasserting connections. 
         resyncEnding(6): a command has been sent to end re-sync, but 
         a response has not yet been received. 
         discovery(7): a query has been sent to the slave for  
         interface information, but a response has not yet  
         been received. 
         established(8): discovery has completed. 
         shutdownStarting(9): the master has started an orderly  
         shutdown of the slave."
		characteristics "-N vsiSessionState -O 1.3.6.1.4.1.9.9.162.1.2.1.8 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiSessionWindowSize
		description 	"The maximum number of command messages the VSI master will  
         send before requiring a response from slave.  If the value  
         is 0, then flow control is not enabled."
		characteristics "-N vsiSessionWindowSize -O 1.3.6.1.4.1.9.9.162.1.2.1.9 -T INTEGER -A RO -X equal -F 0"

		readonly gauge	vsiSessionCmdsPending
		description 	"The number of unacknowledged command messages.  The VSI master 
         sends commands to the slave, without receiving acknowledgement, 
         until the window size threshold has been reached.  This 
         object is a count of the number of commands that have been 
         sent and that are awaiting acknowledgment."
		characteristics "-N vsiSessionCmdsPending -O 1.3.6.1.4.1.9.9.162.1.2.1.10 -T GAUGE -A RO -X equal -F 0"

		readonly int	vsiSessionActiveId
		description 	"ID supplied by the slave to identify the session.  This value  
         is interpreted by the master, when a change occurs, to  
         indicate that a resync should be inititated."
		characteristics "-N vsiSessionActiveId -O 1.3.6.1.4.1.9.9.162.1.2.1.11 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiSessionPowerupId
		description 	"ID supplied by the slave to identify the session at powerup. 
         This is the initial value for vsiSessionActiveId, and its  
         default is zero.  Changes in vsiSessionActiveId signal a  
         resync."
		characteristics "-N vsiSessionPowerupId -O 1.3.6.1.4.1.9.9.162.1.2.1.12 -T INTEGER -A RO -X equal -F 0"

	)

	table	vsiLogicalIfTable
	description "A table containing information about logical interfaces 
         on the slave switch controlled via VSI.  Logical interfaces 
         are an abstraction of exposed, external ATM interfaces that 
         are available for connections.  When two external interfaces, 
         represented by two logical interfaces, are paired, they  
         provide physical paths through the switch.  These physical  
         pathways support cross-connects."
	characteristics "-K TWO"
	(
		readonly int	vsiLogicalIfControllerIndex
		description 	"The VSI controller index of the controller through which 
         this logical interface was learned."
		characteristics "-N vsiLogicalIfControllerIndex -O 1.3.6.1.4.1.9.9.162.1.3.1.1 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiLogicalIfIndex
		description 	"A unique value, greater than zero, for each VSI logical  
         interface.  Logical interface numbers are selected by the 
         switch to be unique and persistent.  The same number is 
         preserved through power failures.  This index is not the same 
         as an ifIndex.  It is an abstract number provided by the slave, 
         representing a virtual interface, which is an identifier to be 
         used when communicating with the slave about an interface."
		characteristics "-N vsiLogicalIfIndex -O 1.3.6.1.4.1.9.9.162.1.3.1.2 -T INTEGER -A RO -X equal -F 0"

		readonly octet[255]	vsiLogicalIfName
		description 	"The physical descriptor of the logical interface, as 
         reported by the VSI slave.  It is distinct from ifName."
		characteristics "-N vsiLogicalIfName -O 1.3.6.1.4.1.9.9.162.1.3.1.3 -T STRING -A RO -X equal -F 0"

		readonly enum vsiLogicalIfOperStateE	vsiLogicalIfOperState
		description 	"The VSI operational state of the logical interface as 
         reported to the VSI master by the VSI slave. 
         removed(1): the interface is not a valid interface on this 
                     controller, is not part of the controller's  
                     partition, or is not available. 
         active(2): the interface is active and ready for use. 
         failedExternal(3): external failure, such as a line alarm, 
                            detected. 
         failedInternal(4): local hardware has detected a failure, 
                            making the interface unusable."
		characteristics "-N vsiLogicalIfOperState -O 1.3.6.1.4.1.9.9.162.1.3.1.4 -T INTEGER -A RO -X equal -F 0"

		readonly enum vsiLogicalIfAdminStateE	vsiLogicalIfAdminState
		description 	"The current administrative (configured) state of the 
         interface, as reported to the VSI master by the VSI slave. 
         down(1): the interface is not a valid interface for the  
         controller. 
         up(2): the interface is configured for use by the controller. 
         pendingDown(3): the interface is being removed from service,  
         and the controller is requested to remove all connections."
		characteristics "-N vsiLogicalIfAdminState -O 1.3.6.1.4.1.9.9.162.1.3.1.5 -T INTEGER -A RO -X equal -F 0"

		readonly counter	vsiLogicalIfRxCells
		description 	"The number of cells received via this logical interface 
         as reported to the VSI master by the VSI slave."
		characteristics "-N vsiLogicalIfRxCells -O 1.3.6.1.4.1.9.9.162.1.3.1.6 -T COUNTER -A RO -X equal -F 0"

		readonly counter	vsiLogicalIfTxCells
		description 	"The number of cells transmitted via this logical 
         interface as reported to the VSI master by the VSI slave."
		characteristics "-N vsiLogicalIfTxCells -O 1.3.6.1.4.1.9.9.162.1.3.1.7 -T COUNTER -A RO -X equal -F 0"

		readonly counter	vsiLogicalIfRxCellsDiscarded
		description 	"The number of received cells discarded by this logical 
         interface as reported to the VSI master by the VSI slave."
		characteristics "-N vsiLogicalIfRxCellsDiscarded -O 1.3.6.1.4.1.9.9.162.1.3.1.8 -T COUNTER -A RO -X equal -F 0"

		readonly counter	vsiLogicalIfTxCellsDiscarded
		description 	"The number of transmitted cells discarded by this logical 
         interface as reported to the VSI master by the VSI slave."
		characteristics "-N vsiLogicalIfTxCellsDiscarded -O 1.3.6.1.4.1.9.9.162.1.3.1.9 -T COUNTER -A RO -X equal -F 0"

		readonly counter	vsiLogicalIfRxHeaderErrors
		description 	"The number of cells received via this logical 
         interface which had header errors, as reported to  
         the VSI master by the VSI slave."
		characteristics "-N vsiLogicalIfRxHeaderErrors -O 1.3.6.1.4.1.9.9.162.1.3.1.10 -T COUNTER -A RO -X equal -F 0"

		readonly counter	vsiLogicalIfRxInvalidAddrs
		description 	"The number of cells received by this logical 
         interface which had invalid addresses, as 
         reported to the VSI master by the VSI slave. 
         An unknown vpi/vci value set is an example of 
         of an invalid address."
		characteristics "-N vsiLogicalIfRxInvalidAddrs -O 1.3.6.1.4.1.9.9.162.1.3.1.11 -T COUNTER -A RO -X equal -F 0"

		readonly gauge	vsiLogicalIfEndPointsInUse
		description 	"The number of end points in use on this logical  
         interface, as reported to the VSI master by the  
         VSI slave.  This reflects an aggregate count of 
         input, output and bi-directional end points. 
         If the logical interface has not been 
         removed, but does not currently support any cross 
         connects, this object will be reported as zero (0)."
		characteristics "-N vsiLogicalIfEndPointsInUse -O 1.3.6.1.4.1.9.9.162.1.3.1.12 -T GAUGE -A RO -X equal -F 0"

		readonly gauge	vsiLogicalIfAvailIngressChnls
		description 	"The number of ingress channels available for this 
         logical interface, as reported to the VSI master 
         by the VSI slave."
		characteristics "-N vsiLogicalIfAvailIngressChnls -O 1.3.6.1.4.1.9.9.162.1.3.1.13 -T GAUGE -A RO -X equal -F 0"

		readonly gauge	vsiLogicalIfAvailEgressChnls
		description 	"The number of egress channels available for this 
         logical interface, as reported to the VSI master 
         by the VSI slave."
		characteristics "-N vsiLogicalIfAvailEgressChnls -O 1.3.6.1.4.1.9.9.162.1.3.1.14 -T GAUGE -A RO -X equal -F 0"

		readonly gauge	vsiLogicalIfAvailIngressCellRate
		description 	"The available ingress cell rate in cells/second 
         for the logical interface, as reported to the VSI 
         master by the VSI slave."
		characteristics "-N vsiLogicalIfAvailIngressCellRate -O 1.3.6.1.4.1.9.9.162.1.3.1.15 -T GAUGE -A RO -X equal -F 0"

		readonly gauge	vsiLogicalIfAvailEgressCellRate
		description 	"The available egress cell rate in cells/second 
         for the logical interface, as reported to the VSI 
         master by the VSI slave."
		characteristics "-N vsiLogicalIfAvailEgressCellRate -O 1.3.6.1.4.1.9.9.162.1.3.1.16 -T GAUGE -A RO -X equal -F 0"

		readonly enum vsiLogicalIfVcMergeSupportedE	vsiLogicalIfVcMergeSupported
		description 	"If this object has the value true(1) then VC merge 
         capability is supported for this logical interface. 
         Otherwise VC merge capability is not supported on 
         this logical interface."
		characteristics "-N vsiLogicalIfVcMergeSupported -O 1.3.6.1.4.1.9.9.162.1.3.1.17 -T INTEGER -A RO -X equal -F 0"

		readonly enum vsiLogicalIfMulticastSupportedE	vsiLogicalIfMulticastSupported
		description 	"If this object has the value true(1) then multicast 
         capability is supported for this logical interface. 
         Otherwise multicast capability is not supported on 
         this logical interface."
		characteristics "-N vsiLogicalIfMulticastSupported -O 1.3.6.1.4.1.9.9.162.1.3.1.18 -T INTEGER -A RO -X equal -F 0"

		readonly enum vsiLogicalIfVpiTranslatedE	vsiLogicalIfVpiTranslated
		description 	"If this object has the value true(1) then vpi values 
         entering on this logical interface are translated at 
         the other end of the link.  This translation may be 
         an indication that the link is supporting a tunnel. 
         Otherwise, vpi values are not translated for traversal 
         of the link."
		characteristics "-N vsiLogicalIfVpiTranslated -O 1.3.6.1.4.1.9.9.162.1.3.1.19 -T INTEGER -A RO -X equal -F 0"

		readonly enum vsiLogicalIfStrictSigRangeE	vsiLogicalIfStrictSigRange
		description 	"If this object has the value true(1) then signalling  
         addresses must be in the strict advertised range for  
         the high-address (vpi).  Otherwise, signalling addresses 
         are not so constrained."
		characteristics "-N vsiLogicalIfStrictSigRange -O 1.3.6.1.4.1.9.9.162.1.3.1.20 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiLogicalIfMaxIngressCellRate
		description 	"The maximum ingress cell rate in cells/second for the  
         logical interface, as reported to the VSI.  This value is 
         the limiting value for vsiLogicalIfAvailIngressCellRate."
		characteristics "-N vsiLogicalIfMaxIngressCellRate -O 1.3.6.1.4.1.9.9.162.1.3.1.21 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiLogicalIfMaxEgressCellRate
		description 	"The maximum egress cell rate in cells/second 
         for the logical interface, as reported to the VSI 
         master by the VSI slave."
		characteristics "-N vsiLogicalIfMaxEgressCellRate -O 1.3.6.1.4.1.9.9.162.1.3.1.22 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiLogicalIfMinVpi
		description 	"Minimum allowed vpi value for this interface 
         as reported by VSI slave switch."
		characteristics "-N vsiLogicalIfMinVpi -O 1.3.6.1.4.1.9.9.162.1.3.1.23 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiLogicalIfMaxVpi
		description 	"Maximum allowed vpi value for this interface 
         as reported by VSI slave switch."
		characteristics "-N vsiLogicalIfMaxVpi -O 1.3.6.1.4.1.9.9.162.1.3.1.24 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiLogicalIfMinVci
		description 	"Minimum allowed vci value for this interface 
         as reported by VSI slave switch."
		characteristics "-N vsiLogicalIfMinVci -O 1.3.6.1.4.1.9.9.162.1.3.1.25 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiLogicalIfMaxVci
		description 	"Maximum allowed vci value for this interface 
         as reported by VSI slave switch."
		characteristics "-N vsiLogicalIfMaxVci -O 1.3.6.1.4.1.9.9.162.1.3.1.26 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiLogicalControlIfIndex
		description 	"The interface index for the interface which 
         controls this logical interface.  This is the ifIndex  
         of the controlling network layer interface, and 
         is a direct manifestation of the mapping between 
         a logical interface and the real ifIndex."
		characteristics "-N vsiLogicalControlIfIndex -O 1.3.6.1.4.1.9.9.162.1.3.1.27 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiLogicalIfSessionIndex
		description 	"All logical interfaces are in the domain of a VSI slave, 
         which communicates with the VSI master through a Session. 
         This object is a selector for an entry in the table 
         vsiSessionTable.  It is the VSI session index for the VSI  
         session which controls this logical interface."
		characteristics "-N vsiLogicalIfSessionIndex -O 1.3.6.1.4.1.9.9.162.1.3.1.28 -T INTEGER -A RO -X equal -F 0"

	)

	table	vsiXCTable
	description "A table containing information about cross-connects 
         on the slave switch controlled via VSI.  cross-connects 
         represent virtual links across two interfaces.  The 
         participating interfaces that support these links are  
         represented in this MIB by the vsiLogicalIfTable entries."
	characteristics "-K FOUR"
	(
		readonly int	vsiXCControllerIndex
		description 	"The  index of the VSI controller through which this 
         cross-connect was instantiated.  This object is the index for  
         a vsiControllerTable entry;  the controller indexed is 
         the VSI protocol partner to the slave that is controlling 
         the logical interfaces on which this cross-connect is 
         supported."
		characteristics "-N vsiXCControllerIndex -O 1.3.6.1.4.1.9.9.162.1.4.1.1 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiXCLogicalIfLow
		description 	"The VSI logical interface index for the first endpoint 
         of the cross-connect.  This object is the selector index 
         for one of the two vsiLogicalIfTable entries that support this 
         cross-connect.  Further, this index value is constrained  
         to be 'numerically lower than' the index value of the other  
         vsiLogicalIfTable entry, indexed by vsiXCLogicalIfHi, 
         supporting this cross-connect."
		characteristics "-N vsiXCLogicalIfLow -O 1.3.6.1.4.1.9.9.162.1.4.1.2 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiXCLogicalIfHi
		description 	"The VSI logical interface index for the second endpoint 
         of the cross-connect.  This object is the selector index 
         for one of the two vsiLogicalIfTable entries that support this 
         cross-connect.  Further, this index value is constrained  
         to be 'numerically higher than' the index value of the other  
         vsiLogicalIfTable entry, indexed by vsiXCLogicalIfLow, 
         supporting this cross-connect."
		characteristics "-N vsiXCLogicalIfHi -O 1.3.6.1.4.1.9.9.162.1.4.1.3 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiXCIndex
		description 	"A unique value greater than zero, for each cross-connect 
         within the set of VsiXCTable entries indexed by 
         vsiXCLogicalIfLow and vsiXCLogicalIfHi. 
         The value should remain constant after a cross-connect 
         is initialized.  These values are assigned in sequence 
         by the management agent, starting from one (1)."
		characteristics "-N vsiXCIndex -O 1.3.6.1.4.1.9.9.162.1.4.1.4 -T INTEGER -A RO -X equal -F 0"

		readonly enum vsiXCStateE	vsiXCState
		description 	"The operational state of the cross-connect, as reported 
         to the VSI master by the VSI slave.   
         deleted(1): the connection does not exist. 
         reserved(2): connection is in the reserved state. 
         committed(3): connection is in the committed state. 
         reservedFail(4): connection is reserved but has an internal  
                          failure."
		characteristics "-N vsiXCState -O 1.3.6.1.4.1.9.9.162.1.4.1.5 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiXCVpiLow
		description 	"The vpi of that endpoint of the cross-connect which 
         terminates on the logical interface whose index is 
         given by vsiXCLogicalIfLow."
		characteristics "-N vsiXCVpiLow -O 1.3.6.1.4.1.9.9.162.1.4.1.6 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiXCVciLow
		description 	"The vci of that endpoint of the cross-connect which 
         terminates on the logical interface whose index is 
         given by vsiXCLogicalIfLow."
		characteristics "-N vsiXCVciLow -O 1.3.6.1.4.1.9.9.162.1.4.1.7 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiXCVpiHi
		description 	"The vpi of that endpoint of the cross-connect which 
         terminates on the logical interface whose index is 
         given by vsiXCLogicalIfHi."
		characteristics "-N vsiXCVpiHi -O 1.3.6.1.4.1.9.9.162.1.4.1.8 -T INTEGER -A RO -X equal -F 0"

		readonly int	vsiXCVciHi
		description 	"The vci of that endpoint of the cross-connect which 
         terminates on the logical interface whose index is 
         given by vsiXCLogicalIfHi."
		characteristics "-N vsiXCVciHi -O 1.3.6.1.4.1.9.9.162.1.4.1.9 -T INTEGER -A RO -X equal -F 0"

	)

agenterrors (
	1	"cannot dispatch request"
	2	"select(2) failed"
	3	"sendto(2) failed"
	4	"recvfrom(2) failed"
	5	"no response from system"
	6	"response too big"
	7	"missing attribute"
	8	"bad attribute type"
	9	"cannot get sysUpTime"
	10	"sysUpTime type bad"
	11	"unknown SNMP error"
	12	"bad variable value"
	13	"variable is read only"
	14	"general error"
	15	"cannot make request PDU"
	16	"cannot make request varbind list"
	17	"cannot parse response PDU"
	18	"request ID - response ID mismatch"
	19	"string contains non-displayable characters"
	20	"cannot open schema file"
	21	"cannot parse schema file"
	22	"cannot open host file"
	23	"cannot parse host file"
	24	"attribute unavailable for set operations"
             )
)


## End of Agent Definitions
