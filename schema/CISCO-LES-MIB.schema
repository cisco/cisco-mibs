## THIS FILE WAS GENERATED BY MIB2SCHEMA
## For attributes containing printable ASCII
## characters, you may add a format string
## specification in the attribute characteristics
## line
## As described in RFC 1157, some agents cannot
## accept messages whose length exceeds 484 octets
## For those groups whose var-bind list has many
## entries, you may split them up into different
## var-bind lists
proxy CISCO-LES-MIB
description "CISCO-LES-MIB agent"
serial 2
rpcid 100122
(

## Enumerated Type Definitions

	enum	lesLanTypeE (
	   1	"dot3"
	   2	"dot5"
	)

	enum	lesMaxFrmE (
	   1516	"dot3"
	   4544	"tr4Mb"
	   9234	"rfc1626"
	   18190	"tr16Mb"
	)

	enum	lesOperStatusE (
	   1	"active"
	   2	"inactive"
	)

	enum	lesAdminStatusE (
	   1	"active"
	   2	"inactive"
	)

	enum	lesStatusE (
	   1	"active"
	   2	"notInService"
	   3	"notReady"
	   4	"createAndGo"
	   5	"createAndWait"
	   6	"destroy"
	)

	enum	lesClientStateE (
	   1	"connected"
	   2	"joinRecv"
	   3	"verify"
	   4	"addLec"
	   5	"busConnect"
	   6	"operational"
	   7	"terminating"
	)

	enum	lesClientStatusE (
	   1	"active"
	   2	"notInService"
	   3	"notReady"
	   4	"createAndGo"
	   5	"createAndWait"
	   6	"destroy"
	)


## Group and Table Declarations


## Group and Table Declarations

	table	lesTable
	description " A (conceptual) table containing all instances of LESs
        on this device."
	characteristics "-K TWO"
	(
		notaccessible octet[32]	lesElanName
		description 	" The name associated with the emulated LAN which this
        LES services."
		characteristics "-N lesElanName -O 1.3.6.1.4.1.9.9.39.1.1.1.1.1 -T STRING -A NA -X equal -F 0"

		notaccessible int	lesIndex
		description 	" A  unique  identifier for   this LES servicing  this
        ELAN.  This value must remain constant while the entry
        is in existence and during the  operation of the agent
        (i.e.   It is allowed to  change  after reboot).  For 
        implementations  that do not allow  more  than one LES
        entity per ELAN per device, it is sufficient to always
        report 1 for  this object and  to only accept one  for
        row creation."
		characteristics "-N lesIndex -O 1.3.6.1.4.1.9.9.39.1.1.1.1.2 -T INTEGER -A NA -X equal -F 0"

		readwrite octet[128]	lesAtmAddrSpec
		description 	" The  ATM address of the   designated LES.  Note that
        setting this object  will  have  the side  effect   of
        reinitializing  the  LES and consequently dropping all
        clients connected  to  it.   If  this  object is   not
        specified    at row creation  time  then  the LES will
        choose a value for itself and attempt to register with
        the  switch    with  that value.   If    an address is
        specified which is  not acceptable to the  switch then
        the  lesOperStatus  will   remain  inactive  until  an
        acceptable address is set."
		characteristics "-N lesAtmAddrSpec -O 1.3.6.1.4.1.9.9.39.1.1.1.1.3 -T STRING -A RW -X equal -F 0"

		readwrite octet[20]	lesAtmAddrMask
		description 	"  An  address    mask   that  when  used   with   the
        lesAtmAddrSpec  specifies the portion  of the LES' ATM
        address that is desired  to be configured.  Note  that
        setting   this    object   will     result     in  the
        reinitialization of  the LES,consequently dropping all
        clients connected to it.  Note that this object cannot
        be set at row  creation time without also  supplying a
        value for the  lesAtmAddrSpec object.  If the LES  ATM
        address  is omitted at creation  time then this object
        defaults  to  the  value of   all  zeros (none  of the
        lesAtmAddrSpec value   is relevant). If  the  LES  ATM
        address is specified at  creation time but this object
        omitted, then this object defaults to the value of all
        ones (all of the lesAtmAddrSpec is relevant)."
		characteristics "-N lesAtmAddrMask -O 1.3.6.1.4.1.9.9.39.1.1.1.1.4 -T STRING -A RW -X equal -F 0"

		readonly octet[128]	lesAtmAddrActual
		description 	" The resultant ATM address  in use by  the LES.  This
        object is a product of the specified ATM address, mask
        and interaction with the switch via the ILMI."
		characteristics "-N lesAtmAddrActual -O 1.3.6.1.4.1.9.9.39.1.1.1.1.5 -T STRING -A RO -X equal -F 0"

		readwrite int	lesIfIndex
		description 	"  The interface that a LES will receive control
                      connections on. The value specified for this object
                      must equate to a value of ifIndex in the ifTable.  The
                      interface  will be for that particular ATM port (the
                      physical interface, not a logical layer on top of
                      it). This object can only be specified upon row
                      creation and cannot be altered there after."
		characteristics "-N lesIfIndex -O 1.3.6.1.4.1.9.9.39.1.1.1.1.6 -T INTEGER -A RW -X equal -F 0"

		readwrite int	lesSubIfNum
		description 	" Specifies   the  sub-interface number that  the  LES
        will reside on.  This sub-interface may be shared with
        a LEC or  BUS of the same ELAN  but is not required to
        do  so.  If no value  is  specified for this object at
        row creation time, then a sub-interface will be chosen
        by  the agent. This   object may only be  specified at
        row creation time and cannot be altered thereafter."
		characteristics "-N lesSubIfNum -O 1.3.6.1.4.1.9.9.39.1.1.1.1.7 -T INTEGER -A RW -X equal -F 0"

		readwrite octet[128]	lesColocBusAtmAddrSpec
		description 	" For  those   implementations who  do  not  allow the
        separate creation/deletion of   the LES and   BUS this
        object allows the operator  to specify the ATM address
        of the BUS at  LES creation time.  For implementations
        that allow separate  process  creation this object  is
        optional.   Otherwise this is  the  ATM address of the
        designated BUS.   Note that setting  this object  will
        have the side  effect  of reinitializing the  BUS  and
        consequently dropping all clients connected to it.  If
        this object is not specified at row creation time then
        the BUS will choose a value for  itself and attempt to
        register  with the  switch  with  that value.  If   an
        address is  specified which is   not acceptable to the
        switch  then the  busOperStatus will  remain  inactive
        until an acceptable address is set."
		characteristics "-N lesColocBusAtmAddrSpec -O 1.3.6.1.4.1.9.9.39.1.1.1.1.8 -T STRING -A RW -X equal -F 0"

		readwrite octet[20]	lesColocBusAtmAddrMask
		description 	"  An   address   mask   that  when   used  with   the
        lesColocatedBusAtmAddr specifies  the portion  of  the
        BUS'  ATM address desired   by management.   Note that
        setting this  object  will  have the  side  effect  of
        reinitializing  the BUS and  consequently dropping all
        clients connected to it.  Note that this object cannot
        be set  at row creation  time without also supplying a
        value   for the lesColocBusAtmAddrSpec  object. If the
        BUS ATM address is omitted  at creation time then this
        object defaults to the value of all zeros (none of the
        lesColocBusAtmAddrSpec  value is relevant). If the BUS
        ATM  address is specified   at creation time but  this
        object omitted, then this object defaults to the value
        of  all ones  (all  of the   lesColocBusAtmAddrSpec is
        relevant)."
		characteristics "-N lesColocBusAtmAddrMask -O 1.3.6.1.4.1.9.9.39.1.1.1.1.9 -T STRING -A RW -X equal -F 0"

		readonly octet[128]	lesColocBusAtmAddrActl
		description 	" The ATM address in use by the BUS.  This object is a
        product of  the specified  ATM  address, its  mask and
        interaction with the switch through the ILMI."
		characteristics "-N lesColocBusAtmAddrActl -O 1.3.6.1.4.1.9.9.39.1.1.1.1.10 -T STRING -A RO -X equal -F 0"

		readonly timeticks	lesUpTime
		description 	"   The  value of   sysUpTime  when  this   LES became
        activated."
		characteristics "-N lesUpTime -O 1.3.6.1.4.1.9.9.39.1.1.1.1.11 -T TIMETICKS -A RO -X equal -F 0"

		readwrite enum lesLanTypeE	lesLanType
		description 	" The type of legacy LAN that this LES emulates."
		characteristics "-N lesLanType -O 1.3.6.1.4.1.9.9.39.1.1.1.1.12 -T INTEGER -A RW -X equal -F 0"

		readwrite enum lesMaxFrmE	lesMaxFrm
		description 	" The maximum frame  size that the specified  LAN type
        can accept."
		characteristics "-N lesMaxFrm -O 1.3.6.1.4.1.9.9.39.1.1.1.1.13 -T INTEGER -A RW -X equal -F 0"

		readwrite int	lesJoinTimeout
		description 	" The number  of seconds  that must  elapse before a  
        Join request can be timed-out."
		characteristics "-N lesJoinTimeout -O 1.3.6.1.4.1.9.9.39.1.1.1.1.14 -T INTEGER -A RW -X equal -F 0"

		readwrite octet[128]	lesLecsAtmAddr
		description 	" The  ATM address  associated with  the LECS  used by
        this LES. If no  LECS is in use  by the LES  then this
        object will have the value of all 0's (i.e 0x0000...).
        If it is  desired that the LES get  the address of the
        LECS  through the ILMI  then this  object may be  left
        unspecified at row activation.  If the ILMI fails then
        the lesOperStatus will  be inactive(2) and this column
        still not present."
		characteristics "-N lesLecsAtmAddr -O 1.3.6.1.4.1.9.9.39.1.1.1.1.15 -T STRING -A RW -X equal -F 0"

		readonly int	lesControlDistVpi
		description 	"  The   virtual  path  identifier   of   the  control
        distribute VCC to all clients, if it exits. "
		characteristics "-N lesControlDistVpi -O 1.3.6.1.4.1.9.9.39.1.1.1.1.16 -T INTEGER -A RO -X equal -F 0"

		readonly int	lesControlDistVci
		description 	" The  virtual  channel  identifier   of  the  control
        distribute VCC to all clients, if it exists. "
		characteristics "-N lesControlDistVci -O 1.3.6.1.4.1.9.9.39.1.1.1.1.17 -T INTEGER -A RO -X equal -F 0"

		readonly enum lesOperStatusE	lesOperStatus
		description 	" This   object reflects the  actual state  of the LES
        which may   differ from   that of   the lesAdminStatus
        object.    This   can   occur    when    the interface
        ifOperStatus  is     'down'  but  the    corresponding
        lesAdminStatus is 'active'."
		characteristics "-N lesOperStatus -O 1.3.6.1.4.1.9.9.39.1.1.1.1.18 -T INTEGER -A RO -X equal -F 0"

		readwrite enum lesAdminStatusE	lesAdminStatus
		description 	"  The  desired  state   of  the  designated   LES  as
        prescribed by the operator. The  actions of the  agent
        will, if at  all  possible, eventually  result  in the
        desired state being  reflected in  the lesOperStatus."
		characteristics "-N lesAdminStatus -O 1.3.6.1.4.1.9.9.39.1.1.1.1.19 -T INTEGER -A RW -X equal -F 0"

		readwrite enum lesStatusE	lesStatus
		description 	" The status of this entry (conceptual row).  This row
        cannot be set to active  until an appropiate value  of
        lesIfIndex has been specified. "
		characteristics "-N lesStatus -O 1.3.6.1.4.1.9.9.39.1.1.1.1.20 -T INTEGER -A RW -X equal -F 0"

		readwrite int	lesSegmentID
		description 	" The segment ID of the emulated LAN this LES is
                      servicing.  This object is only valid if the
                      lesLanType is dot5(2)."
		characteristics "-N lesSegmentID -O 1.3.6.1.4.1.9.9.39.1.1.1.1.21 -T INTEGER -A RW -X equal -F 0"

	)

	table	lesStatsTable
	description " A (conceptual) table of statistics associated with
        LES instances on the device."
	characteristics "-K ???"
	(
		readonly counter	lesInErrors
		description 	" The number of frames  received by the LES which were
        either malformed  or did  not follow standard protocol
        (i.e. receiving   a  LE-ARP request   for a  multicast
        address)."
		characteristics "-N lesInErrors -O 1.3.6.1.4.1.9.9.39.1.1.2.1.1 -T COUNTER -A RO -X equal -F 0"

		readonly octet[128]	lesInErrorLastLec
		description 	" The ATM address of the LE client whose last frame to
        the LES resulted in  an increment  to the  lesInErrors
        counter."
		characteristics "-N lesInErrorLastLec -O 1.3.6.1.4.1.9.9.39.1.1.2.1.2 -T STRING -A RO -X equal -F 0"

		readonly counter	lesInFlushReplies
		description 	" The number of Flush  replies that have been received
        by the LES and forwarded onto its clients."
		characteristics "-N lesInFlushReplies -O 1.3.6.1.4.1.9.9.39.1.1.2.1.3 -T COUNTER -A RO -X equal -F 0"

		readonly counter	lesInJoinReqs
		description 	" The number  of LE_JOIN_REQUESTs received by this LES
        since activation."
		characteristics "-N lesInJoinReqs -O 1.3.6.1.4.1.9.9.39.1.1.2.1.4 -T COUNTER -A RO -X equal -F 0"

		readonly counter	lesOutJoinFails
		description 	"    The  number  of   rejection     LE_JOIN_RESPONSEs
        transmitted by this LES since activation."
		characteristics "-N lesOutJoinFails -O 1.3.6.1.4.1.9.9.39.1.1.2.1.5 -T COUNTER -A RO -X equal -F 0"

		readonly int	lesJoinLastFailCause
		description 	" The cause for the transmission of the last rejection
        LE_JOIN_RESPONSE.  This   is the value  of the  status
        field  within the rejection  response.   If the server
        has never issued a rejection response then this object
        will have the value of 0."
		characteristics "-N lesJoinLastFailCause -O 1.3.6.1.4.1.9.9.39.1.1.2.1.6 -T INTEGER -A RO -X equal -F 0"

		readonly octet[128]	lesJoinLastFailLec
		description 	"   The  ATM  address   of   the  client   whose  last
        LE_JOIN_REQUEST resulted  in a   rejection  by    this
        server.  If the  server  has never issued a  rejection
        response then this object  will have the value of  all
        0's (i.e. 0x000...)."
		characteristics "-N lesJoinLastFailLec -O 1.3.6.1.4.1.9.9.39.1.1.2.1.7 -T STRING -A RO -X equal -F 0"

		readonly counter	lesOutConfigReqs
		description 	" The number of LE_CONFIGURE_REQUESTs sent by this LES
        since activation."
		characteristics "-N lesOutConfigReqs -O 1.3.6.1.4.1.9.9.39.1.1.2.1.8 -T COUNTER -A RO -X equal -F 0"

		readonly counter	lesInConfigResps
		description 	"  The  number  of successful   LE_CONFIGURE_RESPONSEs
        received by this LES since activation."
		characteristics "-N lesInConfigResps -O 1.3.6.1.4.1.9.9.39.1.1.2.1.9 -T COUNTER -A RO -X equal -F 0"

		readonly counter	lesInConfigFails
		description 	"   The   number of   rejection LE_CONFIGURE_RESPONSEs
        received by this LES since activation."
		characteristics "-N lesInConfigFails -O 1.3.6.1.4.1.9.9.39.1.1.2.1.10 -T COUNTER -A RO -X equal -F 0"

		readonly counter	lesInRegisReqs
		description 	" The  number of LE_REGISTER_REQUESTs received by this
        LES since activation."
		characteristics "-N lesInRegisReqs -O 1.3.6.1.4.1.9.9.39.1.1.2.1.11 -T COUNTER -A RO -X equal -F 0"

		readonly counter	lesOutRegisFails
		description 	"  The   number of  rejection    LE_REGISTER_RESPONSEs
        transmitted by this LES since activation."
		characteristics "-N lesOutRegisFails -O 1.3.6.1.4.1.9.9.39.1.1.2.1.12 -T COUNTER -A RO -X equal -F 0"

		readonly int	lesRegisLastFailCause
		description 	" The cause for the transmission of the last rejection
        LE_REGISTER_RESPONSE.  This  variable has the value of
        the status field  within  the  last  failure  register
        response.  If the  server  has never   issued a failed
        registration response then   the value of this  object
        shall be 0."
		characteristics "-N lesRegisLastFailCause -O 1.3.6.1.4.1.9.9.39.1.1.2.1.13 -T INTEGER -A RO -X equal -F 0"

		readonly octet[128]	lesRegisLastFailLec
		description 	"   The   ATM  address  of    the  client whose   last
        LE_REGISTER_REQUEST to  this LES resulted  in failure.
        If the server  has never issued a  failed registration
        response  then the value  of this  object shall be all
        0's (i.e. x0000...)."
		characteristics "-N lesRegisLastFailLec -O 1.3.6.1.4.1.9.9.39.1.1.2.1.14 -T STRING -A RO -X equal -F 0"

		readonly counter	lesInUnregReqs
		description 	" The number  of valid LE_UNREGISTER_REQUESTs received
        by this LES since activation."
		characteristics "-N lesInUnregReqs -O 1.3.6.1.4.1.9.9.39.1.1.2.1.15 -T COUNTER -A RO -X equal -F 0"

		readonly counter	lesInLearpUcasts
		description 	"   The number  of  LE_ARP_REQUESTs  received  by this
        LES  since   activation and  responded   to  directly.
        This applies   to all   LE_ARP requests for  addresses
        registered by  LE clients with  the LES.  Note  that a
        LES is not required to answer LE_ARP requests directly
        and  may  forward  the   request  on  to the   control
        distribute regardless. This counter does not apply to
        those  requests the    BUS (the broadcast   address)."
		characteristics "-N lesInLearpUcasts -O 1.3.6.1.4.1.9.9.39.1.1.2.1.16 -T COUNTER -A RO -X equal -F 0"

		readonly counter	lesInLearpBroadcasts
		description 	" The number of LE_ARP_REQUEST received by the LES for
        the broadcast address.  These requests are responded
        to with the BUS' ATM address."
		characteristics "-N lesInLearpBroadcasts -O 1.3.6.1.4.1.9.9.39.1.1.2.1.17 -T COUNTER -A RO -X equal -F 0"

		readonly counter	lesOutLearpFwd
		description 	" The number of LE_ARP_REQUEST frames forwarded onto
        the control distribute VC."
		characteristics "-N lesOutLearpFwd -O 1.3.6.1.4.1.9.9.39.1.1.2.1.18 -T COUNTER -A RO -X equal -F 0"

		readonly counter	lesInLearpResps
		description 	" The number of LE_ARP_RESPONSEs received by this LES
        and forwarded onto the control distribute. "
		characteristics "-N lesInLearpResps -O 1.3.6.1.4.1.9.9.39.1.1.2.1.19 -T COUNTER -A RO -X equal -F 0"

		readonly counter	lesInNarpReqs
		description 	" The number of  Negative LE_ARP requests  received by
        this LES and  forwarded  on to the control  distribute
        VC."
		characteristics "-N lesInNarpReqs -O 1.3.6.1.4.1.9.9.39.1.1.2.1.20 -T COUNTER -A RO -X equal -F 0"

		readonly counter	lesInTopolReqs
		description 	" The number of  LE_TOPOLOGY_REQUESTs received by this
        LES since activation."
		characteristics "-N lesInTopolReqs -O 1.3.6.1.4.1.9.9.39.1.1.2.1.21 -T COUNTER -A RO -X equal -F 0"

	)

	table	lesClientTable
	description " A (conceptual) table listing the clients associated
        with a LES.  Note that the local or network management
        will not create or destroy rows in this table but
        may only inspect what LECs are currently joined
        with the LES."
	characteristics "-K ONE"
	(
		notaccessible int	lesClientLecid
		description 	" The LECID selected for an LEC by the LES during the JOIN
        phase."
		characteristics "-N lesClientLecid -O 1.3.6.1.4.1.9.9.39.1.2.1.1.1 -T INTEGER -A NA -X equal -F 0"

		readonly octet[128]	lesClientAtmAddr
		description 	" The primary ATM address associated with the designated LEC."
		characteristics "-N lesClientAtmAddr -O 1.3.6.1.4.1.9.9.39.1.2.1.1.2 -T STRING -A RO -X equal -F 0"

		readonly enum lesClientStateE	lesClientState
		description 	" The current state of the LEC.  Note that busConnect is only a
        monitorable state if the BUS is colocated with the LES.  In the
        event that it is not then the client state should proceed 
        immediately to operational within this table (though it may not
        do so in actuality). Note that there is no relation between the
        client's internal state and the state reflected here.  The 
        state here is only the LES's observed state of the LEC."
		characteristics "-N lesClientState -O 1.3.6.1.4.1.9.9.39.1.2.1.1.3 -T INTEGER -A RO -X equal -F 0"

		readonly int	lesClientIfIndex
		description 	" The interface of the bi-directional control direct connection
        between the LEC and LES."
		characteristics "-N lesClientIfIndex -O 1.3.6.1.4.1.9.9.39.1.2.1.1.4 -T INTEGER -A RO -X equal -F 0"

		readonly int	lesClientControlVpi
		description 	" The VPI of the bi-directional control direct connection 
        between the LEC and LES."
		characteristics "-N lesClientControlVpi -O 1.3.6.1.4.1.9.9.39.1.2.1.1.5 -T INTEGER -A RO -X equal -F 0"

		readonly int	lesClientControlVci
		description 	" The VCI of the bi-directional control direct connection 
        between the LEC and LES."
		characteristics "-N lesClientControlVci -O 1.3.6.1.4.1.9.9.39.1.2.1.1.6 -T INTEGER -A RO -X equal -F 0"

		readwrite enum lesClientStatusE	lesClientStatus
		description 	" The status of the (conceptual) row.  Note that this
        object may only return the active(1) state.  The only
        value which may be written to this object is the
        destroy(6) state which will cause the client to be
        dropped from the ELAN."
		characteristics "-N lesClientStatus -O 1.3.6.1.4.1.9.9.39.1.2.1.1.7 -T INTEGER -A RW -X equal -F 0"

	)

	table	lesMacRegTable
	description " A (conceptual) table of MAC addresses registered with the LES
        by its clients."
	characteristics "-K ???"
	(
		notaccessible octet[6]	lesMacRegMacAddress
		description 	" The MAC address of a registered client."
		characteristics "-N lesMacRegMacAddress -O 1.3.6.1.4.1.9.9.39.1.3.1.1.1 -T STRING -A NA -X equal -F 0"

		readonly octet[128]	lesMacRegAtmAddr
		description 	" The ATM address of the LEC that corresponds to the
        registered MAC address."
		characteristics "-N lesMacRegAtmAddr -O 1.3.6.1.4.1.9.9.39.1.3.1.1.3 -T STRING -A RO -X equal -F 0"

		readonly int	lesMacRegLecid
		description 	"    The  LECID of   the   client  who registered  the
        designated MAC address."
		characteristics "-N lesMacRegLecid -O 1.3.6.1.4.1.9.9.39.1.3.1.1.4 -T INTEGER -A RO -X equal -F 0"

	)

	table	lesRDRegTable
	description " A (conceptual) table of Route Descriptors registered
                      with the LES by its clients."
	characteristics "-K TWO"
	(
		notaccessible int	lesRDRegSegmentId
		description 	" The segment ID of a registered client."
		characteristics "-N lesRDRegSegmentId -O 1.3.6.1.4.1.9.9.39.1.3.2.1.1 -T INTEGER -A NA -X equal -F 0"

		notaccessible int	lesRDRegBridgeNum
		description 	" The bridge number of a registered client going to
              the designated segment."
		characteristics "-N lesRDRegBridgeNum -O 1.3.6.1.4.1.9.9.39.1.3.2.1.2 -T INTEGER -A NA -X equal -F 0"

		readonly octet[128]	lesRDRegAtmAddr
		description 	" The ATM address of the LEC that corresponds to the
        registered Route Descriptor."
		characteristics "-N lesRDRegAtmAddr -O 1.3.6.1.4.1.9.9.39.1.3.2.1.3 -T STRING -A RO -X equal -F 0"

		readonly int	lesRDRegLecid
		description 	"    The  LECID of   the   client  who registered  the
        designated Route Descriptor address."
		characteristics "-N lesRDRegLecid -O 1.3.6.1.4.1.9.9.39.1.3.2.1.4 -T INTEGER -A RO -X equal -F 0"

	)

agenterrors (
	1	"cannot dispatch request"
	2	"select(2) failed"
	3	"sendto(2) failed"
	4	"recvfrom(2) failed"
	5	"no response from system"
	6	"response too big"
	7	"missing attribute"
	8	"bad attribute type"
	9	"cannot get sysUpTime"
	10	"sysUpTime type bad"
	11	"unknown SNMP error"
	12	"bad variable value"
	13	"variable is read only"
	14	"general error"
	15	"cannot make request PDU"
	16	"cannot make request varbind list"
	17	"cannot parse response PDU"
	18	"request ID - response ID mismatch"
	19	"string contains non-displayable characters"
	20	"cannot open schema file"
	21	"cannot parse schema file"
	22	"cannot open host file"
	23	"cannot parse host file"
	24	"attribute unavailable for set operations"
             )
)


## End of Agent Definitions
