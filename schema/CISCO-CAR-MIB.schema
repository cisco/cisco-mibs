## THIS FILE WAS GENERATED BY MIB2SCHEMA
## For attributes containing printable ASCII
## characters, you may add a format string
## specification in the attribute characteristics
## line
## As described in RFC 1157, some agents cannot
## accept messages whose length exceeds 484 octets
## For those groups whose var-bind list has many
## entries, you may split them up into different
## var-bind lists
proxy CISCO-CAR-MIB
description "CISCO-CAR-MIB agent"
serial 2
rpcid 100122
(

## Enumerated Type Definitions

	enum	ccarConfigDirectionE (
	   1	"input"
	   2	"output"
	)

	enum	ccarConfigTypeE (
	   1	"all"
	   2	"quickAcc"
	   3	"standardAcc"
	)

	enum	ccarConfigConformActionE (
	   1	"drop"
	   2	"xmit"
	   3	"continue"
	   4	"precedXmit"
	   5	"precedCont"
	)

	enum	ccarConfigExceedActionE (
	   1	"drop"
	   2	"xmit"
	   3	"continue"
	   4	"precedXmit"
	   5	"precedCont"
	)


## Group and Table Declarations

	table	ccarConfigTable
	description "A table of rate limit configuration entries.
        Rate Limit is a method of traffic control.  It allows a set
        of rate limits to be configured and applied to packets flowing
        into/out of an interface to regulate network traffic."
	characteristics "-K TWO"
	(
		notaccessible enum ccarConfigDirectionE	ccarConfigDirection
		description 	"The data source for the Rate Limit object."
		characteristics "-N ccarConfigDirection -O 1.3.6.1.4.1.9.9.113.1.1.1.1.1 -T INTEGER -A NA -X equal -F 0"

		notaccessible int	ccarConfigRowIndex
		description 	"An arbitrary index for rate limit objects.
        It will increase as the list is traversed,
        but may skip."
		characteristics "-N ccarConfigRowIndex -O 1.3.6.1.4.1.9.9.113.1.1.1.1.2 -T INTEGER -A NA -X equal -F 0"

		readonly enum ccarConfigTypeE	ccarConfigType
		description 	"The type of traffic rate-limited against."
		characteristics "-N ccarConfigType -O 1.3.6.1.4.1.9.9.113.1.1.1.1.3 -T INTEGER -A RO -X equal -F 0"

		readonly int	ccarConfigAccIdx
		description 	"The index to the access list if RateLimitType is either
        quickAcc or standardAcc."
		characteristics "-N ccarConfigAccIdx -O 1.3.6.1.4.1.9.9.113.1.1.1.1.4 -T INTEGER -A RO -X equal -F 0"

		readonly int	ccarConfigRate
		description 	"The comitted access rate.  This is the sustained
        rate permitted by the rate limit."
		characteristics "-N ccarConfigRate -O 1.3.6.1.4.1.9.9.113.1.1.1.1.5 -T INTEGER -A RO -X equal -F 0"

		readonly int	ccarConfigLimit
		description 	"The rate limit.  The amout of traffic, in bytes,
        in excess of the committed access rate which will be
        instantaneously permitted by the rate limit."
		characteristics "-N ccarConfigLimit -O 1.3.6.1.4.1.9.9.113.1.1.1.1.6 -T INTEGER -A RO -X equal -F 0"

		readonly int	ccarConfigExtLimit
		description 	"The extended burst limit. The amount of traffic,
        in bytes, in excess of the burst limit which may be
        conditionnally permitted by the rate limit.
        The probability that the traffic is not permitted
        increases as the received burst increases.
        
        P(not permitted) =
            (BurstRate - ConfLimit) / (ConfLimitExt - ConfLimit)."
		characteristics "-N ccarConfigExtLimit -O 1.3.6.1.4.1.9.9.113.1.1.1.1.7 -T INTEGER -A RO -X equal -F 0"

		readonly enum ccarConfigConformActionE	ccarConfigConformAction
		description 	"Action to be taken when the traffic is within
        the Rate Limit."
		characteristics "-N ccarConfigConformAction -O 1.3.6.1.4.1.9.9.113.1.1.1.1.8 -T INTEGER -A RO -X equal -F 0"

		readonly enum ccarConfigExceedActionE	ccarConfigExceedAction
		description 	"Action to be taken when the traffic exceeds
        the Rate Limit."
		characteristics "-N ccarConfigExceedAction -O 1.3.6.1.4.1.9.9.113.1.1.1.1.9 -T INTEGER -A RO -X equal -F 0"

	)

	table	ccarStatTable
	description "A table of rate limit status entries."
	characteristics "-K ???"
	(
		readonly counter	ccarStatSwitchedPkts
		description 	"The counter of packets permitted by this rate limit."
		characteristics "-N ccarStatSwitchedPkts -O 1.3.6.1.4.1.9.9.113.1.2.1.1.1 -T COUNTER -A RO -X equal -F 0"

		readonly counter	ccarStatSwitchedBytes
		description 	"The counter of bytes permitted by this interface."
		characteristics "-N ccarStatSwitchedBytes -O 1.3.6.1.4.1.9.9.113.1.2.1.1.2 -T COUNTER -A RO -X equal -F 0"

		readonly counter	ccarStatFilteredPkts
		description 	"The counter of packets which exceeded this rate limit."
		characteristics "-N ccarStatFilteredPkts -O 1.3.6.1.4.1.9.9.113.1.2.1.1.3 -T COUNTER -A RO -X equal -F 0"

		readonly counter	ccarStatFilteredBytes
		description 	"The counter of bytes which exceeded this rate limit."
		characteristics "-N ccarStatFilteredBytes -O 1.3.6.1.4.1.9.9.113.1.2.1.1.4 -T COUNTER -A RO -X equal -F 0"

		readonly gauge	ccarStatCurBurst
		description 	"The current received burst size."
		characteristics "-N ccarStatCurBurst -O 1.3.6.1.4.1.9.9.113.1.2.1.1.5 -T GAUGE -A RO -X equal -F 0"

		readonly counter	ccarStatSwitchedPktsOverflow
		description 	"The high-word of the counter of packets permitted 
         by this rate limit.
        
           NOTE:  The object ccarStatSwitchedPkts contains the
           lower 32 bits of the switched packet counts.
           If the number of switched packets is larger
           than what can be stored in a 32 bit value, then
           the high word value is stored in this object.
           As such SNMP Manager applications should 
           combine this object along with the 
           ccarStatSwitchedPkts object to come up with the
           64-bit value.
        
           SNMP v2c or v3 Manager can use the 
           ccarStatHCSwitchedPkts object directly 
           (which is a 64-bit object)."
		characteristics "-N ccarStatSwitchedPktsOverflow -O 1.3.6.1.4.1.9.9.113.1.2.1.1.6 -T COUNTER -A RO -X equal -F 0"

		readonly counter	ccarStatSwitchedBytesOverflow
		description 	"The high-word of the counter of bytes permitted by 
         this interface.
        
           NOTE:  The object ccarStatSwitchedBytes contains the
           lower 32 bits of the switched byte counts.
           If the number of switched bytes is larger
           than what can be stored in a 32 bit value, then
           the high word value is stored in this object.
           As such SNMP Manager applications should 
           combine this object along with the 
           ccarStatSwitchedBytes object to come up with the
           64-bit value.
        
           SNMP v2c or v3 Manager can use the 
           ccarStatHCSwitchedBytes object directly 
           (which is a 64-bit object)."
		characteristics "-N ccarStatSwitchedBytesOverflow -O 1.3.6.1.4.1.9.9.113.1.2.1.1.7 -T COUNTER -A RO -X equal -F 0"

		readonly counter	ccarStatFilteredPktsOverflow
		description 	"The high-word of the counter of packets which exceeded 
         this rate limit.
        
           NOTE:  The object ccarStatFilteredPkts contains the
           lower 32 bits of the filtered packet counts.
           If the number of filtered packets is larger
           than what can be stored in a 32 bit value, then
           the high word value is stored in this object.
           As such SNMP Manager applications should combine 
           this object along with the ccarStatFilteredPkts 
           object to come up with the 64-bit value.
        
           SNMP v2c or v3 Manager can use the 
           ccarStatHCFilteredPkts object directly 
           (which is a 64-bit object)."
		characteristics "-N ccarStatFilteredPktsOverflow -O 1.3.6.1.4.1.9.9.113.1.2.1.1.8 -T COUNTER -A RO -X equal -F 0"

		readonly counter	ccarStatFilteredBytesOverflow
		description 	"The high-word of the counter of bytes which exceeded 
         this rate limit.
        
           NOTE:  The object ccarStatFilteredBytes contains the
           lower 32 bits of the filtered byte counts.
           If the number of filtered bytes is larger
           than what can be stored in a 32 bit value, then
           the high word value is stored in this object.
           As such SNMP Manager applications should combine 
           this object along with the ccarStatFilteredBytes 
           object to come up with the 64-bit value.
        
           SNMP v2c or v3 Manager can use the 
           ccarStatHCFilteredBytes object directly 
           (which is a 64-bit object)."
		characteristics "-N ccarStatFilteredBytesOverflow -O 1.3.6.1.4.1.9.9.113.1.2.1.1.9 -T COUNTER -A RO -X equal -F 0"

		readonly counter	ccarStatHCSwitchedPkts
		description 	"The counter of packets permitted by this rate limit.
        
           NOTE:  This is a 64 bit (High Capacity)
           version of the ccarStatSwitchedPkts
           counter for use with SNMP v2c or v3 Managers"
		characteristics "-N ccarStatHCSwitchedPkts -O 1.3.6.1.4.1.9.9.113.1.2.1.1.10 -T COUNTER -A RO -X equal -F 0"

		readonly counter	ccarStatHCSwitchedBytes
		description 	"The counter of bytes permitted by this interface.
        
           NOTE:  This is a 64 bit (High Capacity)
           version of the ccarStatSwitchedBytes
           counter for use with SNMP v2c or v3 Managers"
		characteristics "-N ccarStatHCSwitchedBytes -O 1.3.6.1.4.1.9.9.113.1.2.1.1.11 -T COUNTER -A RO -X equal -F 0"

		readonly counter	ccarStatHCFilteredPkts
		description 	"The counter of packets which exceeded this rate limit.
        
           NOTE:  This is a 64 bit (High Capacity)
           version of the ccarStatFilteredPkts
           counter for use with SNMP v2c or v3 Managers"
		characteristics "-N ccarStatHCFilteredPkts -O 1.3.6.1.4.1.9.9.113.1.2.1.1.12 -T COUNTER -A RO -X equal -F 0"

		readonly counter	ccarStatHCFilteredBytes
		description 	"The counter of bytes which exceeded this rate limit.
        
           NOTE:  This is a 64 bit (High Capacity)
           version of the ccarStatFilteredBytes
           counter for use with SNMP v2c or v3 Managers"
		characteristics "-N ccarStatHCFilteredBytes -O 1.3.6.1.4.1.9.9.113.1.2.1.1.13 -T COUNTER -A RO -X equal -F 0"

	)

agenterrors (
	1	"cannot dispatch request"
	2	"select(2) failed"
	3	"sendto(2) failed"
	4	"recvfrom(2) failed"
	5	"no response from system"
	6	"response too big"
	7	"missing attribute"
	8	"bad attribute type"
	9	"cannot get sysUpTime"
	10	"sysUpTime type bad"
	11	"unknown SNMP error"
	12	"bad variable value"
	13	"variable is read only"
	14	"general error"
	15	"cannot make request PDU"
	16	"cannot make request varbind list"
	17	"cannot parse response PDU"
	18	"request ID - response ID mismatch"
	19	"string contains non-displayable characters"
	20	"cannot open schema file"
	21	"cannot parse schema file"
	22	"cannot open host file"
	23	"cannot parse host file"
	24	"attribute unavailable for set operations"
             )
)


## End of Agent Definitions
