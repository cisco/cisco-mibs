## THIS FILE WAS GENERATED BY MIB2SCHEMA
## For attributes containing printable ASCII
## characters, you may add a format string
## specification in the attribute characteristics
## line
## As described in RFC 1157, some agents cannot
## accept messages whose length exceeds 484 octets
## For those groups whose var-bind list has many
## entries, you may split them up into different
## var-bind lists
proxy CISCO-OUTAGE-MONITOR-MIB
description "CISCO-OUTAGE-MONITOR-MIB agent"
serial 2
rpcid 100122
(

## Enumerated Type Definitions

	enum	cOutageNotificationsEnabledE (
	   1	"true"
	   2	"false"
	)

	enum	cOutageNotificationFilterEnabledE (
	   1	"true"
	   2	"false"
	)

	enum	cOutageEventObjectTypeE (
	   1	"interface"
	   2	"physicalEntity"
	   3	"swProcess"
	   4	"remoteObject"
	   5	"logicalEntity"
	)

	enum	cOutageObjectTypeE (
	   1	"interface"
	   2	"physicalEntity"
	   3	"swProcess"
	   4	"remoteObject"
	   5	"logicalEntity"
	)

	enum	cOutageCurrentStatusE (
	   1	"up"
	   2	"down"
	)

	enum	cOutageRemoteObjIDTypeE (
	   1	"ipv4"
	   2	"ipv6"
	   3	"ipv4z"
	   4	"ipv6z"
	   16	"dns"
	)


## Group and Table Declarations

	group	cOutageBasicInfo
	description "(null)"
	characteristics "-K NONE"
	(
		readonly octet[255]	cOutageApplVersion
		description 	"outage monitoring application version 
        string. 
        
        The format will be: 
        
        'Version.Release.Patch-Level: Textual-Description' 
        
        For example:  '1.0.0: Initial Outage Monitor 
        Application'"
		characteristics "-N cOutageApplVersion -O 1.3.6.1.4.1.9.9.280.1.1.1 -T STRING -A RO -X equal -F 0"

		readonly counter	cOutageNotificationsSent
		description 	"The number of outage notifications that have been  
        sent. This number may include notifications 
        that were prevented from being transmitted due to 
        reasons such as resource limitations and/or 
        non-connectivity.  If one is receiving notifications, 
        one can periodically poll this object to determine if 
        any notifications were missed.  If missed, a poll of  
        the cOutageHistoryTable might be necessary."
		characteristics "-N cOutageNotificationsSent -O 1.3.6.1.4.1.9.9.280.1.1.2 -T COUNTER -A RO -X equal -F 0"

		readwrite enum cOutageNotificationsEnabledE	cOutageNotificationsEnabled
		description 	"This object indicates whether outage notifications  
        will or will not be sent when an outage related event  
        is generated by the device.  Disabling notifications 
        does not prevent outage event from being added 
        to the cOutageHistoryTable."
		characteristics "-N cOutageNotificationsEnabled -O 1.3.6.1.4.1.9.9.280.1.1.3 -T INTEGER -A RW -X equal -F 0"

		readwrite enum cOutageNotificationFilterEnabledE	cOutageNotificationFilterEnabled
		description 	"This object indicates whether certain outage related  
        event notifications will or will not be sent when the  
        cOutageNotificationsEnabled is TRUE. 
        
        In the event structure, it is possible to have  
        certain hierarchies such that an event causes several  
        related events. The original event is called a root  
        event and other related events are called sub-events.        
        
        If it is enabled, only a root event notification will 
        be sent out and all other related sub-events will be  
        filtered out. The cOutageHistoryTable maintains only  
        the root event. However, it does not prevent the object  
        entries from being updated in the cOutageObjectTable. 
        
        The filtering policy, which correlates a root event and  
        the following sub-events, may vary and can be controlled  
        by the configuration."
		characteristics "-N cOutageNotificationFilterEnabled -O 1.3.6.1.4.1.9.9.280.1.1.4 -T INTEGER -A RW -X equal -F 0"

		readonly counter	cOutageFilteredEvents
		description 	"The number of events which were filtered out.  
        This object can be utilized to determine how  
        many following events got filtered out related  
        to the notified event."
		characteristics "-N cOutageFilteredEvents -O 1.3.6.1.4.1.9.9.280.1.1.5 -T COUNTER -A RO -X equal -F 0"

	)

	group	cOutageHistory
	description "(null)"
	characteristics "-K NONE"
	(
		readwrite gauge	cOutageHistTableSize
		description 	"The upper limit on the number of entries that the 
        cOutageHistoryTable may contain."
		characteristics "-N cOutageHistTableSize -O 1.3.6.1.4.1.9.9.280.1.2.1 -T GAUGE -A RW -X equal -F 0"

		readonly counter	cOutageHistMsgsFlushed
		description 	"The number of entries that have been removed from 
        the cOutageHistoryTable in order to make room for new 
        entries.  The value of this object starts at '0' and  
        monotonically increases for each flushed message.  
        If the value of this object is '4294967295', the value  
        will become '0'. This object can be utilized to  
        determine whether your polling frequency on the  
        history table is fast enough and/or the size of your  
        history table is large enough such that you are not  
        missing events."
		characteristics "-N cOutageHistMsgsFlushed -O 1.3.6.1.4.1.9.9.280.1.2.2 -T COUNTER -A RO -X equal -F 0"

	)

	table	cOutageHistoryTable
	description "A table of outage related events generated by this router. 
        Conceptual row entries are dynamically added into this  
        table when outage related events occur. 
        
        Entries are stored in FIFO order.  When the maximum number  
        of entries has been reached in the table, the oldest entry  
        in the table is removed immediately.   
        
        When a table is reduced to a smaller size N, the oldest 
        entries are immediately removed from the table leaving  
        a maximum of N entries."
	characteristics "-K ONE"
	(
		notaccessible gauge	cOutageEventIndex
		description 	"An arbitrary non-zero integer value that uniquely  
        identifies a COutageHistoryEntry in the table.  
        The value of this table starts at '1' and monotonically  
        increases for each object failure and recovery event  
        received by the agent. If the value of this object is  
        '4294967295', the agent will reset it to '1' upon  
        receiving the next event."
		characteristics "-N cOutageEventIndex -O 1.3.6.1.4.1.9.9.280.1.2.3.1.1 -T GAUGE -A NA -X equal -F 0"

		readonly enum cOutageEventObjectTypeE	cOutageEventObjectType
		description 	"This object specifies the monitored object's type.  
        It comprises interface(1), physicalEntity(2),  
        swProcess(3), remoteObject(4), and logicalEntity(5).   
        
        The detailed description has been presented above  
        in the OutageMonObjectType definition."
		characteristics "-N cOutageEventObjectType -O 1.3.6.1.4.1.9.9.280.1.2.3.1.2 -T INTEGER -A RO -X equal -F 0"

		readonly gauge	cOutageEventMonObjectIndex
		description 	"This object identifies a specific index of the  
        monitored object based upon the cOutageEventObjectType.  
        If it is interface(1), it correlates the object  
        with ifIndex in ifTable in the IF-MIB.          
        If it is physicalEntity(2), it correlates the object  
        with entPhysicalIndex in entPhysicalTable in the  
        ENTITY-MIB.  
        If it is swProcess(3), the value corresponds to an entry 
        in the cOutageCpmMapTable, which correlates the object  
        with cpmCPUTotalIndex and cpmProcessPID in 
        cpmProcessTable in the CISCO-PROCESS-MIB.  
        If it is remoteObject(4), the value corresponds to an  
        entry in the cOutageRemoteObjMapTable, which correlates the  
        object with its ID and description.   
        If it is logicalEntity(5), the value corresponds to an  
        entry in the cOutageLogicalObjMapTable, which correlates the  
        object with the description.   
         
        It also correlates the event entry with a specific  
        entry in cOutageObjectTable."
		characteristics "-N cOutageEventMonObjectIndex -O 1.3.6.1.4.1.9.9.280.1.2.3.1.3 -T GAUGE -A RO -X equal -F 0"

		readonly gauge	cOutageEventTypeIndex
		description 	"The type of outage related event. The value  
        corresponds to an entry in the cOutageEventTypeMapTable."
		characteristics "-N cOutageEventTypeIndex -O 1.3.6.1.4.1.9.9.280.1.2.3.1.4 -T GAUGE -A RO -X equal -F 0"

		readonly octet[11]	cOutageEventTime
		description 	"This object specifies the date and time of the   
        event was generated.  
        
        The value of sysUpTime, which is reinitialized during  
        the router boot-up, is not sufficient, since it  
        shall be able to measure the outage time of the  
        router failure over the measurement period."
		characteristics "-N cOutageEventTime -O 1.3.6.1.4.1.9.9.280.1.2.3.1.5 -T STRING -A RO -X equal -F 0"

		readonly gauge	cOutageEventInterval
		description 	"Object that identifies the time duration between  
        transitions in available state of a component.  
        According to the cOutageEventTypeIndex, If the event type 
        is up, the interval time from the last down time is  
        TTR (Time To Recovery).  
        Otherwise, the interval time from the last up is TTF  
        (Time To Failure)."
		characteristics "-N cOutageEventInterval -O 1.3.6.1.4.1.9.9.280.1.2.3.1.6 -T GAUGE -A RO -X equal -F 0"

	)

	table	cOutageEventTypeMapTable
	description "A table containing information about  
        cOutageEventTypeMapIndex value mapping.  
        Each conceptual row specifies a unique mapping  
        between a cOutageEventTypeMapIndex value, and an  
        outage related event type."
	characteristics "-K ONE"
	(
		notaccessible gauge	cOutageEventTypeMapIndex
		description 	"This object uniquely identifies an event description."
		characteristics "-N cOutageEventTypeMapIndex -O 1.3.6.1.4.1.9.9.280.1.3.1.1.1 -T GAUGE -A NA -X equal -F 0"

		readonly octet[128]	cOutageEventTypeName
		description 	"The name of the outage related event."
		characteristics "-N cOutageEventTypeName -O 1.3.6.1.4.1.9.9.280.1.3.1.1.2 -T STRING -A RO -X equal -F 0"

		readonly octet[255]	cOutageEventTypeDescrText
		description 	"This object specifies a human-readable  
        message describing information about the  
        outage related event."
		characteristics "-N cOutageEventTypeDescrText -O 1.3.6.1.4.1.9.9.280.1.3.1.1.3 -T STRING -A RO -X equal -F 0"

	)

	table	cOutageObjectTable
	description "A table of monitored object entries generated  
        by this router, which include interface, physical  
        entry, and software process. 
        
        The conceptual row entries are initially established 
        during the monitoring start-up procedure based upon  
        configuration set-up. The entries are also dynamically  
        added into this table when a new object is detected.  
        
        The entry information will be updated, when the outage  
        related event occurs. 
        
        The entry will be removed only by configuration reset."
	characteristics "-K TWO"
	(
		notaccessible enum cOutageObjectTypeE	cOutageObjectType
		description 	"This object specifies the monitored object's type.  
        It comprises interface(1), physicalEntity(2),  
        swProcess(3), remoteObject(4), and logicalEntity(5).  
        
        The detailed description has been presented above  
        in the OutageMonObjectType definition."
		characteristics "-N cOutageObjectType -O 1.3.6.1.4.1.9.9.280.1.4.1.1.1 -T INTEGER -A NA -X equal -F 0"

		notaccessible gauge	cOutageMonitoredObjectIndex
		description 	"This object identifies a specific index of the  
        monitored object based upon the cOutageObjectType.  
        If it is interface(1), it correlates the object  
        with ifIndex in ifTable in the IF-MIB.          
        If it is PhysicalEntity(2), it correlates the object  
        with entPhysicalIndex in entPhysicalTable in the  
        ENTITY-MIB.  
        If it is SWProcess(3), the value corresponds to an entry 
        in the cOutageCpmMapTable, which correlates the object  
        with cpmCPUTotalIndex and cpmProcessPID in 
        cpmProcessTable in the CISCO-PROCESS-MIB. 
        If it is remoteObject(4), the value corresponds to an 
        entry in the cOutageRemoteObjMapTable, which correlates the 
        object with its ID and description. 
        If it is logicalEntity(5), the value corresponds to an  
        entry in the cOutageLogicalObjMapTable, which correlates the  
        object with the description."
		characteristics "-N cOutageMonitoredObjectIndex -O 1.3.6.1.4.1.9.9.280.1.4.1.1.2 -T GAUGE -A NA -X equal -F 0"

		readonly enum cOutageCurrentStatusE	cOutageCurrentStatus
		description 	"The current operational state of the monitored object.  
        The Up(1) state indicates that the monitored object is  
        actively or passively running in the normal condition.  
        The Down(2) state indicates that the monitored object  
        is experiencing some degree of outage."
		characteristics "-N cOutageCurrentStatus -O 1.3.6.1.4.1.9.9.280.1.4.1.1.3 -T INTEGER -A RO -X equal -F 0"

		readonly gauge	cOutageAOTSinceMeasureStarted
		description 	"The total outage time in second of a component from the  
        beginning of the measurement. The table stores the outage   
        time up to the last recovery event. However, a request   
        comes in during the down status of a component, the report   
        adds the outage time between the last failure to the 
        current."
		characteristics "-N cOutageAOTSinceMeasureStarted -O 1.3.6.1.4.1.9.9.280.1.4.1.1.4 -T GAUGE -A RO -X equal -F 0"

		readonly gauge	cOutageNAFSinceMeasureStarted
		description 	"The total number of failure instances from the beginning  
        of the measurement. Every failure event of a component  
        adds 1 to this object."
		characteristics "-N cOutageNAFSinceMeasureStarted -O 1.3.6.1.4.1.9.9.280.1.4.1.1.5 -T GAUGE -A RO -X equal -F 0"

		readonly octet[11]	cOutageRecordingStartTime
		description 	"This object specifies the date and time of the 
        object to start outage recording. 
        
        The instance value of this cOutageRecordingStartTime  
        value is initialized to the time when the monitored  
        object is added into this cOutageObjectTable."
		characteristics "-N cOutageRecordingStartTime -O 1.3.6.1.4.1.9.9.280.1.4.1.1.6 -T STRING -A RO -X equal -F 0"

	)

	table	cOutageCpmMapTable
	description "A table containing information about 
        cOutageMonitoredObjectIndex value mapping, 
        when the cOutageObjectType is swProcess. 
        Each conceptual row specifies a unique mapping 
        between a cOutageMonitoredObjectIndex value, and  
        cpmCPUTotalIndex and cpmProcessPID in 
        cpmProcessTable in the CISCO-PROCESS-MIB."
	characteristics "-K ONE"
	(
		notaccessible gauge	cOutageCpmMapIndex
		description 	"This object uniquely identifies cpmCPUTotalIndex and  
        cpmProcessPID in CISCO-PROCESS-MIB. The cpmCPUTotalIndex  
        is used for the cpmCPUTotalTable reference. A pair of  
        { cpmCPUTotalIndex, cpmProcessPID } is used for the  
        cpmProcessTable reference."
		characteristics "-N cOutageCpmMapIndex -O 1.3.6.1.4.1.9.9.280.1.5.1.1.1 -T GAUGE -A NA -X equal -F 0"

		readonly gauge	cOutageCpmCPUTotalIndex
		description 	"An index that uniquely represents a CPU in cpmCPUTotalTable 
        in CISCO-PROCESS-MIB."
		characteristics "-N cOutageCpmCPUTotalIndex -O 1.3.6.1.4.1.9.9.280.1.5.1.1.2 -T GAUGE -A RO -X equal -F 0"

		readonly gauge	cOutageCpmProcessPID
		description 	"This object contains the process ID. It identifies an  
        entry combined with cpmCPUTotalIndex in cpmProcessTable  
        in CISCO-PROCESS-MIB."
		characteristics "-N cOutageCpmProcessPID -O 1.3.6.1.4.1.9.9.280.1.5.1.1.3 -T GAUGE -A RO -X equal -F 0"

	)

	table	cOutageRemoteObjMapTable
	description "A table containing information about 
        cOutageMonitoredObjectIndex value mapping, 
        when the cOutageObjectType is remoteObject. 
        Each conceptual row specifies a unique mapping 
        between a cOutageMonitoredObjectIndex value, and 
        a remote object related information such as the 
        object ID and the object description.  
        Using IP ICMP protocol to monitor the remote object,  
        the object ID can be Network Layer Address, which  
        has been configured by users via configuration file  
        or CLI."
	characteristics "-K ONE"
	(
		notaccessible gauge	cOutageRemoteObjMapIndex
		description 	"This object uniquely identifies a remote object 
        description."
		characteristics "-N cOutageRemoteObjMapIndex -O 1.3.6.1.4.1.9.9.280.1.6.1.1.1 -T GAUGE -A NA -X equal -F 0"

		readonly enum cOutageRemoteObjIDTypeE	cOutageRemoteObjIDType
		description 	"Represents the type of address stored in 
        cOutageRemoteObjID."
		characteristics "-N cOutageRemoteObjIDType -O 1.3.6.1.4.1.9.9.280.1.6.1.1.2 -T INTEGER -A RO -X equal -F 0"

		readonly octet[255]	cOutageRemoteObjID
		description 	"This object represents the Network Layer Address 
        of the remote object, which is configured by 
        users via configuration file or CLI.  
        It could be an IP address or DNS  
        depends on users input."
		characteristics "-N cOutageRemoteObjID -O 1.3.6.1.4.1.9.9.280.1.6.1.1.3 -T STRING -A RO -X equal -F 0"

		readonly octet[255]	cOutageRemoteObjDescrText
		description 	"This object specifies a human-readable 
        message describing information about the 
        monitored remote components such as router name  
        or interface name, etc."
		characteristics "-N cOutageRemoteObjDescrText -O 1.3.6.1.4.1.9.9.280.1.6.1.1.4 -T STRING -A RO -X equal -F 0"

	)

	table	cOutageLogicalObjMapTable
	description "A table containing information about 
        cOutageMonitoredObjectIndex value mapping, 
        when the cOutageObjectType is logicalEntity. 
        Each conceptual row specifies a unique mapping 
        between a cOutageMonitoredObjectIndex value, and 
        a logical entity object related information such  
        as the object description."
	characteristics "-K ONE"
	(
		notaccessible gauge	cOutageLogicalObjMapIndex
		description 	"This object uniquely identifies a logical entity  
        object description."
		characteristics "-N cOutageLogicalObjMapIndex -O 1.3.6.1.4.1.9.9.280.1.7.1.1.1 -T GAUGE -A NA -X equal -F 0"

		readonly octet[255]	cOutageLogicalObjDescrText
		description 	"This object specifies a human-readable 
        message describing a detailed information  
        about the monitored logical entity."
		characteristics "-N cOutageLogicalObjDescrText -O 1.3.6.1.4.1.9.9.280.1.7.1.1.2 -T STRING -A RO -X equal -F 0"

	)

agenterrors (
	1	"cannot dispatch request"
	2	"select(2) failed"
	3	"sendto(2) failed"
	4	"recvfrom(2) failed"
	5	"no response from system"
	6	"response too big"
	7	"missing attribute"
	8	"bad attribute type"
	9	"cannot get sysUpTime"
	10	"sysUpTime type bad"
	11	"unknown SNMP error"
	12	"bad variable value"
	13	"variable is read only"
	14	"general error"
	15	"cannot make request PDU"
	16	"cannot make request varbind list"
	17	"cannot parse response PDU"
	18	"request ID - response ID mismatch"
	19	"string contains non-displayable characters"
	20	"cannot open schema file"
	21	"cannot parse schema file"
	22	"cannot open host file"
	23	"cannot parse host file"
	24	"attribute unavailable for set operations"
             )
)


## End of Agent Definitions
