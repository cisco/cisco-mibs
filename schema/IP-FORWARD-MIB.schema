## THIS FILE WAS GENERATED BY MIB2SCHEMA
## For attributes containing printable ASCII
## characters, you may add a format string
## specification in the attribute characteristics
## line
## As described in RFC 1157, some agents cannot
## accept messages whose length exceeds 484 octets
## For those groups whose var-bind list has many
## entries, you may split them up into different
## var-bind lists
proxy IP-FORWARD-MIB
description "IP-FORWARD-MIB agent"
serial 2
rpcid 100122
(

## Enumerated Type Definitions

	enum	ipCidrRouteTypeE (
	   1	"other"
	   2	"reject"
	   3	"local"
	   4	"remote"
	)

	enum	ipCidrRouteProtoE (
	   1	"other"
	   2	"local"
	   3	"netmgmt"
	   4	"icmp"
	   5	"egp"
	   6	"ggp"
	   7	"hello"
	   8	"rip"
	   9	"isIs"
	   10	"esIs"
	   11	"ciscoIgrp"
	   12	"bbnSpfIgp"
	   13	"ospf"
	   14	"bgp"
	   15	"idpr"
	   16	"ciscoEigrp"
	)

	enum	ipCidrRouteStatusE (
	   1	"active"
	   2	"notInService"
	   3	"notReady"
	   4	"createAndGo"
	   5	"createAndWait"
	   6	"destroy"
	)

	enum	ipForwardTypeE (
	   1	"other"
	   2	"invalid"
	   3	"local"
	   4	"remote"
	)

	enum	ipForwardProtoE (
	   1	"other"
	   2	"local"
	   3	"netmgmt"
	   4	"icmp"
	   5	"egp"
	   6	"ggp"
	   7	"hello"
	   8	"rip"
	   9	"is-is"
	   10	"es-is"
	   11	"ciscoIgrp"
	   12	"bbnSpfIgp"
	   13	"ospf"
	   14	"bgp"
	   15	"idpr"
	)


## Group and Table Declarations

	group	ipForward
	description "(null)"
	characteristics "-K NONE"
	(
		readonly gauge	ipCidrRouteNumber
		description 	"The number of current ipCidrRouteTable entries
        that are not invalid."
		characteristics "-N ipCidrRouteNumber -O 1.3.6.1.2.1.4.24.3 -T GAUGE -A RO -X equal -F 0"

		readonly gauge	ipForwardNumber
		description 	"The number of current  ipForwardTable  entries
        that are not invalid."
		characteristics "-N ipForwardNumber -O 1.3.6.1.2.1.4.24.1 -T GAUGE -A RO -X equal -F 0"

	)

	table	ipCidrRouteTable
	description "This entity's IP Routing table."
	characteristics "-K THIRTEEN"
	(
		readonly netaddress	ipCidrRouteDest
		description 	"The destination IP address of this route.
        
        This object may not take a Multicast (Class  D)
        address value.
        
        Any assignment (implicit or  otherwise)  of  an
        instance  of  this  object to a value x must be
        rejected if the bitwise logical-AND of  x  with
        the  value of the corresponding instance of the
        ipCidrRouteMask object is not equal to x."
		characteristics "-N ipCidrRouteDest -O 1.3.6.1.2.1.4.24.4.1.1 -T IPADDRESS -A RO -X equal -F 0"

		readonly netaddress	ipCidrRouteMask
		description 	"Indicate the mask to be logical-ANDed with the
        destination  address  before  being compared to
        the value  in  the  ipCidrRouteDest  field.   For
        those  systems  that  do  not support arbitrary
        subnet masks, an agent constructs the value  of
        the  ipCidrRouteMask  by  reference to the IP Ad-
        dress Class.
        
        Any assignment (implicit or  otherwise)  of  an
        instance  of  this  object to a value x must be
        rejected if the bitwise logical-AND of  x  with
        the  value of the corresponding instance of the
        ipCidrRouteDest object is not equal to ipCidrRoute-
        Dest."
		characteristics "-N ipCidrRouteMask -O 1.3.6.1.2.1.4.24.4.1.2 -T IPADDRESS -A RO -X equal -F 0"

		readonly int	ipCidrRouteTos
		description 	"The policy specifier is the IP TOS Field.  The encoding
        of IP TOS is as specified  by  the  following convention.
        Zero indicates the default path if no more  specific
        policy applies.
        
        +-----+-----+-----+-----+-----+-----+-----+-----+
        |                 |                       |     |
        |   PRECEDENCE    |    TYPE OF SERVICE    |  0  |
        |                 |                       |     |
        +-----+-----+-----+-----+-----+-----+-----+-----+
        
                 IP TOS                IP TOS
            Field     Policy      Field     Policy
            Contents    Code      Contents    Code
            0 0 0 0  ==>   0      0 0 0 1  ==>   2
            0 0 1 0  ==>   4      0 0 1 1  ==>   6
            0 1 0 0  ==>   8      0 1 0 1  ==>  10
            0 1 1 0  ==>  12      0 1 1 1  ==>  14
            1 0 0 0  ==>  16      1 0 0 1  ==>  18
            1 0 1 0  ==>  20      1 0 1 1  ==>  22
            1 1 0 0  ==>  24      1 1 0 1  ==>  26
            1 1 1 0  ==>  28      1 1 1 1  ==>  30"
		characteristics "-N ipCidrRouteTos -O 1.3.6.1.2.1.4.24.4.1.3 -T INTEGER -A RO -X equal -F 0"

		readonly netaddress	ipCidrRouteNextHop
		description 	"On remote routes, the address of the next sys-
        tem en route; Otherwise, 0.0.0.0."
		characteristics "-N ipCidrRouteNextHop -O 1.3.6.1.2.1.4.24.4.1.4 -T IPADDRESS -A RO -X equal -F 0"

		readwrite int	ipCidrRouteIfIndex
		description 	"The ifIndex value which identifies  the  local
        interface  through  which  the next hop of this
        route should be reached."
		characteristics "-N ipCidrRouteIfIndex -O 1.3.6.1.2.1.4.24.4.1.5 -T INTEGER -A RW -X equal -F 0"

		readwrite enum ipCidrRouteTypeE	ipCidrRouteType
		description 	"The type of route.  Note that local(3)  refers
        to  a route for which the next hop is the final
        destination; remote(4) refers to  a  route  for
        which  the  next  hop is not the final destina-
        tion.
        
        Routes which do not result in traffic forwarding or
        rejection should not be displayed even if the
        implementation keeps them stored internally.
        
        
        reject (2) refers to a route which, if matched, discards
        the message as unreachable. This is used in some
        protocols as a means of correctly aggregating routes."
		characteristics "-N ipCidrRouteType -O 1.3.6.1.2.1.4.24.4.1.6 -T INTEGER -A RW -X equal -F 0"

		readonly enum ipCidrRouteProtoE	ipCidrRouteProto
		description 	"The routing mechanism via which this route was
        learned.  Inclusion of values for gateway rout-
        ing protocols is not  intended  to  imply  that
        hosts should support those protocols."
		characteristics "-N ipCidrRouteProto -O 1.3.6.1.2.1.4.24.4.1.7 -T INTEGER -A RO -X equal -F 0"

		readonly int	ipCidrRouteAge
		description 	"The number of seconds  since  this  route  was
        last  updated  or  otherwise  determined  to be
        correct.  Note that no semantics of  `too  old'
        can  be implied except through knowledge of the
        routing  protocol  by  which  the   route   was
        learned."
		characteristics "-N ipCidrRouteAge -O 1.3.6.1.2.1.4.24.4.1.8 -T INTEGER -A RO -X equal -F 0"

		readwrite objectid	ipCidrRouteInfo
		description 	"A reference to MIB definitions specific to the
        particular  routing protocol which is responsi-
        ble for this route, as determined by the  value
        specified  in the route's ipCidrRouteProto value.
        If this information is not present,  its  value
        should be set to the OBJECT IDENTIFIER { 0 0 },
        which is a syntactically valid object  identif-
        ier, and any implementation conforming to ASN.1
        and the Basic Encoding Rules must  be  able  to
        generate and recognize this value."
		characteristics "-N ipCidrRouteInfo -O 1.3.6.1.2.1.4.24.4.1.9 -T OBJECTID -A RW -X equal -F 0"

		readwrite int	ipCidrRouteNextHopAS
		description 	"The Autonomous System Number of the Next  Hop.
        The  semantics of this object are determined by
        the routing-protocol specified in  the  route's
        ipCidrRouteProto  value. When  this object is
        unknown or not relevant its value should be set
        to zero."
		characteristics "-N ipCidrRouteNextHopAS -O 1.3.6.1.2.1.4.24.4.1.10 -T INTEGER -A RW -X equal -F 0"

		readwrite int	ipCidrRouteMetric1
		description 	"The primary routing  metric  for  this  route.
        The  semantics of this metric are determined by
        the routing-protocol specified in  the  route's
        ipCidrRouteProto  value.   If  this metric is not
        used, its value should be set to -1."
		characteristics "-N ipCidrRouteMetric1 -O 1.3.6.1.2.1.4.24.4.1.11 -T INTEGER -A RW -X equal -F 0"

		readwrite int	ipCidrRouteMetric2
		description 	"An alternate routing metric  for  this  route.
        The  semantics of this metric are determined by
        the routing-protocol specified in  the  route's
        ipCidrRouteProto  value.   If  this metric is not
        used, its value should be set to -1."
		characteristics "-N ipCidrRouteMetric2 -O 1.3.6.1.2.1.4.24.4.1.12 -T INTEGER -A RW -X equal -F 0"

		readwrite int	ipCidrRouteMetric3
		description 	"An alternate routing metric  for  this  route.
        The  semantics of this metric are determined by
        the routing-protocol specified in  the  route's
        ipCidrRouteProto  value.   If  this metric is not
        used, its value should be set to -1."
		characteristics "-N ipCidrRouteMetric3 -O 1.3.6.1.2.1.4.24.4.1.13 -T INTEGER -A RW -X equal -F 0"

		readwrite int	ipCidrRouteMetric4
		description 	"An alternate routing metric  for  this  route.
        The  semantics of this metric are determined by
        the routing-protocol specified in  the  route's
        ipCidrRouteProto  value.   If  this metric is not
        used, its value should be set to -1."
		characteristics "-N ipCidrRouteMetric4 -O 1.3.6.1.2.1.4.24.4.1.14 -T INTEGER -A RW -X equal -F 0"

		readwrite int	ipCidrRouteMetric5
		description 	"An alternate routing metric  for  this  route.
        The  semantics of this metric are determined by
        the routing-protocol specified in  the  route's
        ipCidrRouteProto  value.   If  this metric is not
        used, its value should be set to -1."
		characteristics "-N ipCidrRouteMetric5 -O 1.3.6.1.2.1.4.24.4.1.15 -T INTEGER -A RW -X equal -F 0"

		readwrite enum ipCidrRouteStatusE	ipCidrRouteStatus
		description 	"The row status variable, used according to
        row installation and removal conventions."
		characteristics "-N ipCidrRouteStatus -O 1.3.6.1.2.1.4.24.4.1.16 -T INTEGER -A RW -X equal -F 0"

	)

	table	ipForwardTable
	description "This entity's IP Routing table."
	characteristics "-K TEN"
	(
		readonly netaddress	ipForwardDest
		description 	"The destination IP address of this route.   An
        entry  with  a value of 0.0.0.0 is considered a
        default route.
        
        This object may not take a Multicast (Class  D)
        address value.
        
        Any assignment (implicit or  otherwise)  of  an
        instance  of  this  object to a value x must be
        rejected if the bitwise logical-AND of  x  with
        the  value of the corresponding instance of the
        ipForwardMask object is not equal to x."
		characteristics "-N ipForwardDest -O 1.3.6.1.2.1.4.24.2.1.1 -T IPADDRESS -A RO -X equal -F 0"

		readwrite netaddress	ipForwardMask
		description 	"Indicate the mask to be logical-ANDed with the
        destination  address  before  being compared to
        the value  in  the  ipForwardDest  field.   For
        those  systems  that  do  not support arbitrary
        subnet masks, an agent constructs the value  of
        the  ipForwardMask  by  reference to the IP Ad-
        dress Class.
        
        Any assignment (implicit or  otherwise)  of  an
        instance  of  this  object to a value x must be
        rejected if the bitwise logical-AND of  x  with
        the  value of the corresponding instance of the
        ipForwardDest object is not equal to ipForward-
        Dest."
		characteristics "-N ipForwardMask -O 1.3.6.1.2.1.4.24.2.1.2 -T IPADDRESS -A RW -X equal -F 0"

		readonly int	ipForwardPolicy
		description 	"The general set of conditions that would cause
        the  selection  of  one multipath route (set of
        next hops for a given destination) is  referred
        to as 'policy'.
        
        Unless the mechanism indicated by ipForwardPro-
        to specifies otherwise, the policy specifier is
        the IP TOS Field.  The encoding of IP TOS is as
         specified  by  the  following convention.  Zero
        indicates the default path if no more  specific
        policy applies.
        
        +-----+-----+-----+-----+-----+-----+-----+-----+
        |                 |                       |     |
        |   PRECEDENCE    |    TYPE OF SERVICE    |  0  |
        |                 |                       |     |
        +-----+-----+-----+-----+-----+-----+-----+-----+
        
                 IP TOS                IP TOS
            Field     Policy      Field     Policy
            Contents    Code      Contents    Code
            0 0 0 0  ==>   0      0 0 0 1  ==>   2
            0 0 1 0  ==>   4      0 0 1 1  ==>   6
            0 1 0 0  ==>   8      0 1 0 1  ==>  10
            0 1 1 0  ==>  12      0 1 1 1  ==>  14
            1 0 0 0  ==>  16      1 0 0 1  ==>  18
            1 0 1 0  ==>  20      1 0 1 1  ==>  22
            1 1 0 0  ==>  24      1 1 0 1  ==>  26
            1 1 1 0  ==>  28      1 1 1 1  ==>  30
        
        Protocols defining 'policy' otherwise must  ei-
        ther define a set of values which are valid for
        this  object  or  must  implement  an  integer-
        instanced  policy table for which this object's
        value acts as an index."
		characteristics "-N ipForwardPolicy -O 1.3.6.1.2.1.4.24.2.1.3 -T INTEGER -A RO -X equal -F 0"

		readonly netaddress	ipForwardNextHop
		description 	"On remote routes, the address of the next sys-
        tem en route; Otherwise, 0.0.0.0."
		characteristics "-N ipForwardNextHop -O 1.3.6.1.2.1.4.24.2.1.4 -T IPADDRESS -A RO -X equal -F 0"

		readwrite int	ipForwardIfIndex
		description 	"The ifIndex value which identifies  the  local
        interface  through  which  the next hop of this
        route should be reached."
		characteristics "-N ipForwardIfIndex -O 1.3.6.1.2.1.4.24.2.1.5 -T INTEGER -A RW -X equal -F 0"

		readwrite enum ipForwardTypeE	ipForwardType
		description 	"The type of route.  Note that local(3)  refers
        to  a route for which the next hop is the final
        destination; remote(4) refers to  a  route  for
        which  the  next  hop is not the final destina-
        tion.
        
        Setting this object to the value invalid(2) has
        the  effect  of  invalidating the corresponding
        entry in the ipForwardTable object.   That  is,
        it  effectively  disassociates  the destination
        identified with said entry from the route iden-
        tified    with    said   entry.    It   is   an
        implementation-specific matter  as  to  whether
        the agent removes an invalidated entry from the
        table.  Accordingly, management  stations  must
        be prepared to receive tabular information from
        agents that corresponds to entries not current-
        ly  in  use.  Proper interpretation of such en-
        tries requires examination of the relevant  ip-
        ForwardType object."
		characteristics "-N ipForwardType -O 1.3.6.1.2.1.4.24.2.1.6 -T INTEGER -A RW -X equal -F 0"

		readonly enum ipForwardProtoE	ipForwardProto
		description 	"The routing mechanism via which this route was
        learned.  Inclusion of values for gateway rout-
        ing protocols is not  intended  to  imply  that
        hosts should support those protocols."
		characteristics "-N ipForwardProto -O 1.3.6.1.2.1.4.24.2.1.7 -T INTEGER -A RO -X equal -F 0"

		readonly int	ipForwardAge
		description 	"The number of seconds  since  this  route  was
        last  updated  or  otherwise  determined  to be
        correct.  Note that no semantics of  `too  old'
        can  be implied except through knowledge of the
        routing  protocol  by  which  the   route   was
        learned."
		characteristics "-N ipForwardAge -O 1.3.6.1.2.1.4.24.2.1.8 -T INTEGER -A RO -X equal -F 0"

		readwrite objectid	ipForwardInfo
		description 	"A reference to MIB definitions specific to the
        particular  routing protocol which is responsi-
        ble for this route, as determined by the  value
        specified  in the route's ipForwardProto value.
        If this information is not present,  its  value
        should be set to the OBJECT IDENTIFIER { 0 0 },
        which is a syntactically valid object  identif-
        ier, and any implementation conforming to ASN.1
        and the Basic Encoding Rules must  be  able  to
        generate and recognize this value."
		characteristics "-N ipForwardInfo -O 1.3.6.1.2.1.4.24.2.1.9 -T OBJECTID -A RW -X equal -F 0"

		readwrite int	ipForwardNextHopAS
		description 	"The Autonomous System Number of the Next  Hop.
        When  this  is  unknown  or not relevant to the
        protocol indicated by ipForwardProto, zero."
		characteristics "-N ipForwardNextHopAS -O 1.3.6.1.2.1.4.24.2.1.10 -T INTEGER -A RW -X equal -F 0"

		readwrite int	ipForwardMetric1
		description 	"The primary routing  metric  for  this  route.
        The  semantics of this metric are determined by
        the routing-protocol specified in  the  route's
        ipForwardProto  value.   If  this metric is not
        used, its value should be set to -1."
		characteristics "-N ipForwardMetric1 -O 1.3.6.1.2.1.4.24.2.1.11 -T INTEGER -A RW -X equal -F 0"

		readwrite int	ipForwardMetric2
		description 	"An alternate routing metric  for  this  route.
        The  semantics of this metric are determined by
        the routing-protocol specified in  the  route's
        ipForwardProto  value.   If  this metric is not
        used, its value should be set to -1."
		characteristics "-N ipForwardMetric2 -O 1.3.6.1.2.1.4.24.2.1.12 -T INTEGER -A RW -X equal -F 0"

		readwrite int	ipForwardMetric3
		description 	"An alternate routing metric  for  this  route.
        The  semantics of this metric are determined by
        the routing-protocol specified in  the  route's
        ipForwardProto  value.   If  this metric is not
        used, its value should be set to -1."
		characteristics "-N ipForwardMetric3 -O 1.3.6.1.2.1.4.24.2.1.13 -T INTEGER -A RW -X equal -F 0"

		readwrite int	ipForwardMetric4
		description 	"An alternate routing metric  for  this  route.
        The  semantics of this metric are determined by
        the routing-protocol specified in  the  route's
        ipForwardProto  value.   If  this metric is not
        used, its value should be set to -1."
		characteristics "-N ipForwardMetric4 -O 1.3.6.1.2.1.4.24.2.1.14 -T INTEGER -A RW -X equal -F 0"

		readwrite int	ipForwardMetric5
		description 	"An alternate routing metric  for  this  route.
        The  semantics of this metric are determined by
        the routing-protocol specified in  the  route's
        ipForwardProto  value.   If  this metric is not
        used, its value should be set to -1."
		characteristics "-N ipForwardMetric5 -O 1.3.6.1.2.1.4.24.2.1.15 -T INTEGER -A RW -X equal -F 0"

	)

agenterrors (
	1	"cannot dispatch request"
	2	"select(2) failed"
	3	"sendto(2) failed"
	4	"recvfrom(2) failed"
	5	"no response from system"
	6	"response too big"
	7	"missing attribute"
	8	"bad attribute type"
	9	"cannot get sysUpTime"
	10	"sysUpTime type bad"
	11	"unknown SNMP error"
	12	"bad variable value"
	13	"variable is read only"
	14	"general error"
	15	"cannot make request PDU"
	16	"cannot make request varbind list"
	17	"cannot parse response PDU"
	18	"request ID - response ID mismatch"
	19	"string contains non-displayable characters"
	20	"cannot open schema file"
	21	"cannot parse schema file"
	22	"cannot open host file"
	23	"cannot parse host file"
	24	"attribute unavailable for set operations"
             )
)


## End of Agent Definitions
