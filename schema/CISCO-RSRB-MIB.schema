## THIS FILE WAS GENERATED BY MIB2SCHEMA
## For attributes containing printable ASCII
## characters, you may add a format string
## specification in the attribute characteristics
## line
## As described in RFC 1157, some agents cannot
## accept messages whose length exceeds 484 octets
## For those groups whose var-bind list has many
## entries, you may split them up into different
## var-bind lists
proxy CISCO-RSRB-MIB
description "CISCO-RSRB-MIB agent"
serial 2
rpcid 100122
(

## Enumerated Type Definitions

	enum	rsrbRemotePeerEncapsulationE (
	   1	"tcp"
	   2	"serial"
	   3	"lan"
	   4	"fst"
	   5	"frameRelay"
	)

	enum	rsrbRemotePeerStateE (
	   1	"dead"
	   2	"closed"
	   3	"opening"
	   4	"openWaitXport"
	   5	"waitRemoteRsp"
	   6	"remoteResponded"
	   7	"remoteOpened"
	   8	"draining"
	   9	"connected"
	)

	enum	rsrbRemotePeerLocalAckE (
	   1	"true"
	   2	"false"
	)

	enum	rsrbRingLocalE (
	   1	"true"
	   2	"false"
	)

	enum	rsrbRingTypeE (
	   1	"static"
	   2	"dynamic"
	   3	"sdllc"
	   4	"qllc"
	   5	"virtual"
	)


## Group and Table Declarations


## Group and Table Declarations

	table	rsrbVirtRingTable
	description "Table of virtual rings in the router."
	characteristics "-K ONE"
	(
		notaccessible int	rsrbVirtRingIndex
		description 	"The configured number of the virtual ring in which this peer is 
         participating."
		characteristics "-N rsrbVirtRingIndex -O 1.3.6.1.4.1.9.9.29.1.1.1.1.1 -T INTEGER -A NA -X equal -F 0"

		readonly netaddress	rsrbVirtRingIPAddr
		description 	"The IP address configured for this RSRB local peer."
		characteristics "-N rsrbVirtRingIPAddr -O 1.3.6.1.4.1.9.9.29.1.1.1.1.2 -T IPADDRESS -A RO -X equal -F 0"

		readonly int	rsrbVirtRingMaxTcpQSize
		description 	"The maximum number of packets that can be held in any single
         outbound TCP queue to a remote peer before packets are discarded.
         This value indicates the configured or default value."
		characteristics "-N rsrbVirtRingMaxTcpQSize -O 1.3.6.1.4.1.9.9.29.1.1.1.1.3 -T INTEGER -A RO -X equal -F 0"

	)

	table	rsrbRemotePeerTable
	description "Table of RSRB remote peers for a virtual ring."
	characteristics "-K ONE"
	(
		notaccessible int	rsrbRemotePeerIndex
		description 	"An arbitrary index that uniquely identifies a remote peer 
         within a virtual ring."
		characteristics "-N rsrbRemotePeerIndex -O 1.3.6.1.4.1.9.9.29.1.2.1.1.1 -T INTEGER -A NA -X equal -F 0"

		readonly enum rsrbRemotePeerEncapsulationE	rsrbRemotePeerEncapsulation
		description 	"How traffic is encapsulated for RSRB:
        
         tcp = IP datagrams over a TCP connection
         serial = point-to-point (serial) direct encapsulation
         lan = direct encapsulation over a LAN
         fst = IP datagrams over a Fast Sequenced Transport connection
         frameRelay = direct encapsulation over frame relay"
		characteristics "-N rsrbRemotePeerEncapsulation -O 1.3.6.1.4.1.9.9.29.1.2.1.1.2 -T INTEGER -A RO -X equal -F 0"

		readonly netaddress	rsrbRemotePeerIPAddr
		description 	"The IP address used by the RSRB remote peer.
         This field is meaningful only for TCP and FST encapsulation.
         For other encapsulation types 0.0.0.0 is returned."
		characteristics "-N rsrbRemotePeerIPAddr -O 1.3.6.1.4.1.9.9.29.1.2.1.1.3 -T IPADDRESS -A RO -X equal -F 0"

		readonly int	rsrbRemotePeerLocalIfIndex
		description 	"Local interface index value to the RSRB remote peer.  Same value
         as used for accessing the Interface MIB for the same interface.
         This field is meaningful only for serial, LAN and
         frame relay encapsulation.  For other encapsulation types 0 
         is returned."
		characteristics "-N rsrbRemotePeerLocalIfIndex -O 1.3.6.1.4.1.9.9.29.1.2.1.1.4 -T INTEGER -A RO -X equal -F 0"

		readonly enum rsrbRemotePeerStateE	rsrbRemotePeerState
		description 	"Current state of the RSRB session with the remote peer."
		characteristics "-N rsrbRemotePeerState -O 1.3.6.1.4.1.9.9.29.1.2.1.1.5 -T INTEGER -A RO -X equal -F 0"

		readonly counter	rsrbRemotePeerPacketsRx
		description 	"Number of packets received from remote peer."
		characteristics "-N rsrbRemotePeerPacketsRx -O 1.3.6.1.4.1.9.9.29.1.2.1.1.6 -T COUNTER -A RO -X equal -F 0"

		readonly counter	rsrbRemotePeerPacketsTx
		description 	"Number of packets transmitted to remote peer."
		characteristics "-N rsrbRemotePeerPacketsTx -O 1.3.6.1.4.1.9.9.29.1.2.1.1.7 -T COUNTER -A RO -X equal -F 0"

		readonly counter	rsrbRemotePeerBytesRx
		description 	"Number of bytes received from remote peer."
		characteristics "-N rsrbRemotePeerBytesRx -O 1.3.6.1.4.1.9.9.29.1.2.1.1.8 -T COUNTER -A RO -X equal -F 0"

		readonly counter	rsrbRemotePeerBytesTx
		description 	"Number of bytes transmitted to remote peer."
		characteristics "-N rsrbRemotePeerBytesTx -O 1.3.6.1.4.1.9.9.29.1.2.1.1.9 -T COUNTER -A RO -X equal -F 0"

		readonly counter	rsrbRemotePeerExplorersRx
		description 	"Number of explorer packets received from the remote peer."
		characteristics "-N rsrbRemotePeerExplorersRx -O 1.3.6.1.4.1.9.9.29.1.2.1.1.10 -T COUNTER -A RO -X equal -F 0"

		readonly int	rsrbRemotePeerTcpQueue
		description 	"Number of packets being held by the local router
         in the TCP output queue to the remote peer."
		characteristics "-N rsrbRemotePeerTcpQueue -O 1.3.6.1.4.1.9.9.29.1.2.1.1.11 -T INTEGER -A RO -X equal -F 0"

		readonly counter	rsrbRemotePeerDrops
		description 	"Number of received packets discarded by the local router.
         The reasons for packets being dropped include, but are not
         limited to, problems with the configuration, queue overflow,
         and protocol errors."
		characteristics "-N rsrbRemotePeerDrops -O 1.3.6.1.4.1.9.9.29.1.2.1.1.12 -T COUNTER -A RO -X equal -F 0"

		readonly enum rsrbRemotePeerLocalAckE	rsrbRemotePeerLocalAck
		description 	"Indicates if local acknowledgment is used for the sessions
         going to the remote peer.
        
         TRUE -> local acknowledgment is used
         FALSE -> local acknowledgment is not used"
		characteristics "-N rsrbRemotePeerLocalAck -O 1.3.6.1.4.1.9.9.29.1.2.1.1.13 -T INTEGER -A RO -X equal -F 0"

		readonly int	rsrbRemotePeerVersion
		description 	"The version of RSRB used between local and remote peer.
         Generally peers can communicate if they are within one version
         of each other.  The value returned is the lower version
         of the two peers."
		characteristics "-N rsrbRemotePeerVersion -O 1.3.6.1.4.1.9.9.29.1.2.1.1.14 -T INTEGER -A RO -X equal -F 0"

	)

	table	rsrbRingTable
	description "Table of local and remote rings associated with a 
         virtual ring."
	characteristics "-K ONE"
	(
		notaccessible int	rsrbRingIndex
		description 	"The configured ring number that uniquely identifies this network 
         segment or ring within the bridged token ring network."
		characteristics "-N rsrbRingIndex -O 1.3.6.1.4.1.9.9.29.1.3.1.1.1 -T INTEGER -A NA -X equal -F 0"

		readonly int	rsrbRingBridge
		description 	"The configured number of the bridge that connects this entry's
         ring to the virtual ring."
		characteristics "-N rsrbRingBridge -O 1.3.6.1.4.1.9.9.29.1.3.1.1.2 -T INTEGER -A RO -X equal -F 0"

		readonly enum rsrbRingLocalE	rsrbRingLocal
		description 	"Indicates if the entry represents a local or remote ring.
         TRUE indicates a local ring.
         FALSE indicates a remote ring."
		characteristics "-N rsrbRingLocal -O 1.3.6.1.4.1.9.9.29.1.3.1.1.3 -T INTEGER -A RO -X equal -F 0"

		readonly enum rsrbRingTypeE	rsrbRingType
		description 	"The ring type."
		characteristics "-N rsrbRingType -O 1.3.6.1.4.1.9.9.29.1.3.1.1.4 -T INTEGER -A RO -X equal -F 0"

		readonly octet[6]	rsrbRingMacAddr
		description 	"The mac address of either the local router's interface to the ring
         for local ring entries, or the remote peer's interface to the ring
         for remote ring entries.  Not valid for entry types
         remote SDLLC and QLLC; for these type '00 00 00 00 00 00' will be
         returned."
		characteristics "-N rsrbRingMacAddr -O 1.3.6.1.4.1.9.9.29.1.3.1.1.5 -T STRING -A RO -X equal -F 0"

		readonly int	rsrbRingLocalIfIndex
		description 	"For local ring entries, the local interface into the ring.  This is
         the same value as used for accessing the Interface MIB for the 
         same interface. 
         Not meaningful for remote ring entries; for these types
         0 will be returned."
		characteristics "-N rsrbRingLocalIfIndex -O 1.3.6.1.4.1.9.9.29.1.3.1.1.6 -T INTEGER -A RO -X equal -F 0"

		readonly netaddress	rsrbRingRemoteIpAddress
		description 	"For remote ring entries, the IP address of the remote RSRB peer.
         Not meaningful for local ring entries, and so 0.0.0.0 will be
         returned."
		characteristics "-N rsrbRingRemoteIpAddress -O 1.3.6.1.4.1.9.9.29.1.3.1.1.7 -T IPADDRESS -A RO -X equal -F 0"

		readonly counter	rsrbRingNbrPacketsFwd
		description 	"The number of packets forwarded by the local router onto this ring."
		characteristics "-N rsrbRingNbrPacketsFwd -O 1.3.6.1.4.1.9.9.29.1.3.1.1.8 -T COUNTER -A RO -X equal -F 0"

	)

agenterrors (
	1	"cannot dispatch request"
	2	"select(2) failed"
	3	"sendto(2) failed"
	4	"recvfrom(2) failed"
	5	"no response from system"
	6	"response too big"
	7	"missing attribute"
	8	"bad attribute type"
	9	"cannot get sysUpTime"
	10	"sysUpTime type bad"
	11	"unknown SNMP error"
	12	"bad variable value"
	13	"variable is read only"
	14	"general error"
	15	"cannot make request PDU"
	16	"cannot make request varbind list"
	17	"cannot parse response PDU"
	18	"request ID - response ID mismatch"
	19	"string contains non-displayable characters"
	20	"cannot open schema file"
	21	"cannot parse schema file"
	22	"cannot open host file"
	23	"cannot parse host file"
	24	"attribute unavailable for set operations"
             )
)


## End of Agent Definitions
